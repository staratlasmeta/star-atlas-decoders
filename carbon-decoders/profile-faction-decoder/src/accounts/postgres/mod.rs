//! This code was AUTOGENERATED using the Codama library.
pub mod profile_faction_account_row;

pub use self::profile_faction_account_row::*;

use super::ProfileFactionAccount;

pub struct ProfileFactionAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for ProfileFactionAccountsMigration {
    fn app(&self) -> &str {
        "profile-faction"
    }

    fn name(&self) -> &str {
        "profile_faction_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![Box::new(ProfileFactionAccountMigrationOperation)]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct ProfileFactionAccountWithMetadata(
    pub ProfileFactionAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(ProfileFactionAccount, carbon_core::account::AccountMetadata)>
    for ProfileFactionAccountWithMetadata
{
    fn from(value: (ProfileFactionAccount, carbon_core::account::AccountMetadata)) -> Self {
        ProfileFactionAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for ProfileFactionAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let ProfileFactionAccountWithMetadata(account, metadata) = self;

        match account {
            ProfileFactionAccount::ProfileFactionAccount(account) => {
                let row = profile_faction_account_row::ProfileFactionAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for ProfileFactionAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let ProfileFactionAccountWithMetadata(account, metadata) = self;
        match account {
            ProfileFactionAccount::ProfileFactionAccount(account) => {
                let row = profile_faction_account_row::ProfileFactionAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
