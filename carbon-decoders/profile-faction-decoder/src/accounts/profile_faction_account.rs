//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

use crate::types::Faction;

/// Stores a profile's enlisted faction on-chain.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct ProfileFactionAccount {
    /// The data version of this account.
    pub version: u8,
    /// The profile this faction enlistment is for.
    pub profile: Pubkey,
    /// The faction of the profile.
    pub faction: Faction,
    /// The bump for this account.
    pub bump: u8,
}

impl ProfileFactionAccount {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[14, 149, 119, 243, 145, 240, 79, 227] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
