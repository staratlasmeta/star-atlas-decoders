//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::ProfileFactionDecoder;

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod profile_faction_account;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum ProfileFactionAccount {
    ProfileFactionAccount(Box<profile_faction_account::ProfileFactionAccount>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for ProfileFactionDecoder {
    type AccountType = ProfileFactionAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = profile_faction_account::ProfileFactionAccount::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ProfileFactionAccount::ProfileFactionAccount(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
