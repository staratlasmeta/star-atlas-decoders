//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Synchronize an [locked_voter::Escrow] with the [LockerHistory]/[EscrowHistory].

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct Sync {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SyncInstructionAccounts {
    pub locker: solana_pubkey::Pubkey,
    pub escrow: solana_pubkey::Pubkey,
    pub locker_history: solana_pubkey::Pubkey,
    pub escrow_history: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Sync {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[4, 219, 40, 164, 21, 157, 189, 88] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for Sync {
    type ArrangedAccounts = SyncInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let locker = next_account(&mut iter)?;
        let escrow = next_account(&mut iter)?;
        let locker_history = next_account(&mut iter)?;
        let escrow_history = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(SyncInstructionAccounts {
            locker,
            escrow,
            locker_history,
            escrow_history,
            remaining: remaining.to_vec(),
        })
    }
}
