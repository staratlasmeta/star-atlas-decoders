//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// used as input struct when registering a Recipe

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RegisterRecipeInput {
    /// the recipe duration (seconds)
    pub duration: i64,
    /// the recipe minimum duration (seconds)
    pub min_duration: i64,
    /// the recipe namespace
    pub namespace: [u8; 32],
    /// the recipe fee amount
    pub fee_amount: Option<u64>,
    /// the maximum number of times that this recipe can be used; if not provided a limit will not be set
    pub usage_limit: Option<u64>,
    /// the recipe's value
    pub value: Option<u64>,
    /// the index of the key in the crafting permissions profile
    pub key_index: u16,
}
