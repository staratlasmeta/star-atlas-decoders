//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::RemoveRecipeIngredientInputGraphQL;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "RemoveRecipeIngredient")]
pub struct RemoveRecipeIngredientGraphQL {
    pub instruction_metadata: crate::instructions::graphql::InstructionMetadataGraphQL,
    pub input: RemoveRecipeIngredientInputGraphQL,
}

impl TryFrom<crate::instructions::postgres::RemoveRecipeIngredientRow>
    for RemoveRecipeIngredientGraphQL
{
    type Error = carbon_core::error::Error;
    fn try_from(
        row: crate::instructions::postgres::RemoveRecipeIngredientRow,
    ) -> Result<Self, Self::Error> {
        Ok(Self {
            instruction_metadata: row.instruction_metadata.into(),
            input: row.input.0.into(),
        })
    }
}
