//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// This account allows for a crafting "domain" which allows an admin to set up
/// craftable items, recipes and crafting facilities tied to a domain that the control

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct Domain {
    /// The data version of this account.
    pub version: u8,
    /// The [`Profile`] that handles the crafting program permissions
    pub profile: Pubkey,
    /// the namespace
    pub namespace: [u8; 32],
}

impl Domain {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[167, 191, 231, 63, 146, 41, 115, 27] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
