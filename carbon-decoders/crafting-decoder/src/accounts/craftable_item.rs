//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// This PDA represents a token registered as an item that can be crafted

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CraftableItem {
    /// The data version of this account.
    pub version: u8,
    /// the domain
    pub domain: Pubkey,
    /// the mint representing the items crafted
    pub mint: Pubkey,
    /// TODO: remove this
    pub creator: Pubkey,
    /// the name of this account
    pub namespace: [u8; 32],
    /// bump for PDA
    pub bump: u8,
}

impl CraftableItem {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[124, 246, 56, 8, 104, 95, 249, 251] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
