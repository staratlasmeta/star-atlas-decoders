//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::SrslyDecoder;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod accept_rental;
pub mod cancel_rental;
pub mod close_contract;
pub mod close_rental;
pub mod create_contract;
pub mod pay_rental;
pub mod reset_rental;

pub use self::accept_rental::*;
pub use self::cancel_rental::*;
pub use self::close_contract::*;
pub use self::close_rental::*;
pub use self::create_contract::*;
pub use self::pay_rental::*;
pub use self::reset_rental::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum SrslyInstruction {
    AcceptRental(AcceptRental),
    CancelRental(CancelRental),
    CloseContract(CloseContract),
    CloseRental(CloseRental),
    CreateContract(CreateContract),
    PayRental(PayRental),
    ResetRental(ResetRental),
}

impl carbon_core::instruction::InstructionDecoder<'_> for SrslyDecoder {
    type InstructionType = SrslyInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = accept_rental::AcceptRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SrslyInstruction::AcceptRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = cancel_rental::CancelRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SrslyInstruction::CancelRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_contract::CloseContract::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SrslyInstruction::CloseContract(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_rental::CloseRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SrslyInstruction::CloseRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_contract::CreateContract::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SrslyInstruction::CreateContract(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = pay_rental::PayRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SrslyInstruction::PayRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = reset_rental::ResetRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SrslyInstruction::ResetRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
