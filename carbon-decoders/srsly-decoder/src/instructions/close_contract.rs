//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Closes an existing rental contract, preventing further rentals.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CloseContract {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CloseContractInstructionAccounts {
    pub owner: solana_pubkey::Pubkey,
    pub owner_token_account: Option<solana_pubkey::Pubkey>,
    pub rental_token_account: Option<solana_pubkey::Pubkey>,
    pub borrower_token_account: Option<solana_pubkey::Pubkey>,
    pub rental_state: Option<solana_pubkey::Pubkey>,
    pub fleet: solana_pubkey::Pubkey,
    pub game_id: Option<solana_pubkey::Pubkey>,
    pub starbase: Option<solana_pubkey::Pubkey>,
    pub starbase_player: Option<solana_pubkey::Pubkey>,
    pub contract: solana_pubkey::Pubkey,
    pub rental_authority: solana_pubkey::Pubkey,
    pub sage_program: solana_pubkey::Pubkey,
    pub token_program: Option<solana_pubkey::Pubkey>,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CloseContract {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[37, 244, 34, 168, 92, 202, 80, 106] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for CloseContract {
    type ArrangedAccounts = CloseContractInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let owner = next_account(&mut iter)?;
        let owner_token_account = next_account(&mut iter);
        let rental_token_account = next_account(&mut iter);
        let borrower_token_account = next_account(&mut iter);
        let rental_state = next_account(&mut iter);
        let fleet = next_account(&mut iter)?;
        let game_id = next_account(&mut iter);
        let starbase = next_account(&mut iter);
        let starbase_player = next_account(&mut iter);
        let contract = next_account(&mut iter)?;
        let rental_authority = next_account(&mut iter)?;
        let sage_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter);

        let remaining = iter.as_slice();

        Some(CloseContractInstructionAccounts {
            owner,
            owner_token_account,
            rental_token_account,
            borrower_token_account,
            rental_state,
            fleet,
            game_id,
            starbase,
            starbase_player,
            contract,
            rental_authority,
            sage_program,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}
