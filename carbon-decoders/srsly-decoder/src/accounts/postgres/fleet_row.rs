//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct FleetRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub game_id: Pubkey,
    pub owner_profile: Pubkey,
    pub fleet_ships: Pubkey,
    pub sub_profile: Pubkey,
    pub sub_profile_invalidator: Pubkey,
}

impl FleetRow {
    pub fn from_parts(source: crate::accounts::fleet::Fleet, metadata: AccountMetadata) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            game_id: source.game_id.into(),
            owner_profile: source.owner_profile.into(),
            fleet_ships: source.fleet_ships.into(),
            sub_profile: source.sub_profile.into(),
            sub_profile_invalidator: source.sub_profile_invalidator.into(),
        }
    }
}

impl TryFrom<FleetRow> for crate::accounts::fleet::Fleet {
    type Error = carbon_core::error::Error;
    fn try_from(source: FleetRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            game_id: *source.game_id,
            owner_profile: *source.owner_profile,
            fleet_ships: *source.fleet_ships,
            sub_profile: *source.sub_profile,
            sub_profile_invalidator: *source.sub_profile_invalidator,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::fleet::Fleet {
    fn table() -> &'static str {
        "fleet_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "game_id",
            "owner_profile",
            "fleet_ships",
            "sub_profile",
            "sub_profile_invalidator",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for FleetRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO fleet_account (
                "version",
                "game_id",
                "owner_profile",
                "fleet_ships",
                "sub_profile",
                "sub_profile_invalidator",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.game_id.clone())
        .bind(self.owner_profile.clone())
        .bind(self.fleet_ships.clone())
        .bind(self.sub_profile.clone())
        .bind(self.sub_profile_invalidator.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for FleetRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO fleet_account (
                "version",
                "game_id",
                "owner_profile",
                "fleet_ships",
                "sub_profile",
                "sub_profile_invalidator",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "game_id" = EXCLUDED."game_id",
                "owner_profile" = EXCLUDED."owner_profile",
                "fleet_ships" = EXCLUDED."fleet_ships",
                "sub_profile" = EXCLUDED."sub_profile",
                "sub_profile_invalidator" = EXCLUDED."sub_profile_invalidator",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.game_id.clone())
        .bind(self.owner_profile.clone())
        .bind(self.fleet_ships.clone())
        .bind(self.sub_profile.clone())
        .bind(self.sub_profile_invalidator.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for FleetRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM fleet_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for FleetRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM fleet_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct FleetMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for FleetMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS fleet_account (
                -- Account data
                "version" INT2 NOT NULL,
                "game_id" BYTEA NOT NULL,
                "owner_profile" BYTEA NOT NULL,
                "fleet_ships" BYTEA NOT NULL,
                "sub_profile" BYTEA NOT NULL,
                "sub_profile_invalidator" BYTEA NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS fleet_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
