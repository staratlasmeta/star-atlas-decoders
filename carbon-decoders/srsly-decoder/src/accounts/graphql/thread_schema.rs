//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::ClockDataGraphQL;
use crate::types::graphql::ExecContextGraphQL;
use crate::types::graphql::SerializableInstructionGraphQL;
use crate::types::graphql::TriggerGraphQL;
use carbon_core::graphql::primitives::Pubkey;
use carbon_core::graphql::primitives::U8;
use carbon_core::graphql::primitives::U64;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Thread")]
pub struct ThreadGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub authority: Pubkey,
    pub bump: U8,
    pub created_at: ClockDataGraphQL,
    pub exec_context: Option<ExecContextGraphQL>,
    pub fee: U64,
    pub id: Vec<U8>,
    pub instructions: Vec<SerializableInstructionGraphQL>,
    pub name: String,
    pub next_instruction: Option<SerializableInstructionGraphQL>,
    pub paused: bool,
    pub rate_limit: U64,
    pub trigger: TriggerGraphQL,
}

impl TryFrom<crate::accounts::postgres::ThreadRow> for ThreadGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::ThreadRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            authority: carbon_core::graphql::primitives::Pubkey(row.authority.0),
            bump: carbon_core::graphql::primitives::U8((*row.bump) as u8),
            created_at: row.created_at.0.into(),
            exec_context: row.exec_context.map(|v| v.0.into()),
            fee: carbon_core::graphql::primitives::U64(*row.fee),
            id: row
                .id
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U8(item))
                .collect(),
            instructions: row
                .instructions
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
            name: row.name,
            next_instruction: row.next_instruction.map(|v| v.0.into()),
            paused: row.paused,
            rate_limit: carbon_core::graphql::primitives::U64(*row.rate_limit),
            trigger: row.trigger.0.into(),
        })
    }
}
