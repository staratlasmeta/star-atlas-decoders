//! This code was AUTOGENERATED using the Codama library.
use crate::MarketplaceDecoder;
use crate::PROGRAM_ID;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod add_fee_exemption;
pub mod add_royalty_tier;
pub mod delete_royalty_tier;
pub mod deregister_currency;
pub mod initialize_marketplace;
pub mod initialize_open_orders_counter;
pub mod noop;
pub mod process_cancel;
pub mod process_exchange;
pub mod process_initialize_buy;
pub mod process_initialize_buy_pda;
pub mod process_initialize_sell;
pub mod process_initialize_sell_pda;
pub mod register_currency;
pub mod remove_fee_exemption;
pub mod update_atlas_rate;
pub mod update_currency_royalty;
pub mod update_currency_vault;
pub mod update_royalty_tier;

pub use self::add_fee_exemption::*;
pub use self::add_royalty_tier::*;
pub use self::delete_royalty_tier::*;
pub use self::deregister_currency::*;
pub use self::initialize_marketplace::*;
pub use self::initialize_open_orders_counter::*;
pub use self::noop::*;
pub use self::process_cancel::*;
pub use self::process_exchange::*;
pub use self::process_initialize_buy::*;
pub use self::process_initialize_buy_pda::*;
pub use self::process_initialize_sell::*;
pub use self::process_initialize_sell_pda::*;
pub use self::register_currency::*;
pub use self::remove_fee_exemption::*;
pub use self::update_atlas_rate::*;
pub use self::update_currency_royalty::*;
pub use self::update_currency_vault::*;
pub use self::update_royalty_tier::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum MarketplaceInstruction {
    AddFeeExemption(AddFeeExemption),
    AddRoyaltyTier(AddRoyaltyTier),
    DeleteRoyaltyTier(DeleteRoyaltyTier),
    DeregisterCurrency(DeregisterCurrency),
    InitializeMarketplace(InitializeMarketplace),
    InitializeOpenOrdersCounter(InitializeOpenOrdersCounter),
    Noop(Noop),
    ProcessCancel(ProcessCancel),
    ProcessExchange(ProcessExchange),
    ProcessInitializeBuy(ProcessInitializeBuy),
    ProcessInitializeBuyPda(ProcessInitializeBuyPda),
    ProcessInitializeSell(ProcessInitializeSell),
    ProcessInitializeSellPda(ProcessInitializeSellPda),
    RegisterCurrency(RegisterCurrency),
    RemoveFeeExemption(RemoveFeeExemption),
    UpdateAtlasRate(UpdateAtlasRate),
    UpdateCurrencyRoyalty(UpdateCurrencyRoyalty),
    UpdateCurrencyVault(UpdateCurrencyVault),
    UpdateRoyaltyTier(UpdateRoyaltyTier),
}

impl carbon_core::instruction::InstructionDecoder<'_> for MarketplaceDecoder {
    type InstructionType = MarketplaceInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = add_fee_exemption::AddFeeExemption::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::AddFeeExemption(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = add_royalty_tier::AddRoyaltyTier::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::AddRoyaltyTier(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = delete_royalty_tier::DeleteRoyaltyTier::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::DeleteRoyaltyTier(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = deregister_currency::DeregisterCurrency::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::DeregisterCurrency(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = initialize_marketplace::InitializeMarketplace::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::InitializeMarketplace(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                initialize_open_orders_counter::InitializeOpenOrdersCounter::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::InitializeOpenOrdersCounter(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = noop::Noop::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::Noop(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_cancel::ProcessCancel::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::ProcessCancel(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_exchange::ProcessExchange::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::ProcessExchange(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_initialize_buy::ProcessInitializeBuy::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::ProcessInitializeBuy(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_initialize_buy_pda::ProcessInitializeBuyPda::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::ProcessInitializeBuyPda(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_initialize_sell::ProcessInitializeSell::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::ProcessInitializeSell(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                process_initialize_sell_pda::ProcessInitializeSellPda::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::ProcessInitializeSellPda(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_currency::RegisterCurrency::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::RegisterCurrency(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_fee_exemption::RemoveFeeExemption::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::RemoveFeeExemption(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_atlas_rate::UpdateAtlasRate::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::UpdateAtlasRate(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_currency_royalty::UpdateCurrencyRoyalty::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::UpdateCurrencyRoyalty(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_currency_vault::UpdateCurrencyVault::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::UpdateCurrencyVault(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_royalty_tier::UpdateRoyaltyTier::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: MarketplaceInstruction::UpdateRoyaltyTier(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
