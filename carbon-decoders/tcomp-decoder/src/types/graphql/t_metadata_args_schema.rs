//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::TCollectionGraphQL;
use crate::types::graphql::TTokenProgramVersionGraphQL;
use crate::types::graphql::TTokenStandardGraphQL;
use crate::types::graphql::TUsesGraphQL;
use carbon_core::graphql::primitives::U8;
use juniper::GraphQLObject;
#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "TMetadataArgs")]
pub struct TMetadataArgsGraphQL {
    pub name: String,
    pub symbol: String,
    pub uri: String,
    pub seller_fee_basis_points: i32,
    pub primary_sale_happened: bool,
    pub is_mutable: bool,
    pub edition_nonce: Option<U8>,
    pub token_standard: Option<TTokenStandardGraphQL>,
    pub collection: Option<TCollectionGraphQL>,
    pub uses: Option<TUsesGraphQL>,
    pub token_program_version: TTokenProgramVersionGraphQL,
    pub creator_shares: Vec<U8>,
    pub creator_verified: Vec<bool>,
}

impl From<crate::types::TMetadataArgs> for TMetadataArgsGraphQL {
    fn from(original: crate::types::TMetadataArgs) -> Self {
        Self {
            name: original.name,
            symbol: original.symbol,
            uri: original.uri,
            seller_fee_basis_points: original.seller_fee_basis_points as i32,
            primary_sale_happened: original.primary_sale_happened,
            is_mutable: original.is_mutable,
            edition_nonce: original
                .edition_nonce
                .map(|v| carbon_core::graphql::primitives::U8(v)),
            token_standard: original.token_standard.map(|v| v.into()),
            collection: original.collection.map(|v| v.into()),
            uses: original.uses.map(|v| v.into()),
            token_program_version: original.token_program_version.into(),
            creator_shares: original
                .creator_shares
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U8(item))
                .collect(),
            creator_verified: original
                .creator_verified
                .into_iter()
                .map(|item| item)
                .collect(),
        }
    }
}
