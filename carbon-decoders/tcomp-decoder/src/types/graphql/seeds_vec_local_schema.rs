//! This code was AUTOGENERATED using the Codama library.
use carbon_core::graphql::primitives::U8;
use juniper::GraphQLObject;
#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "SeedsVecLocal")]
pub struct SeedsVecLocalGraphQL {
    pub seeds: Vec<Vec<U8>>,
}

impl From<crate::types::SeedsVecLocal> for SeedsVecLocalGraphQL {
    fn from(original: crate::types::SeedsVecLocal) -> Self {
        Self {
            seeds: original
                .seeds
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(|item| carbon_core::graphql::primitives::U8(item))
                        .collect()
                })
                .collect(),
        }
    }
}
