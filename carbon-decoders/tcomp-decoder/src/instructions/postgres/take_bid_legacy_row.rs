//! This code was AUTOGENERATED using the Codama library.
use crate::types::AuthorizationDataLocal;
use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U16;
use carbon_core::postgres::primitives::U64;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct TakeBidLegacyRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub min_amount: U64,
    pub optional_royalty_pct: Option<U16>,
    pub rules_acc_present: bool,
    pub authorization_data: Option<sqlx::types::Json<AuthorizationDataLocal>>,
}

impl TakeBidLegacyRow {
    pub fn from_parts(
        source: crate::instructions::take_bid_legacy::TakeBidLegacy,
        metadata: InstructionMetadata,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            min_amount: source.min_amount.into(),
            optional_royalty_pct: source.optional_royalty_pct.map(|value| value.into()),
            rules_acc_present: source.rules_acc_present.into(),
            authorization_data: source
                .authorization_data
                .map(|value| sqlx::types::Json(value.into())),
        }
    }
}

impl TryFrom<TakeBidLegacyRow> for crate::instructions::take_bid_legacy::TakeBidLegacy {
    type Error = carbon_core::error::Error;
    fn try_from(source: TakeBidLegacyRow) -> Result<Self, Self::Error> {
        Ok(Self {
            min_amount: *source.min_amount,
            optional_royalty_pct: source
                .optional_royalty_pct
                .map(|value| {
                    value.try_into().map_err(|_| {
                        carbon_core::error::Error::Custom(
                            "Failed to convert value from postgres primitive".to_string(),
                        )
                    })
                })
                .transpose()?,
            rules_acc_present: source.rules_acc_present.into(),
            authorization_data: source.authorization_data.map(|value| value.0),
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::take_bid_legacy::TakeBidLegacy
{
    fn table() -> &'static str {
        "take_bid_legacy_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "min_amount",
            "optional_royalty_pct",
            "rules_acc_present",
            "authorization_data",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for TakeBidLegacyRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO take_bid_legacy_instruction (
                "min_amount",
                "optional_royalty_pct",
                "rules_acc_present",
                "authorization_data",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(self.min_amount.clone())
        .bind(self.optional_royalty_pct.clone())
        .bind(self.rules_acc_present.clone())
        .bind(self.authorization_data.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for TakeBidLegacyRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO take_bid_legacy_instruction (
                "min_amount",
                "optional_royalty_pct",
                "rules_acc_present",
                "authorization_data",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "min_amount" = EXCLUDED."min_amount",
                "optional_royalty_pct" = EXCLUDED."optional_royalty_pct",
                "rules_acc_present" = EXCLUDED."rules_acc_present",
                "authorization_data" = EXCLUDED."authorization_data",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.min_amount.clone())
        .bind(self.optional_royalty_pct.clone())
        .bind(self.rules_acc_present.clone())
        .bind(self.authorization_data.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for TakeBidLegacyRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM take_bid_legacy_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for TakeBidLegacyRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM take_bid_legacy_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct TakeBidLegacyMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for TakeBidLegacyMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS take_bid_legacy_instruction (
                -- Instruction data
                "min_amount" NUMERIC(20) NOT NULL,
                "optional_royalty_pct" INT4,
                "rules_acc_present" BOOLEAN NOT NULL,
                "authorization_data" JSONB,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS take_bid_legacy_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
