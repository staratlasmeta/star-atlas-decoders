//! This code was AUTOGENERATED using the Codama library.
use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U64;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct EditRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub amount: U64,
    pub expire_in_sec: Option<U64>,
    pub currency: Option<Pubkey>,
    pub private_taker: Option<Pubkey>,
    pub maker_broker: Option<Pubkey>,
}

impl EditRow {
    pub fn from_parts(
        source: crate::instructions::edit::Edit,
        metadata: InstructionMetadata,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            amount: source.amount.into(),
            expire_in_sec: source.expire_in_sec.map(|value| value.into()),
            currency: source.currency.map(|value| value.into()),
            private_taker: source.private_taker.map(|value| value.into()),
            maker_broker: source.maker_broker.map(|value| value.into()),
        }
    }
}

impl TryFrom<EditRow> for crate::instructions::edit::Edit {
    type Error = carbon_core::error::Error;
    fn try_from(source: EditRow) -> Result<Self, Self::Error> {
        Ok(Self {
            amount: *source.amount,
            expire_in_sec: source.expire_in_sec.map(|value| *value),
            currency: source.currency.map(|value| *value),
            private_taker: source.private_taker.map(|value| *value),
            maker_broker: source.maker_broker.map(|value| *value),
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::instructions::edit::Edit {
    fn table() -> &'static str {
        "edit_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "amount",
            "expire_in_sec",
            "currency",
            "private_taker",
            "maker_broker",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for EditRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO edit_instruction (
                "amount",
                "expire_in_sec",
                "currency",
                "private_taker",
                "maker_broker",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.amount.clone())
        .bind(self.expire_in_sec.clone())
        .bind(self.currency.clone())
        .bind(self.private_taker.clone())
        .bind(self.maker_broker.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for EditRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO edit_instruction (
                "amount",
                "expire_in_sec",
                "currency",
                "private_taker",
                "maker_broker",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "amount" = EXCLUDED."amount",
                "expire_in_sec" = EXCLUDED."expire_in_sec",
                "currency" = EXCLUDED."currency",
                "private_taker" = EXCLUDED."private_taker",
                "maker_broker" = EXCLUDED."maker_broker",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.amount.clone())
        .bind(self.expire_in_sec.clone())
        .bind(self.currency.clone())
        .bind(self.private_taker.clone())
        .bind(self.maker_broker.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for EditRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM edit_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for EditRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM edit_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct EditMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for EditMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS edit_instruction (
                -- Instruction data
                "amount" NUMERIC(20) NOT NULL,
                "expire_in_sec" NUMERIC(20),
                "currency" BYTEA,
                "private_taker" BYTEA,
                "maker_broker" BYTEA,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS edit_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
