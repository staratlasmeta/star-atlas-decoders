//! This code was AUTOGENERATED using the Codama library.
use crate::types::LootInfo;
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// Represents the loot available at a location

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct Loot {
    /// the data version of this account.
    pub version: u8,
    /// Sector coordinates as [x, y]
    pub sector: [i64; 2],
    /// the `Game` that this belongs to
    pub game_id: Pubkey,
    /// the  player who created this account
    pub creator: Pubkey,
    /// the actual lootable items
    pub items: [LootInfo; 2],
}

impl Loot {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[151, 225, 207, 228, 115, 210, 64, 159] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
