//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U16;
use carbon_core::postgres::primitives::U32;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct CombatConfigRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub game_id: Pubkey,
    pub global_ceasefire: U8,
    pub loot_exclusivity_time: U16,
    pub starbase_upgrade_progress_continuation: U32,
    pub crew_respawn_time: U16,
    pub raw_ships_respawn_time: U16,
}

impl CombatConfigRow {
    pub fn from_parts(
        source: crate::accounts::combat_config::CombatConfig,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            game_id: source.game_id.into(),
            global_ceasefire: source.global_ceasefire.into(),
            loot_exclusivity_time: source.loot_exclusivity_time.into(),
            starbase_upgrade_progress_continuation: source
                .starbase_upgrade_progress_continuation
                .into(),
            crew_respawn_time: source.crew_respawn_time.into(),
            raw_ships_respawn_time: source.raw_ships_respawn_time.into(),
        }
    }
}

impl TryFrom<CombatConfigRow> for crate::accounts::combat_config::CombatConfig {
    type Error = carbon_core::error::Error;
    fn try_from(source: CombatConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            game_id: *source.game_id,
            global_ceasefire: source.global_ceasefire.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            loot_exclusivity_time: source.loot_exclusivity_time.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            starbase_upgrade_progress_continuation: source
                .starbase_upgrade_progress_continuation
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert value from postgres primitive".to_string(),
                    )
                })?,
            crew_respawn_time: source.crew_respawn_time.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            raw_ships_respawn_time: source.raw_ships_respawn_time.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::combat_config::CombatConfig {
    fn table() -> &'static str {
        "combat_config_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "game_id",
            "global_ceasefire",
            "loot_exclusivity_time",
            "starbase_upgrade_progress_continuation",
            "crew_respawn_time",
            "raw_ships_respawn_time",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CombatConfigRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO combat_config_account (
                "version",
                "game_id",
                "global_ceasefire",
                "loot_exclusivity_time",
                "starbase_upgrade_progress_continuation",
                "crew_respawn_time",
                "raw_ships_respawn_time",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.game_id.clone())
        .bind(self.global_ceasefire.clone())
        .bind(self.loot_exclusivity_time.clone())
        .bind(self.starbase_upgrade_progress_continuation.clone())
        .bind(self.crew_respawn_time.clone())
        .bind(self.raw_ships_respawn_time.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CombatConfigRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO combat_config_account (
                "version",
                "game_id",
                "global_ceasefire",
                "loot_exclusivity_time",
                "starbase_upgrade_progress_continuation",
                "crew_respawn_time",
                "raw_ships_respawn_time",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "game_id" = EXCLUDED."game_id",
                "global_ceasefire" = EXCLUDED."global_ceasefire",
                "loot_exclusivity_time" = EXCLUDED."loot_exclusivity_time",
                "starbase_upgrade_progress_continuation" = EXCLUDED."starbase_upgrade_progress_continuation",
                "crew_respawn_time" = EXCLUDED."crew_respawn_time",
                "raw_ships_respawn_time" = EXCLUDED."raw_ships_respawn_time",
                __slot = EXCLUDED.__slot
            "#)
        .bind(self.version.clone())
        .bind(self.game_id.clone())
        .bind(self.global_ceasefire.clone())
        .bind(self.loot_exclusivity_time.clone())
        .bind(self.starbase_upgrade_progress_continuation.clone())
        .bind(self.crew_respawn_time.clone())
        .bind(self.raw_ships_respawn_time.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for CombatConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM combat_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for CombatConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM combat_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct CombatConfigMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for CombatConfigMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS combat_config_account (
                -- Account data
                "version" INT2 NOT NULL,
                "game_id" BYTEA NOT NULL,
                "global_ceasefire" INT2 NOT NULL,
                "loot_exclusivity_time" INT4 NOT NULL,
                "starbase_upgrade_progress_continuation" INT8 NOT NULL,
                "crew_respawn_time" INT4 NOT NULL,
                "raw_ships_respawn_time" INT4 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS combat_config_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
