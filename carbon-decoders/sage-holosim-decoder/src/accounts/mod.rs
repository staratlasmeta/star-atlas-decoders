//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::SageDecoder;

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod combat_config;
pub mod crafting_instance;
pub mod disbanded_fleet;
pub mod fleet;
pub mod fleet_ships;
pub mod game;
pub mod game_state;
pub mod loot;
pub mod mine_item;
pub mod planet;
pub mod player_crew_record;
pub mod progression_config;
pub mod resource;
pub mod sage_crew_config;
pub mod sage_player_profile;
pub mod sector;
pub mod ship;
pub mod star;
pub mod starbase;
pub mod starbase_player;
pub mod survey_data_unit_tracker;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum SageAccount {
    CombatConfig(Box<combat_config::CombatConfig>),
    CraftingInstance(Box<crafting_instance::CraftingInstance>),
    DisbandedFleet(Box<disbanded_fleet::DisbandedFleet>),
    Fleet(Box<fleet::Fleet>),
    FleetShips(Box<fleet_ships::FleetShips>),
    Game(Box<game::Game>),
    GameState(Box<game_state::GameState>),
    Loot(Box<loot::Loot>),
    MineItem(Box<mine_item::MineItem>),
    Planet(Box<planet::Planet>),
    PlayerCrewRecord(Box<player_crew_record::PlayerCrewRecord>),
    ProgressionConfig(Box<progression_config::ProgressionConfig>),
    Resource(Box<resource::Resource>),
    SageCrewConfig(Box<sage_crew_config::SageCrewConfig>),
    SagePlayerProfile(Box<sage_player_profile::SagePlayerProfile>),
    Sector(Box<sector::Sector>),
    Ship(Box<ship::Ship>),
    Star(Box<star::Star>),
    Starbase(Box<starbase::Starbase>),
    StarbasePlayer(Box<starbase_player::StarbasePlayer>),
    SurveyDataUnitTracker(Box<survey_data_unit_tracker::SurveyDataUnitTracker>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for SageDecoder {
    type AccountType = SageAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = combat_config::CombatConfig::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::CombatConfig(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = crafting_instance::CraftingInstance::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::CraftingInstance(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = disbanded_fleet::DisbandedFleet::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::DisbandedFleet(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = fleet::Fleet::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Fleet(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = fleet_ships::FleetShips::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::FleetShips(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = game::Game::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Game(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = game_state::GameState::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::GameState(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = loot::Loot::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Loot(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = mine_item::MineItem::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::MineItem(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = planet::Planet::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Planet(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = player_crew_record::PlayerCrewRecord::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::PlayerCrewRecord(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = progression_config::ProgressionConfig::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::ProgressionConfig(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = resource::Resource::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Resource(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = sage_crew_config::SageCrewConfig::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::SageCrewConfig(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = sage_player_profile::SagePlayerProfile::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::SagePlayerProfile(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = sector::Sector::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Sector(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = ship::Ship::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Ship(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = star::Star::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Star(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = starbase::Starbase::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::Starbase(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = starbase_player::StarbasePlayer::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::StarbasePlayer(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = survey_data_unit_tracker::SurveyDataUnitTracker::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SageAccount::SurveyDataUnitTracker(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
