//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// Progression Config

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CombatConfig {
    /// the data version of this account.
    pub version: u8,
    /// the `Game` that this belongs to
    pub game_id: Pubkey,
    /// global ceasefire; >+1 means true
    pub global_ceasefire: u8,
    /// amount of time when the last-hitter of a fleet will have exclusive access to loot
    pub loot_exclusivity_time: u16,
    /// amount of time that is added to starbase level 0 upgrade process in case it is destroyed
    pub starbase_upgrade_progress_continuation: u32,
    /// Time taken for crew to respawn when a starbase is destroyed
    pub crew_respawn_time: u16,
    /// Time taken for individual ships to respawn when a starbase is destroyed
    pub raw_ships_respawn_time: u16,
}

impl CombatConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[245, 211, 72, 63, 44, 130, 118, 193] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
