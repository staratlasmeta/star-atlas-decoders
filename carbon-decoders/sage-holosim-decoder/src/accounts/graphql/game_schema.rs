//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::CargoGraphQL;
use crate::types::graphql::CraftingGraphQL;
use crate::types::graphql::MintsGraphQL;
use crate::types::graphql::PointsGraphQL;
use crate::types::graphql::RiskZonesDataGraphQL;
use crate::types::graphql::VaultsGraphQL;
use carbon_core::graphql::primitives::Pubkey;
use carbon_core::graphql::primitives::U8;
use carbon_core::graphql::primitives::U64;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Game")]
pub struct GameGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub version: U8,
    pub update_id: U64,
    pub profile: Pubkey,
    pub game_state: Pubkey,
    pub points: PointsGraphQL,
    pub cargo: CargoGraphQL,
    pub crafting: CraftingGraphQL,
    pub mints: MintsGraphQL,
    pub vaults: VaultsGraphQL,
    pub risk_zones: RiskZonesDataGraphQL,
}

impl TryFrom<crate::accounts::postgres::GameRow> for GameGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::GameRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            version: carbon_core::graphql::primitives::U8((*row.version) as u8),
            update_id: carbon_core::graphql::primitives::U64(*row.update_id),
            profile: carbon_core::graphql::primitives::Pubkey(row.profile.0),
            game_state: carbon_core::graphql::primitives::Pubkey(row.game_state.0),
            points: row.points.0.into(),
            cargo: row.cargo.0.into(),
            crafting: row.crafting.0.into(),
            mints: row.mints.0.into(),
            vaults: row.vaults.0.into(),
            risk_zones: row.risk_zones.0.into(),
        })
    }
}
