//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::LootInfoGraphQL;
use carbon_core::graphql::primitives::I64;
use carbon_core::graphql::primitives::Pubkey;
use carbon_core::graphql::primitives::U8;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Loot")]
pub struct LootGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub version: U8,
    pub sector: Vec<I64>,
    pub game_id: Pubkey,
    pub creator: Pubkey,
    pub items: Vec<LootInfoGraphQL>,
}

impl TryFrom<crate::accounts::postgres::LootRow> for LootGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::LootRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            version: carbon_core::graphql::primitives::U8((*row.version) as u8),
            sector: row
                .sector
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::I64(item))
                .collect(),
            game_id: carbon_core::graphql::primitives::Pubkey(row.game_id.0),
            creator: carbon_core::graphql::primitives::Pubkey(row.creator.0),
            items: row.items.0.into_iter().map(|item| item.into()).collect(),
        })
    }
}
