//! This code was AUTOGENERATED using the Codama library.
use carbon_core::graphql::primitives::I64;
use carbon_core::graphql::primitives::Pubkey;
use carbon_core::graphql::primitives::U8;
use carbon_core::graphql::primitives::U32;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Sector")]
pub struct SectorGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub version: U8,
    pub game_id: Pubkey,
    pub coordinates: Vec<I64>,
    pub discoverer: Pubkey,
    pub name: Vec<U8>,
    pub num_stars: i32,
    pub num_planets: i32,
    pub num_moons: i32,
    pub num_asteroid_belts: i32,
    pub last_scan_time: I64,
    pub last_scan_chance: U32,
    pub bump: U8,
    pub num_connections: i32,
}

impl TryFrom<crate::accounts::postgres::SectorRow> for SectorGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::SectorRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            version: carbon_core::graphql::primitives::U8((*row.version) as u8),
            game_id: carbon_core::graphql::primitives::Pubkey(row.game_id.0),
            coordinates: row
                .coordinates
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::I64(item))
                .collect(),
            discoverer: carbon_core::graphql::primitives::Pubkey(row.discoverer.0),
            name: row
                .name
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U8(item))
                .collect(),
            num_stars: (*row.num_stars) as i32,
            num_planets: (*row.num_planets) as i32,
            num_moons: (*row.num_moons) as i32,
            num_asteroid_belts: (*row.num_asteroid_belts) as i32,
            last_scan_time: carbon_core::graphql::primitives::I64(row.last_scan_time),
            last_scan_chance: carbon_core::graphql::primitives::U32((*row.last_scan_chance) as u32),
            bump: carbon_core::graphql::primitives::U8((*row.bump) as u8),
            num_connections: (*row.num_connections) as i32,
        })
    }
}
