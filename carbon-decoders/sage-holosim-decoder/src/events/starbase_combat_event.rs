//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct StarbaseCombatEventEvent {
    pub attacker_fleet: Pubkey,
    pub target_starbase: Pubkey,
    pub sector_key: Pubkey,
    pub combat_id: [u8; 32],
    pub timestamp: i64,
    pub attacker_faction: u8,
    pub starbase_faction: u8,
    pub damage_dealt: u32,
    pub starbase_destroyed: bool,
    pub starbase_captured: bool,
    pub starbase_hp_before: u32,
    pub starbase_hp_after: u32,
}

impl StarbaseCombatEventEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 16 {
            return None;
        }
        let discriminator = &data[0..16];
        if discriminator
            != &[
                228, 69, 165, 46, 81, 203, 154, 29, 83, 116, 97, 114, 98, 97, 115, 101,
            ]
        {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[16..];

        Self::deserialize(data_slice)
    }
}
