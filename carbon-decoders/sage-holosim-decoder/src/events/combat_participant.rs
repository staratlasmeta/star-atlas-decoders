//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CombatParticipantEvent {
    pub fleet_key: Pubkey,

    pub fleet_label: [u8; 64],
    pub fleet_state: u8,
    pub owner_profile: Pubkey,
    pub player_key: Pubkey,
    pub faction: u8,
    pub public_key: Pubkey,
    pub pre_hp: u32,
    pub pre_sp: u32,
    pub pre_ap: u32,
    pub post_hp: u32,
    pub post_sp: u32,
    pub post_ap: u32,
    pub total_attack_power: u32,
    pub total_defense_power: u32,
    pub agility: u32,
    pub ship_class_counts: [u32; 8],
    pub position_x: i64,
    pub position_y: i64,
    pub sector_key: Pubkey,
    pub ammo_count: u64,
    pub fuel_count: u64,
    pub combat_xp: u64,
    pub council_rank: u16,
}

impl CombatParticipantEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 16 {
            return None;
        }
        let discriminator = &data[0..16];
        if discriminator
            != &[
                228, 69, 165, 46, 81, 203, 154, 29, 67, 111, 109, 98, 97, 116, 80, 97,
            ]
        {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[16..];

        Self::deserialize(data_slice)
    }
}
