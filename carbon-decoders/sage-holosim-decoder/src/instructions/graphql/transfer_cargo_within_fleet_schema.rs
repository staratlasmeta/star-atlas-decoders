//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::TransferCargoWithinFleetInputGraphQL;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "TransferCargoWithinFleet")]
pub struct TransferCargoWithinFleetGraphQL {
    pub instruction_metadata: crate::instructions::graphql::InstructionMetadataGraphQL,
    pub input: TransferCargoWithinFleetInputGraphQL,
}

impl TryFrom<crate::instructions::postgres::TransferCargoWithinFleetRow>
    for TransferCargoWithinFleetGraphQL
{
    type Error = carbon_core::error::Error;
    fn try_from(
        row: crate::instructions::postgres::TransferCargoWithinFleetRow,
    ) -> Result<Self, Self::Error> {
        Ok(Self {
            instruction_metadata: row.instruction_metadata.into(),
            input: row.input.0.into(),
        })
    }
}
