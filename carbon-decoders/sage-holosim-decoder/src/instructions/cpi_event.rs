//! This code was AUTOGENERATED using the Codama library.

pub use super::super::events;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(
    Debug,
    Clone,
    carbon_core::borsh::BorshSerialize,
    carbon_core::borsh::BorshDeserialize,
    PartialEq,
)]
pub enum CpiEvent {
    BattleLog(events::battle_log::BattleLogEvent),
    CombatInitiatedEvent(events::combat_initiated_event::CombatInitiatedEventEvent),
    CombatLogEvent(events::combat_log_event::CombatLogEventEvent),
    CombatLootDropEvent(events::combat_loot_drop_event::CombatLootDropEventEvent),
    CombatParticipant(events::combat_participant::CombatParticipantEvent),
    CombatResolvedEvent(events::combat_resolved_event::CombatResolvedEventEvent),
    StarbaseCombatEvent(events::starbase_combat_event::StarbaseCombatEventEvent),
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CpiEventInstructionAccounts {
    pub program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CpiEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[228, 69, 165, 46, 81, 203, 154, 29] {
            return None;
        }

        let event_data = &data[8..];

        if let Some(decoded) = events::battle_log::BattleLogEvent::decode(event_data) {
            return Some(CpiEvent::BattleLog(decoded));
        }
        if let Some(decoded) =
            events::combat_initiated_event::CombatInitiatedEventEvent::decode(event_data)
        {
            return Some(CpiEvent::CombatInitiatedEvent(decoded));
        }
        if let Some(decoded) = events::combat_log_event::CombatLogEventEvent::decode(event_data) {
            return Some(CpiEvent::CombatLogEvent(decoded));
        }
        if let Some(decoded) =
            events::combat_loot_drop_event::CombatLootDropEventEvent::decode(event_data)
        {
            return Some(CpiEvent::CombatLootDropEvent(decoded));
        }
        if let Some(decoded) =
            events::combat_participant::CombatParticipantEvent::decode(event_data)
        {
            return Some(CpiEvent::CombatParticipant(decoded));
        }
        if let Some(decoded) =
            events::combat_resolved_event::CombatResolvedEventEvent::decode(event_data)
        {
            return Some(CpiEvent::CombatResolvedEvent(decoded));
        }
        if let Some(decoded) =
            events::starbase_combat_event::StarbaseCombatEventEvent::decode(event_data)
        {
            return Some(CpiEvent::StarbaseCombatEvent(decoded));
        }
        None
    }
}

impl ArrangeAccounts for CpiEvent {
    type ArrangedAccounts = CpiEventInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let [program, event_authority, remaining @ ..] = accounts else {
            return None;
        };

        Some(CpiEventInstructionAccounts {
            program: program.pubkey,
            event_authority: event_authority.pubkey,
            remaining: remaining.to_vec(),
        })
    }
}
