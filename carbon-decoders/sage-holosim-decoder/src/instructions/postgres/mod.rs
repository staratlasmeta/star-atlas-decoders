//! This code was AUTOGENERATED using the Codama library.
pub mod activate_game_state_row;
pub mod add_connection_row;
pub mod add_crew_to_game_row;
pub mod add_rental_row;
pub mod add_ship_escrow_row;
pub mod add_ship_to_fleet_row;
pub mod attack_fleet_row;
pub mod attack_starbase_row;
pub mod burn_crafting_consumables_row;
pub mod cancel_crafting_process_row;
pub mod change_rental_row;
pub mod claim_crafting_non_consumables_row;
pub mod claim_crafting_outputs_row;
pub mod close_crafting_process_row;
pub mod close_disbanded_fleet_row;
pub mod close_fleet_cargo_pod_token_account_row;
pub mod close_player_crew_record_row;
pub mod close_starbase_cargo_token_account_row;
pub mod close_upgrade_process_row;
pub mod complete_starbase_upgrade_row;
pub mod copy_game_state_row;
pub mod cpi_event_row;
pub mod create_cargo_pod_row;
pub mod create_certificate_mint_row;
pub mod create_crafting_process_row;
pub mod create_fleet_row;
pub mod create_starbase_upgrade_resource_process_row;
pub mod deposit_cargo_to_fleet_row;
pub mod deposit_cargo_to_game_row;
pub mod deposit_crafting_ingredient_row;
pub mod deposit_starbase_upkeep_resource_row;
pub mod deregister_combat_config_row;
pub mod deregister_mine_item_row;
pub mod deregister_progression_config_row;
pub mod deregister_resource_row;
pub mod deregister_starbase_row;
pub mod deregister_survey_data_unit_tracker_row;
pub mod dev_add_crew_to_game_row;
pub mod dev_deposit_cargo_to_game_row;
pub mod disband_fleet_row;
pub mod disbanded_fleet_to_escrow_row;
pub mod discover_sector_row;
pub mod drain_mine_item_bank_row;
pub mod drain_survey_data_units_bank_row;
pub mod fleet_state_handler_row;
pub mod force_disband_fleet_row;
pub mod force_drop_fleet_cargo_row;
pub mod idle_to_loading_bay_row;
pub mod idle_to_respawn_row;
pub mod init_game_row;
pub mod init_game_state_row;
pub mod invalidate_rental_row;
pub mod invalidate_ship_row;
pub mod load_fleet_crew_row;
pub mod loading_bay_to_idle_row;
pub mod mine_asteroid_to_respawn_row;
pub mod mint_certificate_row;
pub mod mint_crew_to_game_row;
pub mod redeem_certificate_row;
pub mod register_combat_config_row;
pub mod register_mine_item_row;
pub mod register_planet_row;
pub mod register_progression_config_row;
pub mod register_resource_row;
pub mod register_sage_crew_config_row;
pub mod register_sage_player_profile_row;
pub mod register_sage_point_modifier_row;
pub mod register_sector_row;
pub mod register_ship_row;
pub mod register_star_row;
pub mod register_starbase_player_row;
pub mod register_starbase_row;
pub mod register_survey_data_unit_tracker_row;
pub mod reload_fleet_ability_power_row;
pub mod remove_cargo_pod_row;
pub mod remove_connection_row;
pub mod remove_crew_from_game_row;
pub mod remove_invalid_ship_escrow_row;
pub mod remove_ship_escrow_row;
pub mod repair_docked_fleet_row;
pub mod repair_idle_fleet_row;
pub mod repair_starbase_row;
pub mod respawn_to_loading_bay_row;
pub mod retrieve_loot_row;
pub mod scan_for_survey_data_units_row;
pub mod set_next_ship_row;
pub mod start_crafting_process_row;
pub mod start_mining_asteroid_row;
pub mod start_starbase_upgrade_row;
pub mod start_subwarp_row;
pub mod stop_crafting_process_row;
pub mod stop_mining_asteroid_row;
pub mod stop_subwarp_row;
pub mod submit_starbase_upgrade_resource_row;
pub mod sync_starbase_player_row;
pub mod sync_starbase_upgrade_ingredients_row;
pub mod transfer_cargo_at_starbase_row;
pub mod transfer_cargo_within_fleet_row;
pub mod unload_fleet_crew_row;
pub mod update_combat_config_row;
pub mod update_game_row;
pub mod update_game_state_row;
pub mod update_mine_item_row;
pub mod update_planet_row;
pub mod update_progression_config_row;
pub mod update_resource_row;
pub mod update_ship_escrow_row;
pub mod update_ship_in_fleet_row;
pub mod update_ship_row;
pub mod update_star_row;
pub mod update_starbase_row;
pub mod update_survey_data_unit_tracker_row;
pub mod warp_lane_row;
pub mod warp_to_coordinate_row;
pub mod withdraw_cargo_from_fleet_row;
pub mod withdraw_cargo_from_game_row;
pub mod withdraw_crafting_ingredient_row;

pub use self::activate_game_state_row::*;
pub use self::add_connection_row::*;
pub use self::add_crew_to_game_row::*;
pub use self::add_rental_row::*;
pub use self::add_ship_escrow_row::*;
pub use self::add_ship_to_fleet_row::*;
pub use self::attack_fleet_row::*;
pub use self::attack_starbase_row::*;
pub use self::burn_crafting_consumables_row::*;
pub use self::cancel_crafting_process_row::*;
pub use self::change_rental_row::*;
pub use self::claim_crafting_non_consumables_row::*;
pub use self::claim_crafting_outputs_row::*;
pub use self::close_crafting_process_row::*;
pub use self::close_disbanded_fleet_row::*;
pub use self::close_fleet_cargo_pod_token_account_row::*;
pub use self::close_player_crew_record_row::*;
pub use self::close_starbase_cargo_token_account_row::*;
pub use self::close_upgrade_process_row::*;
pub use self::complete_starbase_upgrade_row::*;
pub use self::copy_game_state_row::*;
pub use self::cpi_event_row::*;
pub use self::create_cargo_pod_row::*;
pub use self::create_certificate_mint_row::*;
pub use self::create_crafting_process_row::*;
pub use self::create_fleet_row::*;
pub use self::create_starbase_upgrade_resource_process_row::*;
pub use self::deposit_cargo_to_fleet_row::*;
pub use self::deposit_cargo_to_game_row::*;
pub use self::deposit_crafting_ingredient_row::*;
pub use self::deposit_starbase_upkeep_resource_row::*;
pub use self::deregister_combat_config_row::*;
pub use self::deregister_mine_item_row::*;
pub use self::deregister_progression_config_row::*;
pub use self::deregister_resource_row::*;
pub use self::deregister_starbase_row::*;
pub use self::deregister_survey_data_unit_tracker_row::*;
pub use self::dev_add_crew_to_game_row::*;
pub use self::dev_deposit_cargo_to_game_row::*;
pub use self::disband_fleet_row::*;
pub use self::disbanded_fleet_to_escrow_row::*;
pub use self::discover_sector_row::*;
pub use self::drain_mine_item_bank_row::*;
pub use self::drain_survey_data_units_bank_row::*;
pub use self::fleet_state_handler_row::*;
pub use self::force_disband_fleet_row::*;
pub use self::force_drop_fleet_cargo_row::*;
pub use self::idle_to_loading_bay_row::*;
pub use self::idle_to_respawn_row::*;
pub use self::init_game_row::*;
pub use self::init_game_state_row::*;
pub use self::invalidate_rental_row::*;
pub use self::invalidate_ship_row::*;
pub use self::load_fleet_crew_row::*;
pub use self::loading_bay_to_idle_row::*;
pub use self::mine_asteroid_to_respawn_row::*;
pub use self::mint_certificate_row::*;
pub use self::mint_crew_to_game_row::*;
pub use self::redeem_certificate_row::*;
pub use self::register_combat_config_row::*;
pub use self::register_mine_item_row::*;
pub use self::register_planet_row::*;
pub use self::register_progression_config_row::*;
pub use self::register_resource_row::*;
pub use self::register_sage_crew_config_row::*;
pub use self::register_sage_player_profile_row::*;
pub use self::register_sage_point_modifier_row::*;
pub use self::register_sector_row::*;
pub use self::register_ship_row::*;
pub use self::register_star_row::*;
pub use self::register_starbase_player_row::*;
pub use self::register_starbase_row::*;
pub use self::register_survey_data_unit_tracker_row::*;
pub use self::reload_fleet_ability_power_row::*;
pub use self::remove_cargo_pod_row::*;
pub use self::remove_connection_row::*;
pub use self::remove_crew_from_game_row::*;
pub use self::remove_invalid_ship_escrow_row::*;
pub use self::remove_ship_escrow_row::*;
pub use self::repair_docked_fleet_row::*;
pub use self::repair_idle_fleet_row::*;
pub use self::repair_starbase_row::*;
pub use self::respawn_to_loading_bay_row::*;
pub use self::retrieve_loot_row::*;
pub use self::scan_for_survey_data_units_row::*;
pub use self::set_next_ship_row::*;
pub use self::start_crafting_process_row::*;
pub use self::start_mining_asteroid_row::*;
pub use self::start_starbase_upgrade_row::*;
pub use self::start_subwarp_row::*;
pub use self::stop_crafting_process_row::*;
pub use self::stop_mining_asteroid_row::*;
pub use self::stop_subwarp_row::*;
pub use self::submit_starbase_upgrade_resource_row::*;
pub use self::sync_starbase_player_row::*;
pub use self::sync_starbase_upgrade_ingredients_row::*;
pub use self::transfer_cargo_at_starbase_row::*;
pub use self::transfer_cargo_within_fleet_row::*;
pub use self::unload_fleet_crew_row::*;
pub use self::update_combat_config_row::*;
pub use self::update_game_row::*;
pub use self::update_game_state_row::*;
pub use self::update_mine_item_row::*;
pub use self::update_planet_row::*;
pub use self::update_progression_config_row::*;
pub use self::update_resource_row::*;
pub use self::update_ship_escrow_row::*;
pub use self::update_ship_in_fleet_row::*;
pub use self::update_ship_row::*;
pub use self::update_star_row::*;
pub use self::update_starbase_row::*;
pub use self::update_survey_data_unit_tracker_row::*;
pub use self::warp_lane_row::*;
pub use self::warp_to_coordinate_row::*;
pub use self::withdraw_cargo_from_fleet_row::*;
pub use self::withdraw_cargo_from_game_row::*;
pub use self::withdraw_crafting_ingredient_row::*;

use super::SageInstruction;

pub struct SageInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for SageInstructionsMigration {
    fn app(&self) -> &str {
        "sage"
    }

    fn name(&self) -> &str {
        "sage_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(ActivateGameStateMigrationOperation),
            Box::new(AddConnectionMigrationOperation),
            Box::new(AddCrewToGameMigrationOperation),
            Box::new(AddRentalMigrationOperation),
            Box::new(AddShipEscrowMigrationOperation),
            Box::new(AddShipToFleetMigrationOperation),
            Box::new(AttackFleetMigrationOperation),
            Box::new(AttackStarbaseMigrationOperation),
            Box::new(BurnCraftingConsumablesMigrationOperation),
            Box::new(CancelCraftingProcessMigrationOperation),
            Box::new(ChangeRentalMigrationOperation),
            Box::new(ClaimCraftingNonConsumablesMigrationOperation),
            Box::new(ClaimCraftingOutputsMigrationOperation),
            Box::new(CloseCraftingProcessMigrationOperation),
            Box::new(CloseDisbandedFleetMigrationOperation),
            Box::new(CloseFleetCargoPodTokenAccountMigrationOperation),
            Box::new(ClosePlayerCrewRecordMigrationOperation),
            Box::new(CloseStarbaseCargoTokenAccountMigrationOperation),
            Box::new(CloseUpgradeProcessMigrationOperation),
            Box::new(CompleteStarbaseUpgradeMigrationOperation),
            Box::new(CopyGameStateMigrationOperation),
            Box::new(CreateCargoPodMigrationOperation),
            Box::new(CreateCertificateMintMigrationOperation),
            Box::new(CreateCraftingProcessMigrationOperation),
            Box::new(CreateFleetMigrationOperation),
            Box::new(CreateStarbaseUpgradeResourceProcessMigrationOperation),
            Box::new(DepositCargoToFleetMigrationOperation),
            Box::new(DepositCargoToGameMigrationOperation),
            Box::new(DepositCraftingIngredientMigrationOperation),
            Box::new(DepositStarbaseUpkeepResourceMigrationOperation),
            Box::new(DeregisterCombatConfigMigrationOperation),
            Box::new(DeregisterMineItemMigrationOperation),
            Box::new(DeregisterProgressionConfigMigrationOperation),
            Box::new(DeregisterResourceMigrationOperation),
            Box::new(DeregisterStarbaseMigrationOperation),
            Box::new(DeregisterSurveyDataUnitTrackerMigrationOperation),
            Box::new(DevAddCrewToGameMigrationOperation),
            Box::new(DevDepositCargoToGameMigrationOperation),
            Box::new(DisbandedFleetToEscrowMigrationOperation),
            Box::new(DisbandFleetMigrationOperation),
            Box::new(DiscoverSectorMigrationOperation),
            Box::new(DrainMineItemBankMigrationOperation),
            Box::new(DrainSurveyDataUnitsBankMigrationOperation),
            Box::new(FleetStateHandlerMigrationOperation),
            Box::new(ForceDisbandFleetMigrationOperation),
            Box::new(ForceDropFleetCargoMigrationOperation),
            Box::new(IdleToLoadingBayMigrationOperation),
            Box::new(IdleToRespawnMigrationOperation),
            Box::new(InitGameMigrationOperation),
            Box::new(InitGameStateMigrationOperation),
            Box::new(InvalidateRentalMigrationOperation),
            Box::new(InvalidateShipMigrationOperation),
            Box::new(LoadFleetCrewMigrationOperation),
            Box::new(LoadingBayToIdleMigrationOperation),
            Box::new(MineAsteroidToRespawnMigrationOperation),
            Box::new(MintCertificateMigrationOperation),
            Box::new(MintCrewToGameMigrationOperation),
            Box::new(RedeemCertificateMigrationOperation),
            Box::new(RegisterCombatConfigMigrationOperation),
            Box::new(RegisterMineItemMigrationOperation),
            Box::new(RegisterPlanetMigrationOperation),
            Box::new(RegisterProgressionConfigMigrationOperation),
            Box::new(RegisterResourceMigrationOperation),
            Box::new(RegisterSageCrewConfigMigrationOperation),
            Box::new(RegisterSagePlayerProfileMigrationOperation),
            Box::new(RegisterSagePointModifierMigrationOperation),
            Box::new(RegisterSectorMigrationOperation),
            Box::new(RegisterShipMigrationOperation),
            Box::new(RegisterStarMigrationOperation),
            Box::new(RegisterStarbaseMigrationOperation),
            Box::new(RegisterStarbasePlayerMigrationOperation),
            Box::new(RegisterSurveyDataUnitTrackerMigrationOperation),
            Box::new(ReloadFleetAbilityPowerMigrationOperation),
            Box::new(RemoveCargoPodMigrationOperation),
            Box::new(RemoveConnectionMigrationOperation),
            Box::new(RemoveCrewFromGameMigrationOperation),
            Box::new(RemoveInvalidShipEscrowMigrationOperation),
            Box::new(RemoveShipEscrowMigrationOperation),
            Box::new(RepairDockedFleetMigrationOperation),
            Box::new(RepairIdleFleetMigrationOperation),
            Box::new(RepairStarbaseMigrationOperation),
            Box::new(RespawnToLoadingBayMigrationOperation),
            Box::new(RetrieveLootMigrationOperation),
            Box::new(ScanForSurveyDataUnitsMigrationOperation),
            Box::new(SetNextShipMigrationOperation),
            Box::new(StartCraftingProcessMigrationOperation),
            Box::new(StartMiningAsteroidMigrationOperation),
            Box::new(StartStarbaseUpgradeMigrationOperation),
            Box::new(StartSubwarpMigrationOperation),
            Box::new(StopCraftingProcessMigrationOperation),
            Box::new(StopMiningAsteroidMigrationOperation),
            Box::new(StopSubwarpMigrationOperation),
            Box::new(SubmitStarbaseUpgradeResourceMigrationOperation),
            Box::new(SyncStarbasePlayerMigrationOperation),
            Box::new(SyncStarbaseUpgradeIngredientsMigrationOperation),
            Box::new(TransferCargoAtStarbaseMigrationOperation),
            Box::new(TransferCargoWithinFleetMigrationOperation),
            Box::new(UnloadFleetCrewMigrationOperation),
            Box::new(UpdateCombatConfigMigrationOperation),
            Box::new(UpdateGameMigrationOperation),
            Box::new(UpdateGameStateMigrationOperation),
            Box::new(UpdateMineItemMigrationOperation),
            Box::new(UpdatePlanetMigrationOperation),
            Box::new(UpdateProgressionConfigMigrationOperation),
            Box::new(UpdateResourceMigrationOperation),
            Box::new(UpdateShipMigrationOperation),
            Box::new(UpdateShipEscrowMigrationOperation),
            Box::new(UpdateShipInFleetMigrationOperation),
            Box::new(UpdateStarMigrationOperation),
            Box::new(UpdateStarbaseMigrationOperation),
            Box::new(UpdateSurveyDataUnitTrackerMigrationOperation),
            Box::new(WarpLaneMigrationOperation),
            Box::new(WarpToCoordinateMigrationOperation),
            Box::new(WithdrawCargoFromFleetMigrationOperation),
            Box::new(WithdrawCargoFromGameMigrationOperation),
            Box::new(WithdrawCraftingIngredientMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct SageInstructionWithMetadata(
    pub SageInstruction,
    pub carbon_core::instruction::InstructionMetadata,
);

impl
    From<(
        SageInstruction,
        carbon_core::instruction::InstructionMetadata,
    )> for SageInstructionWithMetadata
{
    fn from(
        value: (
            SageInstruction,
            carbon_core::instruction::InstructionMetadata,
        ),
    ) -> Self {
        SageInstructionWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for SageInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let SageInstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            SageInstruction::ActivateGameState(instruction) => {
                let row = activate_game_state_row::ActivateGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::AddConnection(instruction) => {
                let row = add_connection_row::AddConnectionRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::AddCrewToGame(instruction) => {
                let row = add_crew_to_game_row::AddCrewToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::AddRental(instruction) => {
                let row =
                    add_rental_row::AddRentalRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::AddShipEscrow(instruction) => {
                let row = add_ship_escrow_row::AddShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::AddShipToFleet(instruction) => {
                let row = add_ship_to_fleet_row::AddShipToFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::AttackFleet(instruction) => {
                let row = attack_fleet_row::AttackFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::AttackStarbase(instruction) => {
                let row = attack_starbase_row::AttackStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::BurnCraftingConsumables(instruction) => {
                let row = burn_crafting_consumables_row::BurnCraftingConsumablesRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CancelCraftingProcess(instruction) => {
                let row = cancel_crafting_process_row::CancelCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ChangeRental(instruction) => {
                let row = change_rental_row::ChangeRentalRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ClaimCraftingNonConsumables(instruction) => {
                let row =
                    claim_crafting_non_consumables_row::ClaimCraftingNonConsumablesRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ClaimCraftingOutputs(instruction) => {
                let row = claim_crafting_outputs_row::ClaimCraftingOutputsRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseCraftingProcess(instruction) => {
                let row = close_crafting_process_row::CloseCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseDisbandedFleet(instruction) => {
                let row = close_disbanded_fleet_row::CloseDisbandedFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseFleetCargoPodTokenAccount(instruction) => {
                let row = close_fleet_cargo_pod_token_account_row::CloseFleetCargoPodTokenAccountRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ClosePlayerCrewRecord(instruction) => {
                let row = close_player_crew_record_row::ClosePlayerCrewRecordRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseStarbaseCargoTokenAccount(instruction) => {
                let row = close_starbase_cargo_token_account_row::CloseStarbaseCargoTokenAccountRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseUpgradeProcess(instruction) => {
                let row = close_upgrade_process_row::CloseUpgradeProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CompleteStarbaseUpgrade(instruction) => {
                let row = complete_starbase_upgrade_row::CompleteStarbaseUpgradeRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CopyGameState(instruction) => {
                let row = copy_game_state_row::CopyGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateCargoPod(instruction) => {
                let row = create_cargo_pod_row::CreateCargoPodRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateCertificateMint(instruction) => {
                let row = create_certificate_mint_row::CreateCertificateMintRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateCraftingProcess(instruction) => {
                let row = create_crafting_process_row::CreateCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateFleet(instruction) => {
                let row = create_fleet_row::CreateFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateStarbaseUpgradeResourceProcess(instruction) => {
                let row = create_starbase_upgrade_resource_process_row::CreateStarbaseUpgradeResourceProcessRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositCargoToFleet(instruction) => {
                let row = deposit_cargo_to_fleet_row::DepositCargoToFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositCargoToGame(instruction) => {
                let row = deposit_cargo_to_game_row::DepositCargoToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositCraftingIngredient(instruction) => {
                let row = deposit_crafting_ingredient_row::DepositCraftingIngredientRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositStarbaseUpkeepResource(instruction) => {
                let row = deposit_starbase_upkeep_resource_row::DepositStarbaseUpkeepResourceRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterCombatConfig(instruction) => {
                let row = deregister_combat_config_row::DeregisterCombatConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterMineItem(instruction) => {
                let row = deregister_mine_item_row::DeregisterMineItemRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterProgressionConfig(instruction) => {
                let row =
                    deregister_progression_config_row::DeregisterProgressionConfigRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterResource(instruction) => {
                let row = deregister_resource_row::DeregisterResourceRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterStarbase(instruction) => {
                let row = deregister_starbase_row::DeregisterStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterSurveyDataUnitTracker(instruction) => {
                let row = deregister_survey_data_unit_tracker_row::DeregisterSurveyDataUnitTrackerRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DevAddCrewToGame(instruction) => {
                let row = dev_add_crew_to_game_row::DevAddCrewToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DevDepositCargoToGame(instruction) => {
                let row = dev_deposit_cargo_to_game_row::DevDepositCargoToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DisbandFleet(instruction) => {
                let row = disband_fleet_row::DisbandFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DisbandedFleetToEscrow(instruction) => {
                let row = disbanded_fleet_to_escrow_row::DisbandedFleetToEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DiscoverSector(instruction) => {
                let row = discover_sector_row::DiscoverSectorRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DrainMineItemBank(instruction) => {
                let row = drain_mine_item_bank_row::DrainMineItemBankRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::DrainSurveyDataUnitsBank(instruction) => {
                let row = drain_survey_data_units_bank_row::DrainSurveyDataUnitsBankRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::FleetStateHandler(instruction) => {
                let row = fleet_state_handler_row::FleetStateHandlerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ForceDisbandFleet(instruction) => {
                let row = force_disband_fleet_row::ForceDisbandFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ForceDropFleetCargo(instruction) => {
                let row = force_drop_fleet_cargo_row::ForceDropFleetCargoRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::IdleToLoadingBay(instruction) => {
                let row = idle_to_loading_bay_row::IdleToLoadingBayRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::IdleToRespawn(instruction) => {
                let row = idle_to_respawn_row::IdleToRespawnRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::InitGame(instruction) => {
                let row =
                    init_game_row::InitGameRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::InitGameState(instruction) => {
                let row = init_game_state_row::InitGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::InvalidateRental(instruction) => {
                let row = invalidate_rental_row::InvalidateRentalRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::InvalidateShip(instruction) => {
                let row = invalidate_ship_row::InvalidateShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::LoadFleetCrew(instruction) => {
                let row = load_fleet_crew_row::LoadFleetCrewRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::LoadingBayToIdle(instruction) => {
                let row = loading_bay_to_idle_row::LoadingBayToIdleRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::MineAsteroidToRespawn(instruction) => {
                let row = mine_asteroid_to_respawn_row::MineAsteroidToRespawnRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::MintCertificate(instruction) => {
                let row = mint_certificate_row::MintCertificateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::MintCrewToGame(instruction) => {
                let row = mint_crew_to_game_row::MintCrewToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RedeemCertificate(instruction) => {
                let row = redeem_certificate_row::RedeemCertificateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterCombatConfig(instruction) => {
                let row = register_combat_config_row::RegisterCombatConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterMineItem(instruction) => {
                let row = register_mine_item_row::RegisterMineItemRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterPlanet(instruction) => {
                let row = register_planet_row::RegisterPlanetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterProgressionConfig(instruction) => {
                let row = register_progression_config_row::RegisterProgressionConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterResource(instruction) => {
                let row = register_resource_row::RegisterResourceRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSageCrewConfig(instruction) => {
                let row = register_sage_crew_config_row::RegisterSageCrewConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSagePlayerProfile(instruction) => {
                let row =
                    register_sage_player_profile_row::RegisterSagePlayerProfileRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSagePointModifier(instruction) => {
                let row =
                    register_sage_point_modifier_row::RegisterSagePointModifierRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSector(instruction) => {
                let row = register_sector_row::RegisterSectorRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterShip(instruction) => {
                let row = register_ship_row::RegisterShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterStar(instruction) => {
                let row = register_star_row::RegisterStarRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterStarbase(instruction) => {
                let row = register_starbase_row::RegisterStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterStarbasePlayer(instruction) => {
                let row = register_starbase_player_row::RegisterStarbasePlayerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSurveyDataUnitTracker(instruction) => {
                let row = register_survey_data_unit_tracker_row::RegisterSurveyDataUnitTrackerRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ReloadFleetAbilityPower(instruction) => {
                let row = reload_fleet_ability_power_row::ReloadFleetAbilityPowerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveCargoPod(instruction) => {
                let row = remove_cargo_pod_row::RemoveCargoPodRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveConnection(instruction) => {
                let row = remove_connection_row::RemoveConnectionRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveCrewFromGame(instruction) => {
                let row = remove_crew_from_game_row::RemoveCrewFromGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveInvalidShipEscrow(instruction) => {
                let row = remove_invalid_ship_escrow_row::RemoveInvalidShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveShipEscrow(instruction) => {
                let row = remove_ship_escrow_row::RemoveShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RepairDockedFleet(instruction) => {
                let row = repair_docked_fleet_row::RepairDockedFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RepairIdleFleet(instruction) => {
                let row = repair_idle_fleet_row::RepairIdleFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RepairStarbase(instruction) => {
                let row = repair_starbase_row::RepairStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RespawnToLoadingBay(instruction) => {
                let row = respawn_to_loading_bay_row::RespawnToLoadingBayRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::RetrieveLoot(instruction) => {
                let row = retrieve_loot_row::RetrieveLootRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::ScanForSurveyDataUnits(instruction) => {
                let row = scan_for_survey_data_units_row::ScanForSurveyDataUnitsRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::SetNextShip(instruction) => {
                let row = set_next_ship_row::SetNextShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::StartCraftingProcess(instruction) => {
                let row = start_crafting_process_row::StartCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::StartMiningAsteroid(instruction) => {
                let row = start_mining_asteroid_row::StartMiningAsteroidRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::StartStarbaseUpgrade(instruction) => {
                let row = start_starbase_upgrade_row::StartStarbaseUpgradeRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::StartSubwarp(instruction) => {
                let row = start_subwarp_row::StartSubwarpRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::StopCraftingProcess(instruction) => {
                let row = stop_crafting_process_row::StopCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::StopMiningAsteroid(instruction) => {
                let row = stop_mining_asteroid_row::StopMiningAsteroidRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::StopSubwarp(instruction) => {
                let row = stop_subwarp_row::StopSubwarpRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::SubmitStarbaseUpgradeResource(instruction) => {
                let row = submit_starbase_upgrade_resource_row::SubmitStarbaseUpgradeResourceRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::SyncStarbasePlayer(instruction) => {
                let row = sync_starbase_player_row::SyncStarbasePlayerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::SyncStarbaseUpgradeIngredients(instruction) => {
                let row = sync_starbase_upgrade_ingredients_row::SyncStarbaseUpgradeIngredientsRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::TransferCargoAtStarbase(instruction) => {
                let row = transfer_cargo_at_starbase_row::TransferCargoAtStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::TransferCargoWithinFleet(instruction) => {
                let row = transfer_cargo_within_fleet_row::TransferCargoWithinFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UnloadFleetCrew(instruction) => {
                let row = unload_fleet_crew_row::UnloadFleetCrewRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateCombatConfig(instruction) => {
                let row = update_combat_config_row::UpdateCombatConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateGame(instruction) => {
                let row = update_game_row::UpdateGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateGameState(instruction) => {
                let row = update_game_state_row::UpdateGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateMineItem(instruction) => {
                let row = update_mine_item_row::UpdateMineItemRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdatePlanet(instruction) => {
                let row = update_planet_row::UpdatePlanetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateProgressionConfig(instruction) => {
                let row = update_progression_config_row::UpdateProgressionConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateResource(instruction) => {
                let row = update_resource_row::UpdateResourceRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateShip(instruction) => {
                let row = update_ship_row::UpdateShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateShipEscrow(instruction) => {
                let row = update_ship_escrow_row::UpdateShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateShipInFleet(instruction) => {
                let row = update_ship_in_fleet_row::UpdateShipInFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateStar(instruction) => {
                let row = update_star_row::UpdateStarRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateStarbase(instruction) => {
                let row = update_starbase_row::UpdateStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateSurveyDataUnitTracker(instruction) => {
                let row =
                    update_survey_data_unit_tracker_row::UpdateSurveyDataUnitTrackerRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::WarpLane(instruction) => {
                let row =
                    warp_lane_row::WarpLaneRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::WarpToCoordinate(instruction) => {
                let row = warp_to_coordinate_row::WarpToCoordinateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::WithdrawCargoFromFleet(instruction) => {
                let row = withdraw_cargo_from_fleet_row::WithdrawCargoFromFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::WithdrawCargoFromGame(instruction) => {
                let row = withdraw_cargo_from_game_row::WithdrawCargoFromGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::WithdrawCraftingIngredient(instruction) => {
                let row =
                    withdraw_crafting_ingredient_row::WithdrawCraftingIngredientRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            SageInstruction::CpiEvent(instruction) => {
                let row =
                    cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for SageInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let SageInstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            SageInstruction::ActivateGameState(instruction) => {
                let row = activate_game_state_row::ActivateGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::AddConnection(instruction) => {
                let row = add_connection_row::AddConnectionRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::AddCrewToGame(instruction) => {
                let row = add_crew_to_game_row::AddCrewToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::AddRental(instruction) => {
                let row =
                    add_rental_row::AddRentalRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::AddShipEscrow(instruction) => {
                let row = add_ship_escrow_row::AddShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::AddShipToFleet(instruction) => {
                let row = add_ship_to_fleet_row::AddShipToFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::AttackFleet(instruction) => {
                let row = attack_fleet_row::AttackFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::AttackStarbase(instruction) => {
                let row = attack_starbase_row::AttackStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::BurnCraftingConsumables(instruction) => {
                let row = burn_crafting_consumables_row::BurnCraftingConsumablesRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CancelCraftingProcess(instruction) => {
                let row = cancel_crafting_process_row::CancelCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ChangeRental(instruction) => {
                let row = change_rental_row::ChangeRentalRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ClaimCraftingNonConsumables(instruction) => {
                let row =
                    claim_crafting_non_consumables_row::ClaimCraftingNonConsumablesRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ClaimCraftingOutputs(instruction) => {
                let row = claim_crafting_outputs_row::ClaimCraftingOutputsRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseCraftingProcess(instruction) => {
                let row = close_crafting_process_row::CloseCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseDisbandedFleet(instruction) => {
                let row = close_disbanded_fleet_row::CloseDisbandedFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseFleetCargoPodTokenAccount(instruction) => {
                let row = close_fleet_cargo_pod_token_account_row::CloseFleetCargoPodTokenAccountRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ClosePlayerCrewRecord(instruction) => {
                let row = close_player_crew_record_row::ClosePlayerCrewRecordRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseStarbaseCargoTokenAccount(instruction) => {
                let row = close_starbase_cargo_token_account_row::CloseStarbaseCargoTokenAccountRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CloseUpgradeProcess(instruction) => {
                let row = close_upgrade_process_row::CloseUpgradeProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CompleteStarbaseUpgrade(instruction) => {
                let row = complete_starbase_upgrade_row::CompleteStarbaseUpgradeRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CopyGameState(instruction) => {
                let row = copy_game_state_row::CopyGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateCargoPod(instruction) => {
                let row = create_cargo_pod_row::CreateCargoPodRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateCertificateMint(instruction) => {
                let row = create_certificate_mint_row::CreateCertificateMintRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateCraftingProcess(instruction) => {
                let row = create_crafting_process_row::CreateCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateFleet(instruction) => {
                let row = create_fleet_row::CreateFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CreateStarbaseUpgradeResourceProcess(instruction) => {
                let row = create_starbase_upgrade_resource_process_row::CreateStarbaseUpgradeResourceProcessRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositCargoToFleet(instruction) => {
                let row = deposit_cargo_to_fleet_row::DepositCargoToFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositCargoToGame(instruction) => {
                let row = deposit_cargo_to_game_row::DepositCargoToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositCraftingIngredient(instruction) => {
                let row = deposit_crafting_ingredient_row::DepositCraftingIngredientRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DepositStarbaseUpkeepResource(instruction) => {
                let row = deposit_starbase_upkeep_resource_row::DepositStarbaseUpkeepResourceRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterCombatConfig(instruction) => {
                let row = deregister_combat_config_row::DeregisterCombatConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterMineItem(instruction) => {
                let row = deregister_mine_item_row::DeregisterMineItemRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterProgressionConfig(instruction) => {
                let row =
                    deregister_progression_config_row::DeregisterProgressionConfigRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterResource(instruction) => {
                let row = deregister_resource_row::DeregisterResourceRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterStarbase(instruction) => {
                let row = deregister_starbase_row::DeregisterStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DeregisterSurveyDataUnitTracker(instruction) => {
                let row = deregister_survey_data_unit_tracker_row::DeregisterSurveyDataUnitTrackerRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DevAddCrewToGame(instruction) => {
                let row = dev_add_crew_to_game_row::DevAddCrewToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DevDepositCargoToGame(instruction) => {
                let row = dev_deposit_cargo_to_game_row::DevDepositCargoToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DisbandFleet(instruction) => {
                let row = disband_fleet_row::DisbandFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DisbandedFleetToEscrow(instruction) => {
                let row = disbanded_fleet_to_escrow_row::DisbandedFleetToEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DiscoverSector(instruction) => {
                let row = discover_sector_row::DiscoverSectorRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DrainMineItemBank(instruction) => {
                let row = drain_mine_item_bank_row::DrainMineItemBankRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::DrainSurveyDataUnitsBank(instruction) => {
                let row = drain_survey_data_units_bank_row::DrainSurveyDataUnitsBankRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::FleetStateHandler(instruction) => {
                let row = fleet_state_handler_row::FleetStateHandlerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ForceDisbandFleet(instruction) => {
                let row = force_disband_fleet_row::ForceDisbandFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ForceDropFleetCargo(instruction) => {
                let row = force_drop_fleet_cargo_row::ForceDropFleetCargoRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::IdleToLoadingBay(instruction) => {
                let row = idle_to_loading_bay_row::IdleToLoadingBayRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::IdleToRespawn(instruction) => {
                let row = idle_to_respawn_row::IdleToRespawnRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::InitGame(instruction) => {
                let row =
                    init_game_row::InitGameRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::InitGameState(instruction) => {
                let row = init_game_state_row::InitGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::InvalidateRental(instruction) => {
                let row = invalidate_rental_row::InvalidateRentalRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::InvalidateShip(instruction) => {
                let row = invalidate_ship_row::InvalidateShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::LoadFleetCrew(instruction) => {
                let row = load_fleet_crew_row::LoadFleetCrewRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::LoadingBayToIdle(instruction) => {
                let row = loading_bay_to_idle_row::LoadingBayToIdleRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::MineAsteroidToRespawn(instruction) => {
                let row = mine_asteroid_to_respawn_row::MineAsteroidToRespawnRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::MintCertificate(instruction) => {
                let row = mint_certificate_row::MintCertificateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::MintCrewToGame(instruction) => {
                let row = mint_crew_to_game_row::MintCrewToGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RedeemCertificate(instruction) => {
                let row = redeem_certificate_row::RedeemCertificateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterCombatConfig(instruction) => {
                let row = register_combat_config_row::RegisterCombatConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterMineItem(instruction) => {
                let row = register_mine_item_row::RegisterMineItemRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterPlanet(instruction) => {
                let row = register_planet_row::RegisterPlanetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterProgressionConfig(instruction) => {
                let row = register_progression_config_row::RegisterProgressionConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterResource(instruction) => {
                let row = register_resource_row::RegisterResourceRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSageCrewConfig(instruction) => {
                let row = register_sage_crew_config_row::RegisterSageCrewConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSagePlayerProfile(instruction) => {
                let row =
                    register_sage_player_profile_row::RegisterSagePlayerProfileRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSagePointModifier(instruction) => {
                let row =
                    register_sage_point_modifier_row::RegisterSagePointModifierRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSector(instruction) => {
                let row = register_sector_row::RegisterSectorRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterShip(instruction) => {
                let row = register_ship_row::RegisterShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterStar(instruction) => {
                let row = register_star_row::RegisterStarRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterStarbase(instruction) => {
                let row = register_starbase_row::RegisterStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterStarbasePlayer(instruction) => {
                let row = register_starbase_player_row::RegisterStarbasePlayerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RegisterSurveyDataUnitTracker(instruction) => {
                let row = register_survey_data_unit_tracker_row::RegisterSurveyDataUnitTrackerRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ReloadFleetAbilityPower(instruction) => {
                let row = reload_fleet_ability_power_row::ReloadFleetAbilityPowerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveCargoPod(instruction) => {
                let row = remove_cargo_pod_row::RemoveCargoPodRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveConnection(instruction) => {
                let row = remove_connection_row::RemoveConnectionRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveCrewFromGame(instruction) => {
                let row = remove_crew_from_game_row::RemoveCrewFromGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveInvalidShipEscrow(instruction) => {
                let row = remove_invalid_ship_escrow_row::RemoveInvalidShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RemoveShipEscrow(instruction) => {
                let row = remove_ship_escrow_row::RemoveShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RepairDockedFleet(instruction) => {
                let row = repair_docked_fleet_row::RepairDockedFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RepairIdleFleet(instruction) => {
                let row = repair_idle_fleet_row::RepairIdleFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RepairStarbase(instruction) => {
                let row = repair_starbase_row::RepairStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RespawnToLoadingBay(instruction) => {
                let row = respawn_to_loading_bay_row::RespawnToLoadingBayRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::RetrieveLoot(instruction) => {
                let row = retrieve_loot_row::RetrieveLootRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::ScanForSurveyDataUnits(instruction) => {
                let row = scan_for_survey_data_units_row::ScanForSurveyDataUnitsRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::SetNextShip(instruction) => {
                let row = set_next_ship_row::SetNextShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::StartCraftingProcess(instruction) => {
                let row = start_crafting_process_row::StartCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::StartMiningAsteroid(instruction) => {
                let row = start_mining_asteroid_row::StartMiningAsteroidRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::StartStarbaseUpgrade(instruction) => {
                let row = start_starbase_upgrade_row::StartStarbaseUpgradeRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::StartSubwarp(instruction) => {
                let row = start_subwarp_row::StartSubwarpRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::StopCraftingProcess(instruction) => {
                let row = stop_crafting_process_row::StopCraftingProcessRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::StopMiningAsteroid(instruction) => {
                let row = stop_mining_asteroid_row::StopMiningAsteroidRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::StopSubwarp(instruction) => {
                let row = stop_subwarp_row::StopSubwarpRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::SubmitStarbaseUpgradeResource(instruction) => {
                let row = submit_starbase_upgrade_resource_row::SubmitStarbaseUpgradeResourceRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::SyncStarbasePlayer(instruction) => {
                let row = sync_starbase_player_row::SyncStarbasePlayerRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::SyncStarbaseUpgradeIngredients(instruction) => {
                let row = sync_starbase_upgrade_ingredients_row::SyncStarbaseUpgradeIngredientsRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::TransferCargoAtStarbase(instruction) => {
                let row = transfer_cargo_at_starbase_row::TransferCargoAtStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::TransferCargoWithinFleet(instruction) => {
                let row = transfer_cargo_within_fleet_row::TransferCargoWithinFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UnloadFleetCrew(instruction) => {
                let row = unload_fleet_crew_row::UnloadFleetCrewRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateCombatConfig(instruction) => {
                let row = update_combat_config_row::UpdateCombatConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateGame(instruction) => {
                let row = update_game_row::UpdateGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateGameState(instruction) => {
                let row = update_game_state_row::UpdateGameStateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateMineItem(instruction) => {
                let row = update_mine_item_row::UpdateMineItemRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdatePlanet(instruction) => {
                let row = update_planet_row::UpdatePlanetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateProgressionConfig(instruction) => {
                let row = update_progression_config_row::UpdateProgressionConfigRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateResource(instruction) => {
                let row = update_resource_row::UpdateResourceRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateShip(instruction) => {
                let row = update_ship_row::UpdateShipRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateShipEscrow(instruction) => {
                let row = update_ship_escrow_row::UpdateShipEscrowRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateShipInFleet(instruction) => {
                let row = update_ship_in_fleet_row::UpdateShipInFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateStar(instruction) => {
                let row = update_star_row::UpdateStarRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateStarbase(instruction) => {
                let row = update_starbase_row::UpdateStarbaseRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::UpdateSurveyDataUnitTracker(instruction) => {
                let row =
                    update_survey_data_unit_tracker_row::UpdateSurveyDataUnitTrackerRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::WarpLane(instruction) => {
                let row =
                    warp_lane_row::WarpLaneRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::WarpToCoordinate(instruction) => {
                let row = warp_to_coordinate_row::WarpToCoordinateRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::WithdrawCargoFromFleet(instruction) => {
                let row = withdraw_cargo_from_fleet_row::WithdrawCargoFromFleetRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::WithdrawCargoFromGame(instruction) => {
                let row = withdraw_cargo_from_game_row::WithdrawCargoFromGameRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::WithdrawCraftingIngredient(instruction) => {
                let row =
                    withdraw_crafting_ingredient_row::WithdrawCraftingIngredientRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            SageInstruction::CpiEvent(instruction) => {
                let row =
                    cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
