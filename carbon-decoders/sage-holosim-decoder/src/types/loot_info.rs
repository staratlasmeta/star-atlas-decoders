//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;
/// Represents loot at a location

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LootInfo {
    /// the time the exclusivity lock expires
    pub exclusivity_unlock_time: i64,
    /// exclusive access to loot until exclusivity_unlock_time passes
    /// This is a Profile or Pubkey::default
    pub destroyer: Pubkey,
    /// the cargo pod that contains the loot
    pub loot: Pubkey,
}
