//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::ProgressionItemInputUnpackedGraphQL;
use carbon_core::graphql::primitives::U64;
use juniper::GraphQLObject;
#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "UpdateProgressionConfigInput")]
pub struct UpdateProgressionConfigInputGraphQL {
    pub key_index: i32,
    pub daily_lp_limit: Option<U64>,
    pub daily_council_rank_xp_limit: Option<U64>,
    pub daily_pilot_xp_limit: Option<U64>,
    pub daily_data_running_xp_limit: Option<U64>,
    pub daily_mining_xp_limit: Option<U64>,
    pub daily_crafting_xp_limit: Option<U64>,
    pub items: Option<Vec<ProgressionItemInputUnpackedGraphQL>>,
}

impl From<crate::types::UpdateProgressionConfigInput> for UpdateProgressionConfigInputGraphQL {
    fn from(original: crate::types::UpdateProgressionConfigInput) -> Self {
        Self {
            key_index: original.key_index as i32,
            daily_lp_limit: original
                .daily_lp_limit
                .map(|v| carbon_core::graphql::primitives::U64(v)),
            daily_council_rank_xp_limit: original
                .daily_council_rank_xp_limit
                .map(|v| carbon_core::graphql::primitives::U64(v)),
            daily_pilot_xp_limit: original
                .daily_pilot_xp_limit
                .map(|v| carbon_core::graphql::primitives::U64(v)),
            daily_data_running_xp_limit: original
                .daily_data_running_xp_limit
                .map(|v| carbon_core::graphql::primitives::U64(v)),
            daily_mining_xp_limit: original
                .daily_mining_xp_limit
                .map(|v| carbon_core::graphql::primitives::U64(v)),
            daily_crafting_xp_limit: original
                .daily_crafting_xp_limit
                .map(|v| carbon_core::graphql::primitives::U64(v)),
            items: original
                .items
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
        }
    }
}
