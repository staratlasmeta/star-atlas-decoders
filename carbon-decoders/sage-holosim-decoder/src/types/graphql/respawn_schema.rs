//! This code was AUTOGENERATED using the Codama library.
use carbon_core::graphql::primitives::I64;
use juniper::GraphQLObject;
#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Respawn")]
pub struct RespawnGraphQL {
    pub sector: Vec<I64>,
    pub start: I64,
}

impl From<crate::types::Respawn> for RespawnGraphQL {
    fn from(original: crate::types::Respawn) -> Self {
        Self {
            sector: original
                .sector
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::I64(item))
                .collect(),
            start: carbon_core::graphql::primitives::I64(original.start),
        }
    }
}
