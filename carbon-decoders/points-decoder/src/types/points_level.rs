//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;
/// Defines an individual points level

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct PointsLevel {
    /// the level
    pub level: u16,
    /// the amount of points required for this level
    /// the total points required for the level i.e. the amount needed to upgrade to this level from level 0
    pub points: u64,
    /// the token mint or vault
    /// this allows for upgrading levels to require tokens (e.g. a piloting license)
    pub token_vault: Pubkey,
    /// the token qty
    pub token_qty: u64,
}
