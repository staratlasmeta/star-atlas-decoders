//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// PDA of an User for each specific type of Points

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct UserPointsAccount {
    /// The data version of this account.
    pub version: u8,
    /// The profile this account is for
    pub profile: Pubkey,
    /// point_category
    pub point_category: Pubkey,
    /// earned_points
    pub earned_points: u64,
    /// spent_points
    pub spent_points: u64,
    /// The current level; 0 when no level
    pub level: u16,
    /// daily earned_points
    pub daily_earned_points: u64,
    /// The last timestamp at which points were earned
    pub last_earned_points_timestamp: i64,
    /// PDA bump
    pub bump: u8,
}

impl UserPointsAccount {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[37, 236, 6, 21, 50, 25, 249, 84] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
