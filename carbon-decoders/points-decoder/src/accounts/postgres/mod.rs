//! This code was AUTOGENERATED using the Codama library.
pub mod point_category_row;
pub mod points_modifier_row;
pub mod user_points_account_row;

pub use self::point_category_row::*;
pub use self::points_modifier_row::*;
pub use self::user_points_account_row::*;

use super::PointsAccount;

pub struct PointsAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for PointsAccountsMigration {
    fn app(&self) -> &str {
        "points"
    }

    fn name(&self) -> &str {
        "points_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(PointCategoryMigrationOperation),
            Box::new(PointsModifierMigrationOperation),
            Box::new(UserPointsAccountMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct PointsAccountWithMetadata(pub PointsAccount, pub carbon_core::account::AccountMetadata);

impl From<(PointsAccount, carbon_core::account::AccountMetadata)> for PointsAccountWithMetadata {
    fn from(value: (PointsAccount, carbon_core::account::AccountMetadata)) -> Self {
        PointsAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for PointsAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let PointsAccountWithMetadata(account, metadata) = self;

        match account {
            PointsAccount::PointCategory(account) => {
                let row = point_category_row::PointCategoryRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            PointsAccount::PointsModifier(account) => {
                let row = points_modifier_row::PointsModifierRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            PointsAccount::UserPointsAccount(account) => {
                let row = user_points_account_row::UserPointsAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for PointsAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let PointsAccountWithMetadata(account, metadata) = self;
        match account {
            PointsAccount::PointCategory(account) => {
                let row = point_category_row::PointCategoryRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            PointsAccount::PointsModifier(account) => {
                let row = points_modifier_row::PointsModifierRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            PointsAccount::UserPointsAccount(account) => {
                let row = user_points_account_row::UserPointsAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
