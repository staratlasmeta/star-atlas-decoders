//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::PointsDecoder;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod add_point_category_level;
pub mod create_point_category;
pub mod create_user_point_account;
pub mod create_user_point_account_with_license;
pub mod decrement_level;
pub mod decrement_points;
pub mod deregister_point_modifier;
pub mod increment_level;
pub mod increment_level_beyond_threshold;
pub mod increment_points;
pub mod register_point_modifier;
pub mod remove_point_category_level;
pub mod spend_points;
pub mod update_point_category;

pub use self::add_point_category_level::*;
pub use self::create_point_category::*;
pub use self::create_user_point_account::*;
pub use self::create_user_point_account_with_license::*;
pub use self::decrement_level::*;
pub use self::decrement_points::*;
pub use self::deregister_point_modifier::*;
pub use self::increment_level::*;
pub use self::increment_level_beyond_threshold::*;
pub use self::increment_points::*;
pub use self::register_point_modifier::*;
pub use self::remove_point_category_level::*;
pub use self::spend_points::*;
pub use self::update_point_category::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum PointsInstruction {
    AddPointCategoryLevel(AddPointCategoryLevel),
    CreatePointCategory(CreatePointCategory),
    CreateUserPointAccount(CreateUserPointAccount),
    CreateUserPointAccountWithLicense(CreateUserPointAccountWithLicense),
    DecrementLevel(DecrementLevel),
    DecrementPoints(DecrementPoints),
    DeregisterPointModifier(DeregisterPointModifier),
    IncrementLevel(IncrementLevel),
    IncrementLevelBeyondThreshold(IncrementLevelBeyondThreshold),
    IncrementPoints(IncrementPoints),
    RegisterPointModifier(RegisterPointModifier),
    RemovePointCategoryLevel(RemovePointCategoryLevel),
    SpendPoints(SpendPoints),
    UpdatePointCategory(UpdatePointCategory),
}

impl carbon_core::instruction::InstructionDecoder<'_> for PointsDecoder {
    type InstructionType = PointsInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = add_point_category_level::AddPointCategoryLevel::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::AddPointCategoryLevel(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_point_category::CreatePointCategory::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::CreatePointCategory(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_user_point_account::CreateUserPointAccount::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::CreateUserPointAccount(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                create_user_point_account_with_license::CreateUserPointAccountWithLicense::decode(
                    data,
                )
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::CreateUserPointAccountWithLicense(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = decrement_level::DecrementLevel::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::DecrementLevel(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = decrement_points::DecrementPoints::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::DecrementPoints(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = deregister_point_modifier::DeregisterPointModifier::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::DeregisterPointModifier(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = increment_level::IncrementLevel::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::IncrementLevel(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                increment_level_beyond_threshold::IncrementLevelBeyondThreshold::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::IncrementLevelBeyondThreshold(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = increment_points::IncrementPoints::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::IncrementPoints(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_point_modifier::RegisterPointModifier::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::RegisterPointModifier(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                remove_point_category_level::RemovePointCategoryLevel::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::RemovePointCategoryLevel(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = spend_points::SpendPoints::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::SpendPoints(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_point_category::UpdatePointCategory::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsInstruction::UpdatePointCategory(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
