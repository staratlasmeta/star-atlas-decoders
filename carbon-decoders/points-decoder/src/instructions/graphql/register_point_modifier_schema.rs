//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "RegisterPointModifier")]
pub struct RegisterPointModifierGraphQL {
    pub instruction_metadata: crate::instructions::graphql::InstructionMetadataGraphQL,
    pub can_increment: bool,
    pub can_decrement: bool,
    pub key_index: i32,
}

impl TryFrom<crate::instructions::postgres::RegisterPointModifierRow>
    for RegisterPointModifierGraphQL
{
    type Error = carbon_core::error::Error;
    fn try_from(
        row: crate::instructions::postgres::RegisterPointModifierRow,
    ) -> Result<Self, Self::Error> {
        Ok(Self {
            instruction_metadata: row.instruction_metadata.into(),
            can_increment: row.can_increment,
            can_decrement: row.can_decrement,
            key_index: (*row.key_index) as i32,
        })
    }
}
