//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::ScoreDecoder;

pub mod score_vars;
pub mod score_vars_ship;
pub mod ship_staking;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum ScoreAccount {
    ScoreVars(Box<score_vars::ScoreVars>),
    ScoreVarsShip(Box<score_vars_ship::ScoreVarsShip>),
    ShipStaking(Box<ship_staking::ShipStaking>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for ScoreDecoder {
    type AccountType = ScoreAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = score_vars::ScoreVars::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ScoreAccount::ScoreVars(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = score_vars_ship::ScoreVarsShip::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ScoreAccount::ScoreVarsShip(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = ship_staking::ShipStaking::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ScoreAccount::ShipStaking(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
