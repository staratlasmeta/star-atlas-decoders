//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::ProxyRewarderDecoder;

pub mod proxy;
pub mod proxy_escrow;
pub mod registered_locker;
pub mod treasury_authority;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum ProxyRewarderAccount {
    Proxy(Box<proxy::Proxy>),
    ProxyEscrow(Box<proxy_escrow::ProxyEscrow>),
    RegisteredLocker(Box<registered_locker::RegisteredLocker>),
    TreasuryAuthority(Box<treasury_authority::TreasuryAuthority>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for ProxyRewarderDecoder {
    type AccountType = ProxyRewarderAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = proxy::Proxy::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ProxyRewarderAccount::Proxy(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = proxy_escrow::ProxyEscrow::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ProxyRewarderAccount::ProxyEscrow(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = registered_locker::RegisteredLocker::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ProxyRewarderAccount::RegisteredLocker(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = treasury_authority::TreasuryAuthority::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ProxyRewarderAccount::TreasuryAuthority(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
