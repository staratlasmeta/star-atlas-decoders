//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::ProxyRewarderDecoder;

pub mod claim_rewards;
pub mod cpi_event;
pub mod new_proxy;
pub mod new_proxy_escrow;
pub mod proxy_exit;
pub mod proxy_lock;
pub mod register_locker;
pub mod update_registered_locker;

pub use self::claim_rewards::*;
pub use self::cpi_event::*;
pub use self::new_proxy::*;
pub use self::new_proxy_escrow::*;
pub use self::proxy_exit::*;
pub use self::proxy_lock::*;
pub use self::register_locker::*;
pub use self::update_registered_locker::*;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[cfg_attr(
    feature = "serde",
    derive(carbon_core::InstructionType, serde::Serialize, serde::Deserialize)
)]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum ProxyRewarderInstruction {
    ClaimRewards(ClaimRewards),
    NewProxy(NewProxy),
    NewProxyEscrow(NewProxyEscrow),
    ProxyExit(ProxyExit),
    ProxyLock(ProxyLock),
    RegisterLocker(RegisterLocker),
    UpdateRegisteredLocker(UpdateRegisteredLocker),
    // Anchor CPI Event Instruction
    CpiEvent(CpiEvent),
}

impl carbon_core::instruction::InstructionDecoder<'_> for ProxyRewarderDecoder {
    type InstructionType = ProxyRewarderInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = register_locker::RegisterLocker::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::RegisterLocker(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_registered_locker::UpdateRegisteredLocker::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::UpdateRegisteredLocker(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = new_proxy_escrow::NewProxyEscrow::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::NewProxyEscrow(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = new_proxy::NewProxy::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::NewProxy(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = proxy_lock::ProxyLock::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::ProxyLock(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = proxy_exit::ProxyExit::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::ProxyExit(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = claim_rewards::ClaimRewards::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::ClaimRewards(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = cpi_event::CpiEvent::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ProxyRewarderInstruction::CpiEvent(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
