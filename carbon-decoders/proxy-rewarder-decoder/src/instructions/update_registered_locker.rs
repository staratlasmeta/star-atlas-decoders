//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Updates `rewards` arrays in registered_locker [proxy_rewarder::registered_locker].

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct UpdateRegisteredLocker {
    pub era: u16,
    pub index: u16,
    pub period_reward_amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateRegisteredLockerInstructionAccounts {
    pub admin: solana_pubkey::Pubkey,
    pub locker: solana_pubkey::Pubkey,
    pub registered_locker: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateRegisteredLocker {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[134, 107, 193, 117, 188, 70, 189, 92] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for UpdateRegisteredLocker {
    type ArrangedAccounts = UpdateRegisteredLockerInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let admin = next_account(&mut iter)?;
        let locker = next_account(&mut iter)?;
        let registered_locker = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateRegisteredLockerInstructionAccounts {
            admin,
            locker,
            registered_locker,
            remaining: remaining.to_vec(),
        })
    }
}
