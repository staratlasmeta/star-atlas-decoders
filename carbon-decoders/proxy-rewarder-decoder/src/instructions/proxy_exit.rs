//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Exits the POLIS DAO; i.e., withdraws all staked tokens in an [Escrow] if the [Escrow] is unlocked.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct ProxyExit {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ProxyExitInstructionAccounts {
    pub locker: solana_pubkey::Pubkey,
    pub escrow: solana_pubkey::Pubkey,
    pub proxy: solana_pubkey::Pubkey,
    pub escrow_token_account: solana_pubkey::Pubkey,
    pub proxy_token_account: solana_pubkey::Pubkey,
    pub user_token_account: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub locked_voter_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub registered_locker: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ProxyExit {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[205, 235, 156, 190, 11, 93, 98, 70] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ProxyExit {
    type ArrangedAccounts = ProxyExitInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let locker = next_account(&mut iter)?;
        let escrow = next_account(&mut iter)?;
        let proxy = next_account(&mut iter)?;
        let escrow_token_account = next_account(&mut iter)?;
        let proxy_token_account = next_account(&mut iter)?;
        let user_token_account = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let locked_voter_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let registered_locker = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ProxyExitInstructionAccounts {
            locker,
            escrow,
            proxy,
            escrow_token_account,
            proxy_token_account,
            user_token_account,
            payer,
            locked_voter_program,
            token_program,
            registered_locker,
            remaining: remaining.to_vec(),
        })
    }
}
