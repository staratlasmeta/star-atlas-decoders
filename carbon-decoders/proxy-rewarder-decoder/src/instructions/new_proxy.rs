//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Creates a new [Proxy] for an account.
///
/// A Reward Escrow, or [Escrow] for short, is an agreement between a users=>proxy (known as the `authority`) and the Reward Program to
/// reward POLIS over specific periods of time. (curve)
/// proportional to the amount of stakers.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct NewProxy {
    pub bump: u8,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct NewProxyInstructionAccounts {
    pub proxy_escrow: solana_pubkey::Pubkey,
    pub proxy: solana_pubkey::Pubkey,
    pub token_mint: solana_pubkey::Pubkey,
    pub proxy_owner: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl NewProxy {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[158, 43, 152, 240, 31, 181, 251, 32] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for NewProxy {
    type ArrangedAccounts = NewProxyInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let proxy_escrow = next_account(&mut iter)?;
        let proxy = next_account(&mut iter)?;
        let token_mint = next_account(&mut iter)?;
        let proxy_owner = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(NewProxyInstructionAccounts {
            proxy_escrow,
            proxy,
            token_mint,
            proxy_owner,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
