//! This code was AUTOGENERATED using the Codama library.
use crate::AtlasStakingDecoder;
use crate::PROGRAM_ID;

pub mod registered_stake;
pub mod staking_account;
pub mod staking_vars;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum AtlasStakingAccount {
    RegisteredStake(Box<registered_stake::RegisteredStake>),
    StakingAccount(Box<staking_account::StakingAccount>),
    StakingVars(Box<staking_vars::StakingVars>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for AtlasStakingDecoder {
    type AccountType = AtlasStakingAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = registered_stake::RegisteredStake::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: AtlasStakingAccount::RegisteredStake(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = staking_account::StakingAccount::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: AtlasStakingAccount::StakingAccount(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = staking_vars::StakingVars::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: AtlasStakingAccount::StakingVars(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
