//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U16;
use carbon_core::postgres::primitives::U64;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct RegisteredStakeRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub authority: Pubkey,
    pub stake_mint: Pubkey,
    pub reward_mint: Pubkey,
    pub current_period: U16,
    pub reward_multiplier: U64,
    pub cooldown_period: U64,
    pub bump: U8,
}

impl RegisteredStakeRow {
    pub fn from_parts(
        source: crate::accounts::registered_stake::RegisteredStake,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            authority: source.authority.into(),
            stake_mint: source.stake_mint.into(),
            reward_mint: source.reward_mint.into(),
            current_period: source.current_period.into(),
            reward_multiplier: source.reward_multiplier.into(),
            cooldown_period: source.cooldown_period.into(),
            bump: source.bump.into(),
        }
    }
}

impl TryFrom<RegisteredStakeRow> for crate::accounts::registered_stake::RegisteredStake {
    type Error = carbon_core::error::Error;
    fn try_from(source: RegisteredStakeRow) -> Result<Self, Self::Error> {
        Ok(Self {
            authority: *source.authority,
            stake_mint: *source.stake_mint,
            reward_mint: *source.reward_mint,
            current_period: source.current_period.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            reward_multiplier: *source.reward_multiplier,
            cooldown_period: *source.cooldown_period,
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::registered_stake::RegisteredStake
{
    fn table() -> &'static str {
        "registered_stake_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "authority",
            "stake_mint",
            "reward_mint",
            "current_period",
            "reward_multiplier",
            "cooldown_period",
            "bump",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RegisteredStakeRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO registered_stake_account (
                "authority",
                "stake_mint",
                "reward_mint",
                "current_period",
                "reward_multiplier",
                "cooldown_period",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.authority.clone())
        .bind(self.stake_mint.clone())
        .bind(self.reward_mint.clone())
        .bind(self.current_period.clone())
        .bind(self.reward_multiplier.clone())
        .bind(self.cooldown_period.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RegisteredStakeRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO registered_stake_account (
                "authority",
                "stake_mint",
                "reward_mint",
                "current_period",
                "reward_multiplier",
                "cooldown_period",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "authority" = EXCLUDED."authority",
                "stake_mint" = EXCLUDED."stake_mint",
                "reward_mint" = EXCLUDED."reward_mint",
                "current_period" = EXCLUDED."current_period",
                "reward_multiplier" = EXCLUDED."reward_multiplier",
                "cooldown_period" = EXCLUDED."cooldown_period",
                "bump" = EXCLUDED."bump",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.authority.clone())
        .bind(self.stake_mint.clone())
        .bind(self.reward_mint.clone())
        .bind(self.current_period.clone())
        .bind(self.reward_multiplier.clone())
        .bind(self.cooldown_period.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for RegisteredStakeRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM registered_stake_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for RegisteredStakeRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM registered_stake_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct RegisteredStakeMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for RegisteredStakeMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS registered_stake_account (
                -- Account data
                "authority" BYTEA NOT NULL,
                "stake_mint" BYTEA NOT NULL,
                "reward_mint" BYTEA NOT NULL,
                "current_period" INT4 NOT NULL,
                "reward_multiplier" NUMERIC(20) NOT NULL,
                "cooldown_period" NUMERIC(20) NOT NULL,
                "bump" INT2 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS registered_stake_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
