//! This code was AUTOGENERATED using the Codama library.
use crate::AtlasStakingDecoder;
use crate::PROGRAM_ID;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod cancel_unstake;
pub mod create_staking_account;
pub mod harvest;
pub mod initialize_staking;
pub mod register_stake;
pub mod settle;
pub mod stake_tokens;
pub mod unstake_tokens;
pub mod update_cooldown_period;
pub mod update_reward_multiplier;
pub mod withdraw_tokens;

pub use self::cancel_unstake::*;
pub use self::create_staking_account::*;
pub use self::harvest::*;
pub use self::initialize_staking::*;
pub use self::register_stake::*;
pub use self::settle::*;
pub use self::stake_tokens::*;
pub use self::unstake_tokens::*;
pub use self::update_cooldown_period::*;
pub use self::update_reward_multiplier::*;
pub use self::withdraw_tokens::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum AtlasStakingInstruction {
    CancelUnstake(CancelUnstake),
    CreateStakingAccount(CreateStakingAccount),
    Harvest(Harvest),
    InitializeStaking(InitializeStaking),
    RegisterStake(RegisterStake),
    Settle(Settle),
    StakeTokens(StakeTokens),
    UnstakeTokens(UnstakeTokens),
    UpdateCooldownPeriod(UpdateCooldownPeriod),
    UpdateRewardMultiplier(UpdateRewardMultiplier),
    WithdrawTokens(WithdrawTokens),
}

impl carbon_core::instruction::InstructionDecoder<'_> for AtlasStakingDecoder {
    type InstructionType = AtlasStakingInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = cancel_unstake::CancelUnstake::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::CancelUnstake(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_staking_account::CreateStakingAccount::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::CreateStakingAccount(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = harvest::Harvest::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::Harvest(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = initialize_staking::InitializeStaking::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::InitializeStaking(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_stake::RegisterStake::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::RegisterStake(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = settle::Settle::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::Settle(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = stake_tokens::StakeTokens::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::StakeTokens(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = unstake_tokens::UnstakeTokens::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::UnstakeTokens(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_cooldown_period::UpdateCooldownPeriod::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::UpdateCooldownPeriod(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_reward_multiplier::UpdateRewardMultiplier::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::UpdateRewardMultiplier(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = withdraw_tokens::WithdrawTokens::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasStakingInstruction::WithdrawTokens(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
