//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// A definition of cargo stats.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CargoStatsDefinition {
    /// The data version of this account.
    pub version: u8,
    /// The authority for this definition.
    pub authority: Pubkey,
    /// The default cargo type. System program (all 0s) if none.
    pub default_cargo_type: Pubkey,
    /// The number of stats in this definition.
    pub stats_count: u16,
    /// The sequence id for the definition
    pub seq_id: u16,
}

impl CargoStatsDefinition {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[60, 136, 133, 145, 129, 17, 131, 109] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
