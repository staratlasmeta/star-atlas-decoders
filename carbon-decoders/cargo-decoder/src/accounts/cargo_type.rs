//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// The stats for a given cargo type (token mint).

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CargoType {
    /// The data version of this account.
    pub version: u8,
    /// The definition this follows
    pub stats_definition: Pubkey,
    /// The mint the cargo type is for
    pub mint: Pubkey,
    /// TODO: remove this
    pub creator: Pubkey,
    /// The bump for this account
    pub bump: u8,
    /// The number of stats in this definition.
    pub stats_count: u16,
    /// The sequence id for the definition
    pub seq_id: u16,
}

impl CargoType {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[152, 102, 188, 230, 200, 14, 164, 224] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
