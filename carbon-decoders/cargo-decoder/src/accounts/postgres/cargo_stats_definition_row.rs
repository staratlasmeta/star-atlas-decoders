//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U16;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct CargoStatsDefinitionRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub authority: Pubkey,
    pub default_cargo_type: Pubkey,
    pub stats_count: U16,
    pub seq_id: U16,
}

impl CargoStatsDefinitionRow {
    pub fn from_parts(
        source: crate::accounts::cargo_stats_definition::CargoStatsDefinition,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            authority: source.authority.into(),
            default_cargo_type: source.default_cargo_type.into(),
            stats_count: source.stats_count.into(),
            seq_id: source.seq_id.into(),
        }
    }
}

impl TryFrom<CargoStatsDefinitionRow>
    for crate::accounts::cargo_stats_definition::CargoStatsDefinition
{
    type Error = carbon_core::error::Error;
    fn try_from(source: CargoStatsDefinitionRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            authority: *source.authority,
            default_cargo_type: *source.default_cargo_type,
            stats_count: source.stats_count.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            seq_id: source.seq_id.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::cargo_stats_definition::CargoStatsDefinition
{
    fn table() -> &'static str {
        "cargo_stats_definition_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "authority",
            "default_cargo_type",
            "stats_count",
            "seq_id",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CargoStatsDefinitionRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO cargo_stats_definition_account (
                "version",
                "authority",
                "default_cargo_type",
                "stats_count",
                "seq_id",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.authority.clone())
        .bind(self.default_cargo_type.clone())
        .bind(self.stats_count.clone())
        .bind(self.seq_id.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CargoStatsDefinitionRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO cargo_stats_definition_account (
                "version",
                "authority",
                "default_cargo_type",
                "stats_count",
                "seq_id",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "authority" = EXCLUDED."authority",
                "default_cargo_type" = EXCLUDED."default_cargo_type",
                "stats_count" = EXCLUDED."stats_count",
                "seq_id" = EXCLUDED."seq_id",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.authority.clone())
        .bind(self.default_cargo_type.clone())
        .bind(self.stats_count.clone())
        .bind(self.seq_id.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for CargoStatsDefinitionRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM cargo_stats_definition_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for CargoStatsDefinitionRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM cargo_stats_definition_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct CargoStatsDefinitionMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for CargoStatsDefinitionMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS cargo_stats_definition_account (
                -- Account data
                "version" INT2 NOT NULL,
                "authority" BYTEA NOT NULL,
                "default_cargo_type" BYTEA NOT NULL,
                "stats_count" INT4 NOT NULL,
                "seq_id" INT4 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS cargo_stats_definition_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
