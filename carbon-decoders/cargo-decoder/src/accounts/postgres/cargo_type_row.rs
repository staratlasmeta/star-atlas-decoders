//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U16;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct CargoTypeRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub stats_definition: Pubkey,
    pub mint: Pubkey,
    pub creator: Pubkey,
    pub bump: U8,
    pub stats_count: U16,
    pub seq_id: U16,
}

impl CargoTypeRow {
    pub fn from_parts(
        source: crate::accounts::cargo_type::CargoType,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            stats_definition: source.stats_definition.into(),
            mint: source.mint.into(),
            creator: source.creator.into(),
            bump: source.bump.into(),
            stats_count: source.stats_count.into(),
            seq_id: source.seq_id.into(),
        }
    }
}

impl TryFrom<CargoTypeRow> for crate::accounts::cargo_type::CargoType {
    type Error = carbon_core::error::Error;
    fn try_from(source: CargoTypeRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            stats_definition: *source.stats_definition,
            mint: *source.mint,
            creator: *source.creator,
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            stats_count: source.stats_count.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            seq_id: source.seq_id.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::cargo_type::CargoType {
    fn table() -> &'static str {
        "cargo_type_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "stats_definition",
            "mint",
            "creator",
            "bump",
            "stats_count",
            "seq_id",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CargoTypeRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO cargo_type_account (
                "version",
                "stats_definition",
                "mint",
                "creator",
                "bump",
                "stats_count",
                "seq_id",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.stats_definition.clone())
        .bind(self.mint.clone())
        .bind(self.creator.clone())
        .bind(self.bump.clone())
        .bind(self.stats_count.clone())
        .bind(self.seq_id.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CargoTypeRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO cargo_type_account (
                "version",
                "stats_definition",
                "mint",
                "creator",
                "bump",
                "stats_count",
                "seq_id",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "stats_definition" = EXCLUDED."stats_definition",
                "mint" = EXCLUDED."mint",
                "creator" = EXCLUDED."creator",
                "bump" = EXCLUDED."bump",
                "stats_count" = EXCLUDED."stats_count",
                "seq_id" = EXCLUDED."seq_id",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.stats_definition.clone())
        .bind(self.mint.clone())
        .bind(self.creator.clone())
        .bind(self.bump.clone())
        .bind(self.stats_count.clone())
        .bind(self.seq_id.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for CargoTypeRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM cargo_type_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for CargoTypeRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM cargo_type_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct CargoTypeMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for CargoTypeMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS cargo_type_account (
                -- Account data
                "version" INT2 NOT NULL,
                "stats_definition" BYTEA NOT NULL,
                "mint" BYTEA NOT NULL,
                "creator" BYTEA NOT NULL,
                "bump" INT2 NOT NULL,
                "stats_count" INT4 NOT NULL,
                "seq_id" INT4 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS cargo_type_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
