//! This code was AUTOGENERATED using the Codama library.
use crate::CargoDecoder;
use crate::PROGRAM_ID;

pub mod add_cargo;
pub mod close_cargo_pod;
pub mod close_token_account;
pub mod consume_cargo;
pub mod init_cargo_pod;
pub mod init_cargo_type;
pub mod init_cargo_type_for_next_seq_id;
pub mod init_cargo_type_from_old_cargo_type;
pub mod init_definition;
pub mod legitimize_cargo;
pub mod mint_to;
pub mod remove_cargo;
pub mod transfer_authority;
pub mod transfer_cargo;
pub mod update_cargo_pod;
pub mod update_definition;
pub mod update_pod_token_account;

pub use self::add_cargo::*;
pub use self::close_cargo_pod::*;
pub use self::close_token_account::*;
pub use self::consume_cargo::*;
pub use self::init_cargo_pod::*;
pub use self::init_cargo_type::*;
pub use self::init_cargo_type_for_next_seq_id::*;
pub use self::init_cargo_type_from_old_cargo_type::*;
pub use self::init_definition::*;
pub use self::legitimize_cargo::*;
pub use self::mint_to::*;
pub use self::remove_cargo::*;
pub use self::transfer_authority::*;
pub use self::transfer_cargo::*;
pub use self::update_cargo_pod::*;
pub use self::update_definition::*;
pub use self::update_pod_token_account::*;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[cfg_attr(
    feature = "serde",
    derive(carbon_core::InstructionType, serde::Serialize, serde::Deserialize)
)]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum CargoInstruction {
    AddCargo(AddCargo),
    CloseCargoPod(CloseCargoPod),
    CloseTokenAccount(CloseTokenAccount),
    ConsumeCargo(ConsumeCargo),
    InitCargoPod(InitCargoPod),
    InitCargoType(InitCargoType),
    InitCargoTypeForNextSeqId(InitCargoTypeForNextSeqId),
    InitCargoTypeFromOldCargoType(InitCargoTypeFromOldCargoType),
    InitDefinition(InitDefinition),
    LegitimizeCargo(LegitimizeCargo),
    MintTo(MintTo),
    RemoveCargo(RemoveCargo),
    TransferAuthority(TransferAuthority),
    TransferCargo(TransferCargo),
    UpdateCargoPod(UpdateCargoPod),
    UpdateDefinition(UpdateDefinition),
    UpdatePodTokenAccount(UpdatePodTokenAccount),
}

impl carbon_core::instruction::InstructionDecoder<'_> for CargoDecoder {
    type InstructionType = CargoInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = add_cargo::AddCargo::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::AddCargo(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_cargo_pod::CloseCargoPod::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::CloseCargoPod(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_token_account::CloseTokenAccount::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::CloseTokenAccount(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = consume_cargo::ConsumeCargo::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::ConsumeCargo(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = init_cargo_pod::InitCargoPod::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::InitCargoPod(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = init_cargo_type::InitCargoType::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::InitCargoType(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                init_cargo_type_for_next_seq_id::InitCargoTypeForNextSeqId::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::InitCargoTypeForNextSeqId(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                init_cargo_type_from_old_cargo_type::InitCargoTypeFromOldCargoType::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::InitCargoTypeFromOldCargoType(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = init_definition::InitDefinition::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::InitDefinition(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = legitimize_cargo::LegitimizeCargo::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::LegitimizeCargo(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = mint_to::MintTo::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::MintTo(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_cargo::RemoveCargo::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::RemoveCargo(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = transfer_authority::TransferAuthority::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::TransferAuthority(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = transfer_cargo::TransferCargo::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::TransferCargo(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_cargo_pod::UpdateCargoPod::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::UpdateCargoPod(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_definition::UpdateDefinition::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::UpdateDefinition(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_pod_token_account::UpdatePodTokenAccount::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CargoInstruction::UpdatePodTokenAccount(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
