//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::PointsStoreDecoder;

pub mod points_store;
pub mod redemption_config;
pub mod user_redemption;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum PointsStoreAccount {
    PointsStore(Box<points_store::PointsStore>),
    RedemptionConfig(Box<redemption_config::RedemptionConfig>),
    UserRedemption(Box<user_redemption::UserRedemption>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for PointsStoreDecoder {
    type AccountType = PointsStoreAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = points_store::PointsStore::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PointsStoreAccount::PointsStore(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = redemption_config::RedemptionConfig::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PointsStoreAccount::RedemptionConfig(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = user_redemption::UserRedemption::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PointsStoreAccount::UserRedemption(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
