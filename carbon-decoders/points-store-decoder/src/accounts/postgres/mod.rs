//! This code was AUTOGENERATED using the Codama library.
pub mod points_store_row;
pub mod redemption_config_row;
pub mod user_redemption_row;

pub use self::points_store_row::*;
pub use self::redemption_config_row::*;
pub use self::user_redemption_row::*;

use super::PointsStoreAccount;

pub struct PointsStoreAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for PointsStoreAccountsMigration {
    fn app(&self) -> &str {
        "points-store"
    }

    fn name(&self) -> &str {
        "points_store_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(PointsStoreMigrationOperation),
            Box::new(RedemptionConfigMigrationOperation),
            Box::new(UserRedemptionMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct PointsStoreAccountWithMetadata(
    pub PointsStoreAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(PointsStoreAccount, carbon_core::account::AccountMetadata)>
    for PointsStoreAccountWithMetadata
{
    fn from(value: (PointsStoreAccount, carbon_core::account::AccountMetadata)) -> Self {
        PointsStoreAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for PointsStoreAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let PointsStoreAccountWithMetadata(account, metadata) = self;

        match account {
            PointsStoreAccount::PointsStore(account) => {
                let row = points_store_row::PointsStoreRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            PointsStoreAccount::RedemptionConfig(account) => {
                let row = redemption_config_row::RedemptionConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            PointsStoreAccount::UserRedemption(account) => {
                let row = user_redemption_row::UserRedemptionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for PointsStoreAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let PointsStoreAccountWithMetadata(account, metadata) = self;
        match account {
            PointsStoreAccount::PointsStore(account) => {
                let row = points_store_row::PointsStoreRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            PointsStoreAccount::RedemptionConfig(account) => {
                let row = redemption_config_row::RedemptionConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            PointsStoreAccount::UserRedemption(account) => {
                let row = user_redemption_row::UserRedemptionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
