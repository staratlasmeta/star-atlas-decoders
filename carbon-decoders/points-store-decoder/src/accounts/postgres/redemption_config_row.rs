//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct RedemptionConfigRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub point_category: Pubkey,
    pub profile: Pubkey,
    pub faction: U8,
    pub bank: Pubkey,
    pub signer_bump: U8,
    pub allow_only_current_epoch: U8,
}

impl RedemptionConfigRow {
    pub fn from_parts(
        source: crate::accounts::redemption_config::RedemptionConfig,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            point_category: source.point_category.into(),
            profile: source.profile.into(),
            faction: source.faction.into(),
            bank: source.bank.into(),
            signer_bump: source.signer_bump.into(),
            allow_only_current_epoch: source.allow_only_current_epoch.into(),
        }
    }
}

impl TryFrom<RedemptionConfigRow> for crate::accounts::redemption_config::RedemptionConfig {
    type Error = carbon_core::error::Error;
    fn try_from(source: RedemptionConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            point_category: *source.point_category,
            profile: *source.profile,
            faction: source.faction.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            bank: *source.bank,
            signer_bump: source.signer_bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            allow_only_current_epoch: source.allow_only_current_epoch.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::redemption_config::RedemptionConfig
{
    fn table() -> &'static str {
        "redemption_config_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "point_category",
            "profile",
            "faction",
            "bank",
            "signer_bump",
            "allow_only_current_epoch",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RedemptionConfigRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO redemption_config_account (
                "version",
                "point_category",
                "profile",
                "faction",
                "bank",
                "signer_bump",
                "allow_only_current_epoch",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.point_category.clone())
        .bind(self.profile.clone())
        .bind(self.faction.clone())
        .bind(self.bank.clone())
        .bind(self.signer_bump.clone())
        .bind(self.allow_only_current_epoch.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RedemptionConfigRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO redemption_config_account (
                "version",
                "point_category",
                "profile",
                "faction",
                "bank",
                "signer_bump",
                "allow_only_current_epoch",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "point_category" = EXCLUDED."point_category",
                "profile" = EXCLUDED."profile",
                "faction" = EXCLUDED."faction",
                "bank" = EXCLUDED."bank",
                "signer_bump" = EXCLUDED."signer_bump",
                "allow_only_current_epoch" = EXCLUDED."allow_only_current_epoch",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.point_category.clone())
        .bind(self.profile.clone())
        .bind(self.faction.clone())
        .bind(self.bank.clone())
        .bind(self.signer_bump.clone())
        .bind(self.allow_only_current_epoch.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for RedemptionConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM redemption_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for RedemptionConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM redemption_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct RedemptionConfigMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for RedemptionConfigMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS redemption_config_account (
                -- Account data
                "version" INT2 NOT NULL,
                "point_category" BYTEA NOT NULL,
                "profile" BYTEA NOT NULL,
                "faction" INT2 NOT NULL,
                "bank" BYTEA NOT NULL,
                "signer_bump" INT2 NOT NULL,
                "allow_only_current_epoch" INT2 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS redemption_config_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
