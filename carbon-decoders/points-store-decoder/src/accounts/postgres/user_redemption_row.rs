//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U64;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct UserRedemptionRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub profile: Pubkey,
    pub point_category: Pubkey,
    pub user_points_account: Pubkey,
    pub config: Pubkey,
    pub points: U64,
    pub day_index: i64,
}

impl UserRedemptionRow {
    pub fn from_parts(
        source: crate::accounts::user_redemption::UserRedemption,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            profile: source.profile.into(),
            point_category: source.point_category.into(),
            user_points_account: source.user_points_account.into(),
            config: source.config.into(),
            points: source.points.into(),
            day_index: source.day_index.into(),
        }
    }
}

impl TryFrom<UserRedemptionRow> for crate::accounts::user_redemption::UserRedemption {
    type Error = carbon_core::error::Error;
    fn try_from(source: UserRedemptionRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            profile: *source.profile,
            point_category: *source.point_category,
            user_points_account: *source.user_points_account,
            config: *source.config,
            points: *source.points,
            day_index: source.day_index.into(),
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::user_redemption::UserRedemption {
    fn table() -> &'static str {
        "user_redemption_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "profile",
            "point_category",
            "user_points_account",
            "config",
            "points",
            "day_index",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for UserRedemptionRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO user_redemption_account (
                "version",
                "profile",
                "point_category",
                "user_points_account",
                "config",
                "points",
                "day_index",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.profile.clone())
        .bind(self.point_category.clone())
        .bind(self.user_points_account.clone())
        .bind(self.config.clone())
        .bind(self.points.clone())
        .bind(self.day_index.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for UserRedemptionRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO user_redemption_account (
                "version",
                "profile",
                "point_category",
                "user_points_account",
                "config",
                "points",
                "day_index",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "profile" = EXCLUDED."profile",
                "point_category" = EXCLUDED."point_category",
                "user_points_account" = EXCLUDED."user_points_account",
                "config" = EXCLUDED."config",
                "points" = EXCLUDED."points",
                "day_index" = EXCLUDED."day_index",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.profile.clone())
        .bind(self.point_category.clone())
        .bind(self.user_points_account.clone())
        .bind(self.config.clone())
        .bind(self.points.clone())
        .bind(self.day_index.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for UserRedemptionRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM user_redemption_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for UserRedemptionRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM user_redemption_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct UserRedemptionMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for UserRedemptionMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS user_redemption_account (
                -- Account data
                "version" INT2 NOT NULL,
                "profile" BYTEA NOT NULL,
                "point_category" BYTEA NOT NULL,
                "user_points_account" BYTEA NOT NULL,
                "config" BYTEA NOT NULL,
                "points" NUMERIC(20) NOT NULL,
                "day_index" INT8 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS user_redemption_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
