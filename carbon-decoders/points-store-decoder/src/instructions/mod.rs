//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::PointsStoreDecoder;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod add_redemption_epoch;
pub mod buy;
pub mod change_store_price;
pub mod claim_tokens;
pub mod close_redemption_config;
pub mod close_store;
pub mod contribute_to_redemption;
pub mod create_points_store;
pub mod create_redemption_config;
pub mod remove_redemption_epoch;
pub mod remove_store_items;
pub mod start_redemption;
pub mod update_redemption_epoch;

pub use self::add_redemption_epoch::*;
pub use self::buy::*;
pub use self::change_store_price::*;
pub use self::claim_tokens::*;
pub use self::close_redemption_config::*;
pub use self::close_store::*;
pub use self::contribute_to_redemption::*;
pub use self::create_points_store::*;
pub use self::create_redemption_config::*;
pub use self::remove_redemption_epoch::*;
pub use self::remove_store_items::*;
pub use self::start_redemption::*;
pub use self::update_redemption_epoch::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum PointsStoreInstruction {
    AddRedemptionEpoch(AddRedemptionEpoch),
    Buy(Buy),
    ChangeStorePrice(ChangeStorePrice),
    ClaimTokens(ClaimTokens),
    CloseRedemptionConfig(CloseRedemptionConfig),
    CloseStore(CloseStore),
    ContributeToRedemption(ContributeToRedemption),
    CreatePointsStore(CreatePointsStore),
    CreateRedemptionConfig(CreateRedemptionConfig),
    RemoveRedemptionEpoch(RemoveRedemptionEpoch),
    RemoveStoreItems(RemoveStoreItems),
    StartRedemption(StartRedemption),
    UpdateRedemptionEpoch(UpdateRedemptionEpoch),
}

impl carbon_core::instruction::InstructionDecoder<'_> for PointsStoreDecoder {
    type InstructionType = PointsStoreInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = add_redemption_epoch::AddRedemptionEpoch::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::AddRedemptionEpoch(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = buy::Buy::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::Buy(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = change_store_price::ChangeStorePrice::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::ChangeStorePrice(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = claim_tokens::ClaimTokens::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::ClaimTokens(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_redemption_config::CloseRedemptionConfig::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::CloseRedemptionConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_store::CloseStore::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::CloseStore(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = contribute_to_redemption::ContributeToRedemption::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::ContributeToRedemption(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_points_store::CreatePointsStore::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::CreatePointsStore(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_redemption_config::CreateRedemptionConfig::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::CreateRedemptionConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_redemption_epoch::RemoveRedemptionEpoch::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::RemoveRedemptionEpoch(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_store_items::RemoveStoreItems::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::RemoveStoreItems(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = start_redemption::StartRedemption::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::StartRedemption(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_redemption_epoch::UpdateRedemptionEpoch::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: PointsStoreInstruction::UpdateRedemptionEpoch(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
