//! This code was AUTOGENERATED using the Codama library.
use crate::LockedVoterDecoder;
use crate::PROGRAM_ID;

pub mod activate_proposal;
pub mod approve_program_lock_privilege;
pub mod cast_vote;
pub mod cpi_event;
pub mod exit;
pub mod lock;
pub mod lock_with_whitelist_entry;
pub mod new_escrow;
pub mod new_locker;
pub mod revoke_program_lock_privilege;
pub mod set_locker_params;
pub mod set_vote_delegate;

pub use self::activate_proposal::*;
pub use self::approve_program_lock_privilege::*;
pub use self::cast_vote::*;
pub use self::cpi_event::*;
pub use self::exit::*;
pub use self::lock::*;
pub use self::lock_with_whitelist_entry::*;
pub use self::new_escrow::*;
pub use self::new_locker::*;
pub use self::revoke_program_lock_privilege::*;
pub use self::set_locker_params::*;
pub use self::set_vote_delegate::*;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[cfg_attr(
    feature = "serde",
    derive(carbon_core::InstructionType, serde::Serialize, serde::Deserialize)
)]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum LockedVoterInstruction {
    ActivateProposal(ActivateProposal),
    ApproveProgramLockPrivilege(ApproveProgramLockPrivilege),
    CastVote(CastVote),
    Exit(Exit),
    Lock(Lock),
    LockWithWhitelistEntry(LockWithWhitelistEntry),
    NewEscrow(NewEscrow),
    NewLocker(NewLocker),
    RevokeProgramLockPrivilege(RevokeProgramLockPrivilege),
    SetLockerParams(SetLockerParams),
    SetVoteDelegate(SetVoteDelegate),
    // Anchor CPI Event Instruction
    CpiEvent(CpiEvent),
}

impl carbon_core::instruction::InstructionDecoder<'_> for LockedVoterDecoder {
    type InstructionType = LockedVoterInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = new_locker::NewLocker::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::NewLocker(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = new_escrow::NewEscrow::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::NewEscrow(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = lock::Lock::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::Lock(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = lock_with_whitelist_entry::LockWithWhitelistEntry::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::LockWithWhitelistEntry(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = exit::Exit::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::Exit(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = activate_proposal::ActivateProposal::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::ActivateProposal(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = cast_vote::CastVote::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::CastVote(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = set_vote_delegate::SetVoteDelegate::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::SetVoteDelegate(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = set_locker_params::SetLockerParams::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::SetLockerParams(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                approve_program_lock_privilege::ApproveProgramLockPrivilege::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::ApproveProgramLockPrivilege(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                revoke_program_lock_privilege::RevokeProgramLockPrivilege::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::RevokeProgramLockPrivilege(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = cpi_event::CpiEvent::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: LockedVoterInstruction::CpiEvent(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
