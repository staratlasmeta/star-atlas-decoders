//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct LockerWhitelistEntryRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub bump: U8,
    pub locker: Pubkey,
    pub program_id: Pubkey,
    pub owner: Pubkey,
}

impl LockerWhitelistEntryRow {
    pub fn from_parts(
        source: crate::accounts::locker_whitelist_entry::LockerWhitelistEntry,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            bump: source.bump.into(),
            locker: source.locker.into(),
            program_id: source.program_id.into(),
            owner: source.owner.into(),
        }
    }
}

impl TryFrom<LockerWhitelistEntryRow>
    for crate::accounts::locker_whitelist_entry::LockerWhitelistEntry
{
    type Error = carbon_core::error::Error;
    fn try_from(source: LockerWhitelistEntryRow) -> Result<Self, Self::Error> {
        Ok(Self {
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            locker: *source.locker,
            program_id: *source.program_id,
            owner: *source.owner,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::locker_whitelist_entry::LockerWhitelistEntry
{
    fn table() -> &'static str {
        "locker_whitelist_entry_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "bump",
            "locker",
            "program_id",
            "owner",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for LockerWhitelistEntryRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO locker_whitelist_entry_account (
                "bump",
                "locker",
                "program_id",
                "owner",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            )"#,
        )
        .bind(self.bump.clone())
        .bind(self.locker.clone())
        .bind(self.program_id.clone())
        .bind(self.owner.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for LockerWhitelistEntryRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO locker_whitelist_entry_account (
                "bump",
                "locker",
                "program_id",
                "owner",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "bump" = EXCLUDED."bump",
                "locker" = EXCLUDED."locker",
                "program_id" = EXCLUDED."program_id",
                "owner" = EXCLUDED."owner",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.bump.clone())
        .bind(self.locker.clone())
        .bind(self.program_id.clone())
        .bind(self.owner.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for LockerWhitelistEntryRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM locker_whitelist_entry_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for LockerWhitelistEntryRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM locker_whitelist_entry_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct LockerWhitelistEntryMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for LockerWhitelistEntryMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS locker_whitelist_entry_account (
                -- Account data
                "bump" INT2 NOT NULL,
                "locker" BYTEA NOT NULL,
                "program_id" BYTEA NOT NULL,
                "owner" BYTEA NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS locker_whitelist_entry_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
