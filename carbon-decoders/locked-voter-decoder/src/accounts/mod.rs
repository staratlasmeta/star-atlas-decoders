//! This code was AUTOGENERATED using the Codama library.
use crate::LockedVoterDecoder;
use crate::PROGRAM_ID;

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod escrow;
pub mod locker;
pub mod locker_whitelist_entry;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum LockedVoterAccount {
    Escrow(Box<escrow::Escrow>),
    Locker(Box<locker::Locker>),
    LockerWhitelistEntry(Box<locker_whitelist_entry::LockerWhitelistEntry>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for LockedVoterDecoder {
    type AccountType = LockedVoterAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = locker::Locker::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: LockedVoterAccount::Locker(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = locker_whitelist_entry::LockerWhitelistEntry::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: LockedVoterAccount::LockerWhitelistEntry(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = escrow::Escrow::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: LockedVoterAccount::Escrow(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
