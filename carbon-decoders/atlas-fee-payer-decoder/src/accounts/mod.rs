//! This code was AUTOGENERATED using the Codama library.
use crate::AtlasFeePayerDecoder;
use crate::PROGRAM_ID;

pub mod fee_payer;
pub mod fee_payer_rates;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum AtlasFeePayerAccount {
    FeePayer(Box<fee_payer::FeePayer>),
    FeePayerRates(Box<fee_payer_rates::FeePayerRates>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for AtlasFeePayerDecoder {
    type AccountType = AtlasFeePayerAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = fee_payer::FeePayer::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: AtlasFeePayerAccount::FeePayer(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = fee_payer_rates::FeePayerRates::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: AtlasFeePayerAccount::FeePayerRates(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
