//! This code was AUTOGENERATED using the Codama library.
pub mod fee_payer_rates_row;
pub mod fee_payer_row;

pub use self::fee_payer_rates_row::*;
pub use self::fee_payer_row::*;

use super::AtlasFeePayerAccount;

pub struct AtlasFeePayerAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for AtlasFeePayerAccountsMigration {
    fn app(&self) -> &str {
        "atlas-fee-payer"
    }

    fn name(&self) -> &str {
        "atlas_fee_payer_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(FeePayerMigrationOperation),
            Box::new(FeePayerRatesMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct AtlasFeePayerAccountWithMetadata(
    pub AtlasFeePayerAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(AtlasFeePayerAccount, carbon_core::account::AccountMetadata)>
    for AtlasFeePayerAccountWithMetadata
{
    fn from(value: (AtlasFeePayerAccount, carbon_core::account::AccountMetadata)) -> Self {
        AtlasFeePayerAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AtlasFeePayerAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let AtlasFeePayerAccountWithMetadata(account, metadata) = self;

        match account {
            AtlasFeePayerAccount::FeePayer(account) => {
                let row =
                    fee_payer_row::FeePayerRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            AtlasFeePayerAccount::FeePayerRates(account) => {
                let row = fee_payer_rates_row::FeePayerRatesRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AtlasFeePayerAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let AtlasFeePayerAccountWithMetadata(account, metadata) = self;
        match account {
            AtlasFeePayerAccount::FeePayer(account) => {
                let row =
                    fee_payer_row::FeePayerRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            AtlasFeePayerAccount::FeePayerRates(account) => {
                let row = fee_payer_rates_row::FeePayerRatesRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
