//! This code was AUTOGENERATED using the Codama library.
use crate::AtlasFeePayerDecoder;
use crate::PROGRAM_ID;

pub mod close_fee_payer;
pub mod create_fee_payer;
pub mod create_fee_payer_rates;
pub mod post_transaction;
pub mod post_transaction_no_vault;
pub mod pre_transaction;
pub mod set_fee_payer_rates;

pub use self::close_fee_payer::*;
pub use self::create_fee_payer::*;
pub use self::create_fee_payer_rates::*;
pub use self::post_transaction::*;
pub use self::post_transaction_no_vault::*;
pub use self::pre_transaction::*;
pub use self::set_fee_payer_rates::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(
    feature = "serde",
    derive(carbon_core::InstructionType, serde::Serialize, serde::Deserialize)
)]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum AtlasFeePayerInstruction {
    CloseFeePayer(CloseFeePayer),
    CreateFeePayer(CreateFeePayer),
    CreateFeePayerRates(CreateFeePayerRates),
    PostTransaction(PostTransaction),
    PostTransactionNoVault(PostTransactionNoVault),
    PreTransaction(PreTransaction),
    SetFeePayerRates(SetFeePayerRates),
}

impl carbon_core::instruction::InstructionDecoder<'_> for AtlasFeePayerDecoder {
    type InstructionType = AtlasFeePayerInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = close_fee_payer::CloseFeePayer::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasFeePayerInstruction::CloseFeePayer(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_fee_payer::CreateFeePayer::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasFeePayerInstruction::CreateFeePayer(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_fee_payer_rates::CreateFeePayerRates::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasFeePayerInstruction::CreateFeePayerRates(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = post_transaction::PostTransaction::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasFeePayerInstruction::PostTransaction(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = post_transaction_no_vault::PostTransactionNoVault::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasFeePayerInstruction::PostTransactionNoVault(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = pre_transaction::PreTransaction::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasFeePayerInstruction::PreTransaction(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = set_fee_payer_rates::SetFeePayerRates::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: AtlasFeePayerInstruction::SetFeePayerRates(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
