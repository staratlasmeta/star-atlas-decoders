//! This code was AUTOGENERATED using the Codama library.
use crate::CrewDecoder;
use crate::PROGRAM_ID;

pub mod mint_crew_member;
pub mod redeem_crew_pack;
pub mod register_crew_config;
pub mod register_pack_tiers;
pub mod register_pack_type;
pub mod register_sft_redemption;
pub mod update_pack_tiers;

pub use self::mint_crew_member::*;
pub use self::redeem_crew_pack::*;
pub use self::register_crew_config::*;
pub use self::register_pack_tiers::*;
pub use self::register_pack_type::*;
pub use self::register_sft_redemption::*;
pub use self::update_pack_tiers::*;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[cfg_attr(
    feature = "serde",
    derive(carbon_core::InstructionType, serde::Serialize, serde::Deserialize)
)]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum CrewInstruction {
    MintCrewMember(MintCrewMember),
    RedeemCrewPack(RedeemCrewPack),
    RegisterCrewConfig(RegisterCrewConfig),
    RegisterPackTiers(RegisterPackTiers),
    RegisterPackType(RegisterPackType),
    RegisterSftRedemption(RegisterSftRedemption),
    UpdatePackTiers(UpdatePackTiers),
}

impl carbon_core::instruction::InstructionDecoder<'_> for CrewDecoder {
    type InstructionType = CrewInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = mint_crew_member::MintCrewMember::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CrewInstruction::MintCrewMember(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = redeem_crew_pack::RedeemCrewPack::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CrewInstruction::RedeemCrewPack(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_crew_config::RegisterCrewConfig::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CrewInstruction::RegisterCrewConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_pack_tiers::RegisterPackTiers::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CrewInstruction::RegisterPackTiers(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_pack_type::RegisterPackType::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CrewInstruction::RegisterPackType(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_sft_redemption::RegisterSftRedemption::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CrewInstruction::RegisterSftRedemption(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_pack_tiers::UpdatePackTiers::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: CrewInstruction::UpdatePackTiers(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
