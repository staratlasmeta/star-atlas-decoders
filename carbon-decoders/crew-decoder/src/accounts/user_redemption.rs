//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// `UserRedemption` is a data structure used for handling the redemption of cNFTs by users in the Crew program.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct UserRedemption {
    /// The data version
    pub version: u8,
    /// The bump seed
    pub bump: u8,
    /// The associated crew config account
    pub crew_config: Pubkey,
    /// The pubkey used to derive the address of the UserRedemption account.
    pub seed_pubkey: Pubkey,
    /// The account that recieves the minted cNFTs
    pub owner: Pubkey,
    /// The number of cNFTs that had already been allocated when this account was created. All
    /// cNFTs minted from this account calculate their mint number (i.e. NFT #0, #100, etc.) from this offset.
    pub mint_offset: u32,
    /// Total number of CNFTs this account is responsible for.
    pub amount: u32,
    /// The number of `cNFTs` that have been minted.
    pub number_minted: u32,
    /// The pack type that is being redeemed.
    pub pack_type: Pubkey,
    /// The user provided hash used to generate the pseudo-random data.
    pub user_seed: [u8; 32],
    /// The system hash used to generate the pseudo-random data.
    pub server_hash: [u8; 32],
    /// Optional sage profile key for minting directly to sage
    pub sage_profile: Pubkey,
}

impl UserRedemption {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[64, 166, 80, 6, 78, 150, 143, 249] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
