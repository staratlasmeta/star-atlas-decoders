//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U32;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct SftRedemptionRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub bump: U8,
    pub pack_type: Pubkey,
    pub sft_mint: Pubkey,
    pub crew_config: Pubkey,
    pub redemption_amount: U32,
}

impl SftRedemptionRow {
    pub fn from_parts(
        source: crate::accounts::sft_redemption::SftRedemption,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            bump: source.bump.into(),
            pack_type: source.pack_type.into(),
            sft_mint: source.sft_mint.into(),
            crew_config: source.crew_config.into(),
            redemption_amount: source.redemption_amount.into(),
        }
    }
}

impl TryFrom<SftRedemptionRow> for crate::accounts::sft_redemption::SftRedemption {
    type Error = carbon_core::error::Error;
    fn try_from(source: SftRedemptionRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            pack_type: *source.pack_type,
            sft_mint: *source.sft_mint,
            crew_config: *source.crew_config,
            redemption_amount: source.redemption_amount.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::sft_redemption::SftRedemption {
    fn table() -> &'static str {
        "sft_redemption_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "bump",
            "pack_type",
            "sft_mint",
            "crew_config",
            "redemption_amount",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for SftRedemptionRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO sft_redemption_account (
                "version",
                "bump",
                "pack_type",
                "sft_mint",
                "crew_config",
                "redemption_amount",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.bump.clone())
        .bind(self.pack_type.clone())
        .bind(self.sft_mint.clone())
        .bind(self.crew_config.clone())
        .bind(self.redemption_amount.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for SftRedemptionRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO sft_redemption_account (
                "version",
                "bump",
                "pack_type",
                "sft_mint",
                "crew_config",
                "redemption_amount",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "bump" = EXCLUDED."bump",
                "pack_type" = EXCLUDED."pack_type",
                "sft_mint" = EXCLUDED."sft_mint",
                "crew_config" = EXCLUDED."crew_config",
                "redemption_amount" = EXCLUDED."redemption_amount",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.bump.clone())
        .bind(self.pack_type.clone())
        .bind(self.sft_mint.clone())
        .bind(self.crew_config.clone())
        .bind(self.redemption_amount.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for SftRedemptionRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM sft_redemption_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for SftRedemptionRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM sft_redemption_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct SftRedemptionMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for SftRedemptionMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS sft_redemption_account (
                -- Account data
                "version" INT2 NOT NULL,
                "bump" INT2 NOT NULL,
                "pack_type" BYTEA NOT NULL,
                "sft_mint" BYTEA NOT NULL,
                "crew_config" BYTEA NOT NULL,
                "redemption_amount" INT8 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS sft_redemption_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
