//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// `PackTiers` is a data structure used for defining the odds of rolling each rarity of card in a pack.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct PackTiers {
    /// The data version
    pub version: u8,
    pub crew_config: Pubkey,
    /// The pubkey used as a PDA seed
    pub seed_pubkey: Pubkey,
    /// The tier of the pack
    pub tier: u8,
    /// The bump seed
    pub bump: u8,
    /// The weight for rolling a common card
    pub common: u32,
    /// The weight for rolling an uncommon card
    pub uncommon: u32,
    /// The weight for rolling a rare card
    pub rare: u32,
    /// The weight for rolling an epic card
    pub epic: u32,
    /// The weight for rolling a legendary card
    pub legendary: u32,
    /// The weight for rolling an anomaly card
    pub anomaly: u32,
}

impl PackTiers {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[169, 103, 200, 158, 52, 252, 24, 43] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
