//! This code was AUTOGENERATED using the Codama library.
use crate::types::CrewCreator;
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// `CrewConfig` is the main auth account that manages access to crew operations

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CrewConfig {
    /// The data version
    pub version: u8,
    /// Used as random bytes for the seed to allow for multiple CrewConfig accounts
    pub seed_pubkey: Pubkey,
    /// The bump seed
    pub bump: u8,
    /// auth profile
    pub profile: Pubkey,
    /// The name prefix
    pub name_prefix: [u8; 32],
    /// The length of the name prefix
    pub name_prefix_len: u8,
    /// The symbol
    pub symbol: [u8; 10],
    /// The length of the symbol
    pub symbol_len: u8,
    /// The uri prefix
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub uri_prefix: [u8; 40],
    /// The length of the uri prefix
    pub uri_prefix_len: u8,
    /// Royalty rate for the collection
    pub seller_fee_basis_points: u16,
    /// The collection the crew belongs to
    pub collection_mint: Pubkey,
    /// The creators who will split any royalties
    pub creators: [CrewCreator; 4],
    /// The number of creators
    pub creator_count: u8,
    /// Total crew minted
    pub total_minted: u32,
    /// Total crew allocated to user redemption accounts
    pub total_allocated: u32,
}

impl CrewConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[254, 50, 98, 124, 166, 241, 196, 174] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
