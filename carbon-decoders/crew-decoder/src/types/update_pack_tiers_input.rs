//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// `UpdatePackTiersInput` is a data structure used for handling the input parameters
/// for the `update_pack_tiers` function in the Crew program.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdatePackTiersInput {
    /// the index of the authorizing key in the crew permissions profile
    pub key_index: u16,
    /// The weight for receiving a common card
    pub common: Option<u32>,
    /// The weight for receiving an uncommon card
    pub uncommon: Option<u32>,
    /// The weight for receiving a rare card
    pub rare: Option<u32>,
    /// The weight for receiving an epic card
    pub epic: Option<u32>,
    /// The weight for receiving a legendary card
    pub legendary: Option<u32>,
    /// The weight for receiving an anomaly card
    pub anomaly: Option<u32>,
}
