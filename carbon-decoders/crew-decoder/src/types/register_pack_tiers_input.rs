//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;
/// `RegisterPackTiersInput` is a data structure used for handling the input parameters
/// for the `register_pack_tiers` function in the Crew program.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct RegisterPackTiersInput {
    /// the index of the authorizing key in the crew permissions profile
    pub key_index: u16,
    /// The seed pubkey for the pack tiers
    pub seed_pubkey: Pubkey,
    /// The tier of the pack to be registered
    pub pack_tier: u8,
    /// The weight for receiving a common card
    pub common: u32,
    /// The weight for receiving an uncommon card
    pub uncommon: u32,
    /// The weight for receiving a rare card
    pub rare: u32,
    /// The weight for receiving an epic card
    pub epic: u32,
    /// The weight for receiving a legendary card
    pub legendary: u32,
    /// The weight for receiving an anomaly card
    pub anomaly: u32,
}
