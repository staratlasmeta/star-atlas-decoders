//! This code was AUTOGENERATED using the Codama library.
use crate::types::CrewCreatorUnpacked;
use carbon_core::borsh;
use solana_pubkey::Pubkey;
/// Arguments for the `register_crew_config` instruction.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RegisterCrewConfigArgs {
    /// The name prefix to use for all minted crew
    pub name_prefix: String,
    /// The symbol to use for all minted crew
    pub symbol: String,
    /// The URI prefix to use for all minted crew
    pub uri_prefix: String,
    /// The fee basis points to charge for selling crew
    pub seller_fee_basis_points: u16,
    /// The mint that will be used for all crew
    pub collection: Pubkey,
    /// The creators of the crew and their share of the royalties
    pub creators: Vec<CrewCreatorUnpacked>,
}
