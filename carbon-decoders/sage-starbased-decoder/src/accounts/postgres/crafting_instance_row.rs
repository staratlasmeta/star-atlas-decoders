//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U16;
use carbon_core::postgres::primitives::U64;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct CraftingInstanceRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub seq_id: U16,
    pub authority: Pubkey,
    pub crafting_process: Pubkey,
    pub instance_type: U8,
    pub num_crew: U64,
    pub bump: U8,
}

impl CraftingInstanceRow {
    pub fn from_parts(
        source: crate::accounts::crafting_instance::CraftingInstance,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            seq_id: source.seq_id.into(),
            authority: source.authority.into(),
            crafting_process: source.crafting_process.into(),
            instance_type: source.instance_type.into(),
            num_crew: source.num_crew.into(),
            bump: source.bump.into(),
        }
    }
}

impl TryFrom<CraftingInstanceRow> for crate::accounts::crafting_instance::CraftingInstance {
    type Error = carbon_core::error::Error;
    fn try_from(source: CraftingInstanceRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            seq_id: source.seq_id.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            authority: *source.authority,
            crafting_process: *source.crafting_process,
            instance_type: source.instance_type.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            num_crew: *source.num_crew,
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::crafting_instance::CraftingInstance
{
    fn table() -> &'static str {
        "crafting_instance_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "seq_id",
            "authority",
            "crafting_process",
            "instance_type",
            "num_crew",
            "bump",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CraftingInstanceRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO crafting_instance_account (
                "version",
                "seq_id",
                "authority",
                "crafting_process",
                "instance_type",
                "num_crew",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.seq_id.clone())
        .bind(self.authority.clone())
        .bind(self.crafting_process.clone())
        .bind(self.instance_type.clone())
        .bind(self.num_crew.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CraftingInstanceRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO crafting_instance_account (
                "version",
                "seq_id",
                "authority",
                "crafting_process",
                "instance_type",
                "num_crew",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "seq_id" = EXCLUDED."seq_id",
                "authority" = EXCLUDED."authority",
                "crafting_process" = EXCLUDED."crafting_process",
                "instance_type" = EXCLUDED."instance_type",
                "num_crew" = EXCLUDED."num_crew",
                "bump" = EXCLUDED."bump",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.seq_id.clone())
        .bind(self.authority.clone())
        .bind(self.crafting_process.clone())
        .bind(self.instance_type.clone())
        .bind(self.num_crew.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for CraftingInstanceRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM crafting_instance_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for CraftingInstanceRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM crafting_instance_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct CraftingInstanceMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for CraftingInstanceMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS crafting_instance_account (
                -- Account data
                "version" INT2 NOT NULL,
                "seq_id" INT4 NOT NULL,
                "authority" BYTEA NOT NULL,
                "crafting_process" BYTEA NOT NULL,
                "instance_type" INT2 NOT NULL,
                "num_crew" NUMERIC(20) NOT NULL,
                "bump" INT2 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS crafting_instance_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
