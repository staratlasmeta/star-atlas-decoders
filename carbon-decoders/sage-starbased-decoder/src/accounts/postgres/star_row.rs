//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U64;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct StarRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub name: Vec<u8>,
    pub game_id: Pubkey,
    pub sector: Vec<i64>,
    pub size: U64,
    pub sub_coordinates: Vec<i64>,
    pub star_type: U8,
}

impl StarRow {
    pub fn from_parts(source: crate::accounts::star::Star, metadata: AccountMetadata) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            name: source.name.to_vec(),
            game_id: source.game_id.into(),
            sector: source
                .sector
                .into_iter()
                .map(|element| element.into())
                .collect(),
            size: source.size.into(),
            sub_coordinates: source
                .sub_coordinates
                .into_iter()
                .map(|element| element.into())
                .collect(),
            star_type: source.star_type.into(),
        }
    }
}

impl TryFrom<StarRow> for crate::accounts::star::Star {
    type Error = carbon_core::error::Error;
    fn try_from(source: StarRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            name: source.name.as_slice().try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert padding from postgres primitive: expected 64 bytes"
                        .to_string(),
                )
            })?,
            game_id: *source.game_id,
            sector: source
                .sector
                .into_iter()
                .map(|element| Ok(element.into()))
                .collect::<Result<Vec<_>, _>>()?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
            size: *source.size,
            sub_coordinates: source
                .sub_coordinates
                .into_iter()
                .map(|element| Ok(element.into()))
                .collect::<Result<Vec<_>, _>>()?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
            star_type: source.star_type.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::star::Star {
    fn table() -> &'static str {
        "star_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "name",
            "game_id",
            "sector",
            "size",
            "sub_coordinates",
            "star_type",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for StarRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO star_account (
                "version",
                "name",
                "game_id",
                "sector",
                "size",
                "sub_coordinates",
                "star_type",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.name.clone())
        .bind(self.game_id.clone())
        .bind(self.sector.clone())
        .bind(self.size.clone())
        .bind(self.sub_coordinates.clone())
        .bind(self.star_type.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for StarRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO star_account (
                "version",
                "name",
                "game_id",
                "sector",
                "size",
                "sub_coordinates",
                "star_type",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "name" = EXCLUDED."name",
                "game_id" = EXCLUDED."game_id",
                "sector" = EXCLUDED."sector",
                "size" = EXCLUDED."size",
                "sub_coordinates" = EXCLUDED."sub_coordinates",
                "star_type" = EXCLUDED."star_type",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.name.clone())
        .bind(self.game_id.clone())
        .bind(self.sector.clone())
        .bind(self.size.clone())
        .bind(self.sub_coordinates.clone())
        .bind(self.star_type.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for StarRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM star_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for StarRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM star_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct StarMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for StarMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS star_account (
                -- Account data
                "version" INT2 NOT NULL,
                "name" BYTEA NOT NULL,
                "game_id" BYTEA NOT NULL,
                "sector" INT8[] NOT NULL,
                "size" NUMERIC(20) NOT NULL,
                "sub_coordinates" INT8[] NOT NULL,
                "star_type" INT2 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS star_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
