//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U8;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct DisbandedFleetRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub version: U8,
    pub game_id: Pubkey,
    pub owner_profile: Pubkey,
    pub starbase: Pubkey,
    pub fleet_label: Vec<u8>,
    pub fleet_ships: Pubkey,
    pub bump: U8,
}

impl DisbandedFleetRow {
    pub fn from_parts(
        source: crate::accounts::disbanded_fleet::DisbandedFleet,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            version: source.version.into(),
            game_id: source.game_id.into(),
            owner_profile: source.owner_profile.into(),
            starbase: source.starbase.into(),
            fleet_label: source.fleet_label.to_vec(),
            fleet_ships: source.fleet_ships.into(),
            bump: source.bump.into(),
        }
    }
}

impl TryFrom<DisbandedFleetRow> for crate::accounts::disbanded_fleet::DisbandedFleet {
    type Error = carbon_core::error::Error;
    fn try_from(source: DisbandedFleetRow) -> Result<Self, Self::Error> {
        Ok(Self {
            version: source.version.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            game_id: *source.game_id,
            owner_profile: *source.owner_profile,
            starbase: *source.starbase,
            fleet_label: source.fleet_label.as_slice().try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert padding from postgres primitive: expected 32 bytes"
                        .to_string(),
                )
            })?,
            fleet_ships: *source.fleet_ships,
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::disbanded_fleet::DisbandedFleet {
    fn table() -> &'static str {
        "disbanded_fleet_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "version",
            "game_id",
            "owner_profile",
            "starbase",
            "fleet_label",
            "fleet_ships",
            "bump",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for DisbandedFleetRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO disbanded_fleet_account (
                "version",
                "game_id",
                "owner_profile",
                "starbase",
                "fleet_label",
                "fleet_ships",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.version.clone())
        .bind(self.game_id.clone())
        .bind(self.owner_profile.clone())
        .bind(self.starbase.clone())
        .bind(self.fleet_label.clone())
        .bind(self.fleet_ships.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for DisbandedFleetRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO disbanded_fleet_account (
                "version",
                "game_id",
                "owner_profile",
                "starbase",
                "fleet_label",
                "fleet_ships",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "version" = EXCLUDED."version",
                "game_id" = EXCLUDED."game_id",
                "owner_profile" = EXCLUDED."owner_profile",
                "starbase" = EXCLUDED."starbase",
                "fleet_label" = EXCLUDED."fleet_label",
                "fleet_ships" = EXCLUDED."fleet_ships",
                "bump" = EXCLUDED."bump",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.version.clone())
        .bind(self.game_id.clone())
        .bind(self.owner_profile.clone())
        .bind(self.starbase.clone())
        .bind(self.fleet_label.clone())
        .bind(self.fleet_ships.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for DisbandedFleetRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM disbanded_fleet_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for DisbandedFleetRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM disbanded_fleet_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct DisbandedFleetMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for DisbandedFleetMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS disbanded_fleet_account (
                -- Account data
                "version" INT2 NOT NULL,
                "game_id" BYTEA NOT NULL,
                "owner_profile" BYTEA NOT NULL,
                "starbase" BYTEA NOT NULL,
                "fleet_label" BYTEA NOT NULL,
                "fleet_ships" BYTEA NOT NULL,
                "bump" INT2 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS disbanded_fleet_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
