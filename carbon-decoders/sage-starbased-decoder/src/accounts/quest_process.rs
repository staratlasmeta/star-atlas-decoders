//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

/// On-chain record of a crew quest being processed at a starbase.
///
/// Field-for-field the `StarbaseUpgradeProcess` shape minus the upgrade-only fields and
/// plus the crew payout `reward_perk_points`. The typed owner keys (`KeyFor<Profile>` /
/// `KeyFor<StarbasePlayer>`) flatten to `Pubkey` and the `StarbaseSeqId` newtype flattens
/// to `u64` in the generated IDL.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct QuestProcess {
    /// The data version of this account.
    pub version: u8,
    /// The `Profile` key (the quest owner).
    pub profile: Pubkey,
    /// The owning `StarbasePlayer` (the live-crew-counter choke-point).
    pub starbase_player: Pubkey,
    /// The starbase sequence id this quest is anchored to (staleness/refund check).
    pub starbase_seq_id: u64,
    /// Number of crew committed to the quest.
    pub num_crew: u16,
    /// Perk points awarded to the roster on completion (the crew-specific payout).
    pub reward_perk_points: u8,
    /// Tail pad so `start_time` / `end_time` stay byte-flush (no semantic meaning).
    pub _pad: [u8; 5],
    /// The quest start timestamp.
    pub start_time: i64,
    /// The quest end timestamp (crew become free / payout claimable at/after this).
    pub end_time: i64,
}

impl QuestProcess {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[204, 80, 96, 46, 233, 179, 161, 47] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
