//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// One equipped gear reference (8 bytes). Sentinel, not `Option`: `gear_id == 0` means an
/// empty slot. Byte-exact to the on-chain `GearRef` Pod (`crew_record`/`components.rs`):
/// `gear_id(2) + tier(1) + _pad(1) + durability(2) + _pad2(2) = 8`.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct GearRef {
    /// On-chain gear definition id. `0` => empty slot (the sentinel).
    pub gear_id: u16,
    /// Gear tier (recipe tier).
    pub tier: u8,
    /// Padding (`components.rs` `GearRef._pad`).
    pub pad: u8,
    /// Remaining durability (sink-eroded).
    pub durability: u16,
    /// Padding to the 8-byte slot (`components.rs` `GearRef._pad2`).
    pub pad2: [u8; 2],
}
