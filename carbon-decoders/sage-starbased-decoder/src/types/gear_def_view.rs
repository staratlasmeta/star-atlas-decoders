//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// A decoded gear-definition row — the `Game`-config view of the gear `Recipe` that
/// `EquipGear` resolves (`instructions/gear.rs:49-54`). The effect-family the item folds
/// into; magnitude is DERIVED from tier, never read from here. Borsh shape:
/// `gear_id(2) + slot(1)` (`slot` is a raw `SlotFamily` discriminant byte).

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct GearDefView {
    /// On-chain gear id (`CargoId`; `0` is the reserved sentinel).
    pub gear_id: u16,
    /// The effect-family this gear folds into (raw `SlotFamily` byte; index into
    /// `gear_scalar[13]`).
    pub slot: u8,
}
