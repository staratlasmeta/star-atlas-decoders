//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// One perk-catalogue row authored on-chain via `SetPerkDefinitions` (the wire/IDL `PerkDef`
/// row). The borsh shape of the on-chain `PerkDefInput` (`instructions/admin.rs:98-115`):
/// `id(2) + slot_family(1) + trigger(1) + mag_per_tier(2) + aptitude_gate(1) +
/// rarity_floor(1) + personality_trait_idx(1) + personality_op(1) +
/// personality_threshold_q8(1)`. `slot_family` / `trigger` / `aptitude_gate` / `rarity_floor`
/// are raw discriminant bytes; `op == 0xFF` => no personality gate.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct PerkDefInput {
    /// Stable catalogue id (`PerkSlot.perk_id`; `0` reserved = empty).
    pub id: u16,
    /// The no-op-default slot family this perk's delta lands on (raw `SlotFamily` byte).
    pub slot_family: u8,
    /// The trigger gating the perk (raw `Trigger` byte).
    pub trigger: u8,
    /// Magnitude per tier, as the `i16` q-format the summary stores.
    pub mag_per_tier: i16,
    /// The aptitude whose major/anomalous tag gates the offer (raw `Aptitude` byte).
    pub aptitude_gate: u8,
    /// The rarity floor (raw `Rarity` byte).
    pub rarity_floor: u8,
    /// Optional personality gate: Big-Five trait index (`op == 0xFF` => none).
    pub personality_trait_idx: u8,
    /// Optional personality gate operator (`0` => `>= threshold`; `1` => `<= threshold`;
    /// `0xFF` => no gate).
    pub personality_op: u8,
    /// Optional personality gate q0.8 threshold.
    pub personality_threshold_q8: u8,
}
