//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// One crew to individuate via `MigrateCrewMember`: its DAS-attested rarity + the
/// rarity-derived base-stat seed. The borsh wire shape of the on-chain `CrewMigrateInput`
/// (`instructions/migrate.rs:60-71`): `crew_asset_id(32) + rarity(1) + base_stat(2)`.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct CrewMigrateInput {
    /// The crew cNFT asset id (Bubblegum leaf) — the stable per-crew identity.
    pub crew_asset_id: [u8; 32],
    /// DAS-attested rarity discriminant (`0..=5`).
    pub rarity: u8,
    /// Rarity-derived base aptitude magnitude seeding every aptitude.
    pub base_stat: u16,
}
