//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// The 9 crew aptitudes. The variant order is LOAD-BEARING: it indexes `CrewStats.apt[9]`
/// and `RosterSummary.sum_apt[9]`. Stored on-chain as a raw `u8` discriminant (perk/gear
/// definition rows carry it as `aptitude_gate`).

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub enum Aptitude {
    Command,
    Engineering,
    Fitness,
    Flight,
    Hospitality,
    Medical,
    Operator,
    Science,
    Scanning,
}
