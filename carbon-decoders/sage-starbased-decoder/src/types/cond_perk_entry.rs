//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// One conditional perk pre-loaded into `RosterSummary.perk_cond[8]` (8 bytes). Byte-exact
/// to the on-chain `CondPerkEntry` Pod (`components.rs`): `perk_id(2) + slot_family(1) +
/// trigger(1) + mag(2) + tier(1) + _pad(1) = 8`. `mag` is an `i16` q-format delta;
/// `slot_family` / `trigger` are raw discriminant bytes.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct CondPerkEntry {
    /// On-chain perk definition id (`0` => unused table slot).
    pub perk_id: u16,
    /// Raw `SlotFamily` discriminant the perk modifies.
    pub slot_family: u8,
    /// Raw `Trigger` discriminant gating the perk.
    pub trigger: u8,
    /// Magnitude as an `i16` q-format delta.
    pub mag: i16,
    /// Perk tier.
    pub tier: u8,
    /// Padding to the 8-byte slot (`components.rs` `CondPerkEntry._pad`).
    pub pad: u8,
}
