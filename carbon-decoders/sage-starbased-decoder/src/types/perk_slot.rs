//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// One unlocked perk slot (4 bytes). Sentinel: `perk_id == 0` means empty. Byte-exact to
/// the on-chain `PerkSlot` Pod (`components.rs`): `perk_id(2) + tier(1) + _pad(1) = 4`.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct PerkSlot {
    /// On-chain perk definition id. `0` => empty slot (the sentinel).
    pub perk_id: u16,
    /// Perk tier.
    pub tier: u8,
    /// Padding to the 4-byte slot (`components.rs` `PerkSlot._pad`).
    pub pad: u8,
}
