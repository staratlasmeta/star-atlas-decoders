//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// 9 aptitude magnitudes + 3 independent tag bitmasks (24 bytes). Byte-exact to the
/// on-chain `CrewStats` Pod (`components.rs`): 9 aptitude `u16` (18) + 3 mask `u16` (6) =
/// 24. The `apt[9]` index order matches the `Aptitude` discriminants.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct CrewStats {
    /// Per-aptitude magnitude, indexed by `Aptitude`.
    pub apt: [u16; 9],
    /// Bit `i` set => aptitude `i` is tagged `Minor`.
    pub minor_mask: u16,
    /// Bit `i` set => aptitude `i` is tagged `Major`.
    pub major_mask: u16,
    /// Bit `i` set => aptitude `i` is tagged `Anomalous`.
    pub anomalous_mask: u16,
}
