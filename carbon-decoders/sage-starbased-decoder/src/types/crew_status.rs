//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;
/// One crew member's assignment: a 33-byte value `{ tag: u8, target: Pubkey }` (the
/// "FleetState remaining-data" tag+target pattern). Byte-exact to the on-chain `CrewStatus`
/// Pod (`crew_status.rs`): `tag(1) + target(32) = 33`. `tag` is the raw `CrewStatusTag`
/// discriminant; `target` is the assigned `Fleet` key, or all-zero (the `OptionalKeyFor`
/// `NONE` sentinel) when the crew is not `OnFleet`.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct CrewStatus {
    /// Raw `CrewStatusTag` discriminant. Kept a `u8` to preserve the `Pod`-ness of the
    /// enclosing `CrewRecord`.
    pub tag: u8,
    /// The assigned `Fleet` key, or all-zero (`OptionalKeyFor::NONE`) when not `OnFleet`.
    pub target: Pubkey,
}
