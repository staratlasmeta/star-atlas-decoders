//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// The 6 crew rarity tiers (`idx 0..=5` = Common..Anomaly). Stored on-chain as the raw
/// `u8` discriminant in `CrewRecord.rarity`; rarity is counted once via the shared base
/// aptitude curve (the anti-P2W keystone).

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub enum Rarity {
    Common,
    Uncommon,
    Rare,
    Epic,
    Legendary,
    Anomaly,
}
