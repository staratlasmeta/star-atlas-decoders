//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// The 13 modifier slot-families carrying a per-slot power budget. The variant order is
/// LOAD-BEARING: it indexes `RosterSummary.perk_uncond[13]` and `gear_scalar[13]`. Stored
/// on-chain as a raw `u8` discriminant (in `CondPerkEntry.slot_family` and perk/gear rows).

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub enum SlotFamily {
    MiningYield,
    RareFind,
    CraftSpeed,
    CraftYield,
    CraftConcurrency,
    WarpSpeed,
    WarpCooldown,
    FuelBurn,
    ScanBonus,
    ScanPower,
    Respawn,
    CombatAp,
    CrewCount,
}
