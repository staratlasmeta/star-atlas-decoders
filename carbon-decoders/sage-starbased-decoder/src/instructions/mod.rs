//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::SageDecoder;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod activate_game_state;
pub mod add_connection;
pub mod add_crew_to_game;
pub mod add_rental;
pub mod add_ship_escrow;
pub mod add_ship_to_fleet;
pub mod burn_crafting_consumables;
pub mod cancel_crafting_process;
pub mod change_rental;
pub mod claim_crafting_non_consumables;
pub mod claim_crafting_outputs;
pub mod close_crafting_process;
pub mod close_disbanded_fleet;
pub mod close_fleet_cargo_pod_token_account;
pub mod close_player_crew_record;
pub mod close_starbase_cargo_token_account;
pub mod close_upgrade_process;
pub mod complete_starbase_upgrade;
pub mod copy_game_state;
pub mod create_cargo_pod;
pub mod create_certificate_mint;
pub mod create_crafting_process;
pub mod create_fleet;
pub mod create_starbase_upgrade_resource_process;
pub mod deposit_cargo_to_fleet;
pub mod deposit_cargo_to_game;
pub mod deposit_crafting_ingredient;
pub mod deposit_starbase_upkeep_resource;
pub mod deregister_mine_item;
pub mod deregister_progression_config;
pub mod deregister_resource;
pub mod deregister_starbase;
pub mod deregister_survey_data_unit_tracker;
pub mod dev_add_crew_to_game;
pub mod dev_deposit_cargo_to_game;
pub mod disband_fleet;
pub mod disbanded_fleet_to_escrow;
pub mod discover_sector;
pub mod drain_mine_item_bank;
pub mod drain_survey_data_units_bank;
pub mod fleet_state_handler;
pub mod force_disband_fleet;
pub mod force_drop_fleet_cargo;
pub mod idle_to_loading_bay;
pub mod idle_to_respawn;
pub mod init_game;
pub mod init_game_state;
pub mod invalidate_rental;
pub mod invalidate_ship;
pub mod load_fleet_crew;
pub mod loading_bay_to_idle;
pub mod loading_bay_to_respawn;
pub mod mine_asteroid_to_respawn;
pub mod mint_certificate;
pub mod mint_crew_to_game;
pub mod redeem_certificate;
pub mod register_mine_item;
pub mod register_planet;
pub mod register_progression_config;
pub mod register_resource;
pub mod register_sage_crew_config;
pub mod register_sage_player_profile;
pub mod register_sage_point_modifier;
pub mod register_sector;
pub mod register_ship;
pub mod register_star;
pub mod register_starbase;
pub mod register_starbase_player;
pub mod register_survey_data_unit_tracker;
pub mod remove_cargo_pod;
pub mod remove_connection;
pub mod remove_crew_from_game;
pub mod remove_invalid_ship_escrow;
pub mod remove_ship_escrow;
pub mod respawn_to_loading_bay;
pub mod scan_for_survey_data_units;
pub mod set_next_ship;
pub mod start_crafting_process;
pub mod start_mining_asteroid;
pub mod start_starbase_upgrade;
pub mod start_subwarp;
pub mod stop_crafting_process;
pub mod stop_mining_asteroid;
pub mod stop_subwarp;
pub mod submit_starbase_upgrade_resource;
pub mod sync_starbase_player;
pub mod sync_starbase_upgrade_ingredients;
pub mod transfer_cargo_at_starbase;
pub mod transfer_cargo_within_fleet;
pub mod unload_fleet_crew;
pub mod update_game;
pub mod update_game_state;
pub mod update_mine_item;
pub mod update_planet;
pub mod update_progression_config;
pub mod update_resource;
pub mod update_ship;
pub mod update_ship_escrow;
pub mod update_ship_in_fleet;
pub mod update_star;
pub mod update_starbase;
pub mod update_survey_data_unit_tracker;
pub mod warp_lane;
pub mod warp_to_coordinate;
pub mod withdraw_cargo_from_fleet;
pub mod withdraw_cargo_from_game;
pub mod withdraw_crafting_ingredient;

pub use self::activate_game_state::*;
pub use self::add_connection::*;
pub use self::add_crew_to_game::*;
pub use self::add_rental::*;
pub use self::add_ship_escrow::*;
pub use self::add_ship_to_fleet::*;
pub use self::burn_crafting_consumables::*;
pub use self::cancel_crafting_process::*;
pub use self::change_rental::*;
pub use self::claim_crafting_non_consumables::*;
pub use self::claim_crafting_outputs::*;
pub use self::close_crafting_process::*;
pub use self::close_disbanded_fleet::*;
pub use self::close_fleet_cargo_pod_token_account::*;
pub use self::close_player_crew_record::*;
pub use self::close_starbase_cargo_token_account::*;
pub use self::close_upgrade_process::*;
pub use self::complete_starbase_upgrade::*;
pub use self::copy_game_state::*;
pub use self::create_cargo_pod::*;
pub use self::create_certificate_mint::*;
pub use self::create_crafting_process::*;
pub use self::create_fleet::*;
pub use self::create_starbase_upgrade_resource_process::*;
pub use self::deposit_cargo_to_fleet::*;
pub use self::deposit_cargo_to_game::*;
pub use self::deposit_crafting_ingredient::*;
pub use self::deposit_starbase_upkeep_resource::*;
pub use self::deregister_mine_item::*;
pub use self::deregister_progression_config::*;
pub use self::deregister_resource::*;
pub use self::deregister_starbase::*;
pub use self::deregister_survey_data_unit_tracker::*;
pub use self::dev_add_crew_to_game::*;
pub use self::dev_deposit_cargo_to_game::*;
pub use self::disband_fleet::*;
pub use self::disbanded_fleet_to_escrow::*;
pub use self::discover_sector::*;
pub use self::drain_mine_item_bank::*;
pub use self::drain_survey_data_units_bank::*;
pub use self::fleet_state_handler::*;
pub use self::force_disband_fleet::*;
pub use self::force_drop_fleet_cargo::*;
pub use self::idle_to_loading_bay::*;
pub use self::idle_to_respawn::*;
pub use self::init_game::*;
pub use self::init_game_state::*;
pub use self::invalidate_rental::*;
pub use self::invalidate_ship::*;
pub use self::load_fleet_crew::*;
pub use self::loading_bay_to_idle::*;
pub use self::loading_bay_to_respawn::*;
pub use self::mine_asteroid_to_respawn::*;
pub use self::mint_certificate::*;
pub use self::mint_crew_to_game::*;
pub use self::redeem_certificate::*;
pub use self::register_mine_item::*;
pub use self::register_planet::*;
pub use self::register_progression_config::*;
pub use self::register_resource::*;
pub use self::register_sage_crew_config::*;
pub use self::register_sage_player_profile::*;
pub use self::register_sage_point_modifier::*;
pub use self::register_sector::*;
pub use self::register_ship::*;
pub use self::register_star::*;
pub use self::register_starbase::*;
pub use self::register_starbase_player::*;
pub use self::register_survey_data_unit_tracker::*;
pub use self::remove_cargo_pod::*;
pub use self::remove_connection::*;
pub use self::remove_crew_from_game::*;
pub use self::remove_invalid_ship_escrow::*;
pub use self::remove_ship_escrow::*;
pub use self::respawn_to_loading_bay::*;
pub use self::scan_for_survey_data_units::*;
pub use self::set_next_ship::*;
pub use self::start_crafting_process::*;
pub use self::start_mining_asteroid::*;
pub use self::start_starbase_upgrade::*;
pub use self::start_subwarp::*;
pub use self::stop_crafting_process::*;
pub use self::stop_mining_asteroid::*;
pub use self::stop_subwarp::*;
pub use self::submit_starbase_upgrade_resource::*;
pub use self::sync_starbase_player::*;
pub use self::sync_starbase_upgrade_ingredients::*;
pub use self::transfer_cargo_at_starbase::*;
pub use self::transfer_cargo_within_fleet::*;
pub use self::unload_fleet_crew::*;
pub use self::update_game::*;
pub use self::update_game_state::*;
pub use self::update_mine_item::*;
pub use self::update_planet::*;
pub use self::update_progression_config::*;
pub use self::update_resource::*;
pub use self::update_ship::*;
pub use self::update_ship_escrow::*;
pub use self::update_ship_in_fleet::*;
pub use self::update_star::*;
pub use self::update_starbase::*;
pub use self::update_survey_data_unit_tracker::*;
pub use self::warp_lane::*;
pub use self::warp_to_coordinate::*;
pub use self::withdraw_cargo_from_fleet::*;
pub use self::withdraw_cargo_from_game::*;
pub use self::withdraw_crafting_ingredient::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum SageInstruction {
    ActivateGameState(ActivateGameState),
    AddConnection(AddConnection),
    AddCrewToGame(AddCrewToGame),
    AddRental(AddRental),
    AddShipEscrow(AddShipEscrow),
    AddShipToFleet(AddShipToFleet),
    BurnCraftingConsumables(BurnCraftingConsumables),
    CancelCraftingProcess(CancelCraftingProcess),
    ChangeRental(ChangeRental),
    ClaimCraftingNonConsumables(ClaimCraftingNonConsumables),
    ClaimCraftingOutputs(ClaimCraftingOutputs),
    CloseCraftingProcess(CloseCraftingProcess),
    CloseDisbandedFleet(CloseDisbandedFleet),
    CloseFleetCargoPodTokenAccount(CloseFleetCargoPodTokenAccount),
    ClosePlayerCrewRecord(ClosePlayerCrewRecord),
    CloseStarbaseCargoTokenAccount(CloseStarbaseCargoTokenAccount),
    CloseUpgradeProcess(CloseUpgradeProcess),
    CompleteStarbaseUpgrade(CompleteStarbaseUpgrade),
    CopyGameState(CopyGameState),
    CreateCargoPod(CreateCargoPod),
    CreateCertificateMint(CreateCertificateMint),
    CreateCraftingProcess(CreateCraftingProcess),
    CreateFleet(CreateFleet),
    CreateStarbaseUpgradeResourceProcess(CreateStarbaseUpgradeResourceProcess),
    DepositCargoToFleet(DepositCargoToFleet),
    DepositCargoToGame(DepositCargoToGame),
    DepositCraftingIngredient(DepositCraftingIngredient),
    DepositStarbaseUpkeepResource(DepositStarbaseUpkeepResource),
    DeregisterMineItem(DeregisterMineItem),
    DeregisterProgressionConfig(DeregisterProgressionConfig),
    DeregisterResource(DeregisterResource),
    DeregisterStarbase(DeregisterStarbase),
    DeregisterSurveyDataUnitTracker(DeregisterSurveyDataUnitTracker),
    DevAddCrewToGame(DevAddCrewToGame),
    DevDepositCargoToGame(DevDepositCargoToGame),
    DisbandedFleetToEscrow(DisbandedFleetToEscrow),
    DisbandFleet(DisbandFleet),
    DiscoverSector(DiscoverSector),
    DrainMineItemBank(DrainMineItemBank),
    DrainSurveyDataUnitsBank(DrainSurveyDataUnitsBank),
    FleetStateHandler(FleetStateHandler),
    ForceDisbandFleet(ForceDisbandFleet),
    ForceDropFleetCargo(ForceDropFleetCargo),
    IdleToLoadingBay(IdleToLoadingBay),
    IdleToRespawn(IdleToRespawn),
    InitGame(InitGame),
    InitGameState(InitGameState),
    InvalidateRental(InvalidateRental),
    InvalidateShip(InvalidateShip),
    LoadFleetCrew(LoadFleetCrew),
    LoadingBayToIdle(LoadingBayToIdle),
    LoadingBayToRespawn(LoadingBayToRespawn),
    MineAsteroidToRespawn(MineAsteroidToRespawn),
    MintCertificate(MintCertificate),
    MintCrewToGame(MintCrewToGame),
    RedeemCertificate(RedeemCertificate),
    RegisterMineItem(RegisterMineItem),
    RegisterPlanet(RegisterPlanet),
    RegisterProgressionConfig(RegisterProgressionConfig),
    RegisterResource(RegisterResource),
    RegisterSageCrewConfig(RegisterSageCrewConfig),
    RegisterSagePlayerProfile(RegisterSagePlayerProfile),
    RegisterSagePointModifier(RegisterSagePointModifier),
    RegisterSector(RegisterSector),
    RegisterShip(RegisterShip),
    RegisterStar(RegisterStar),
    RegisterStarbase(RegisterStarbase),
    RegisterStarbasePlayer(RegisterStarbasePlayer),
    RegisterSurveyDataUnitTracker(RegisterSurveyDataUnitTracker),
    RemoveCargoPod(RemoveCargoPod),
    RemoveConnection(RemoveConnection),
    RemoveCrewFromGame(RemoveCrewFromGame),
    RemoveInvalidShipEscrow(RemoveInvalidShipEscrow),
    RemoveShipEscrow(RemoveShipEscrow),
    RespawnToLoadingBay(RespawnToLoadingBay),
    ScanForSurveyDataUnits(ScanForSurveyDataUnits),
    SetNextShip(SetNextShip),
    StartCraftingProcess(StartCraftingProcess),
    StartMiningAsteroid(StartMiningAsteroid),
    StartStarbaseUpgrade(StartStarbaseUpgrade),
    StartSubwarp(StartSubwarp),
    StopCraftingProcess(StopCraftingProcess),
    StopMiningAsteroid(StopMiningAsteroid),
    StopSubwarp(StopSubwarp),
    SubmitStarbaseUpgradeResource(SubmitStarbaseUpgradeResource),
    SyncStarbasePlayer(SyncStarbasePlayer),
    SyncStarbaseUpgradeIngredients(SyncStarbaseUpgradeIngredients),
    TransferCargoAtStarbase(TransferCargoAtStarbase),
    TransferCargoWithinFleet(TransferCargoWithinFleet),
    UnloadFleetCrew(UnloadFleetCrew),
    UpdateGame(UpdateGame),
    UpdateGameState(UpdateGameState),
    UpdateMineItem(UpdateMineItem),
    UpdatePlanet(UpdatePlanet),
    UpdateProgressionConfig(UpdateProgressionConfig),
    UpdateResource(UpdateResource),
    UpdateShip(UpdateShip),
    UpdateShipEscrow(UpdateShipEscrow),
    UpdateShipInFleet(UpdateShipInFleet),
    UpdateStar(UpdateStar),
    UpdateStarbase(UpdateStarbase),
    UpdateSurveyDataUnitTracker(UpdateSurveyDataUnitTracker),
    WarpLane(WarpLane),
    WarpToCoordinate(WarpToCoordinate),
    WithdrawCargoFromFleet(WithdrawCargoFromFleet),
    WithdrawCargoFromGame(WithdrawCargoFromGame),
    WithdrawCraftingIngredient(WithdrawCraftingIngredient),
}

impl carbon_core::instruction::InstructionDecoder<'_> for SageDecoder {
    type InstructionType = SageInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = activate_game_state::ActivateGameState::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ActivateGameState(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = add_connection::AddConnection::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::AddConnection(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = add_crew_to_game::AddCrewToGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::AddCrewToGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = add_rental::AddRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::AddRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = add_ship_escrow::AddShipEscrow::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::AddShipEscrow(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = add_ship_to_fleet::AddShipToFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::AddShipToFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = burn_crafting_consumables::BurnCraftingConsumables::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::BurnCraftingConsumables(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = cancel_crafting_process::CancelCraftingProcess::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CancelCraftingProcess(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = change_rental::ChangeRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ChangeRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                claim_crafting_non_consumables::ClaimCraftingNonConsumables::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ClaimCraftingNonConsumables(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = claim_crafting_outputs::ClaimCraftingOutputs::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ClaimCraftingOutputs(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_crafting_process::CloseCraftingProcess::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CloseCraftingProcess(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_disbanded_fleet::CloseDisbandedFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CloseDisbandedFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                close_fleet_cargo_pod_token_account::CloseFleetCargoPodTokenAccount::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CloseFleetCargoPodTokenAccount(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_player_crew_record::ClosePlayerCrewRecord::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ClosePlayerCrewRecord(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                close_starbase_cargo_token_account::CloseStarbaseCargoTokenAccount::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CloseStarbaseCargoTokenAccount(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = close_upgrade_process::CloseUpgradeProcess::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CloseUpgradeProcess(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = complete_starbase_upgrade::CompleteStarbaseUpgrade::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CompleteStarbaseUpgrade(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = copy_game_state::CopyGameState::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CopyGameState(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_cargo_pod::CreateCargoPod::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CreateCargoPod(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_certificate_mint::CreateCertificateMint::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CreateCertificateMint(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_crafting_process::CreateCraftingProcess::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CreateCraftingProcess(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_fleet::CreateFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CreateFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_starbase_upgrade_resource_process::CreateStarbaseUpgradeResourceProcess::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::CreateStarbaseUpgradeResourceProcess(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = deposit_cargo_to_fleet::DepositCargoToFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DepositCargoToFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = deposit_cargo_to_game::DepositCargoToGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DepositCargoToGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                deposit_crafting_ingredient::DepositCraftingIngredient::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DepositCraftingIngredient(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                deposit_starbase_upkeep_resource::DepositStarbaseUpkeepResource::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DepositStarbaseUpkeepResource(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = deregister_mine_item::DeregisterMineItem::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DeregisterMineItem(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                deregister_progression_config::DeregisterProgressionConfig::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DeregisterProgressionConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = deregister_resource::DeregisterResource::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DeregisterResource(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = deregister_starbase::DeregisterStarbase::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DeregisterStarbase(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                deregister_survey_data_unit_tracker::DeregisterSurveyDataUnitTracker::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DeregisterSurveyDataUnitTracker(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = dev_add_crew_to_game::DevAddCrewToGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DevAddCrewToGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = dev_deposit_cargo_to_game::DevDepositCargoToGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DevDepositCargoToGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = disband_fleet::DisbandFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DisbandFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = disbanded_fleet_to_escrow::DisbandedFleetToEscrow::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DisbandedFleetToEscrow(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = discover_sector::DiscoverSector::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DiscoverSector(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = drain_mine_item_bank::DrainMineItemBank::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DrainMineItemBank(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                drain_survey_data_units_bank::DrainSurveyDataUnitsBank::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::DrainSurveyDataUnitsBank(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = fleet_state_handler::FleetStateHandler::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::FleetStateHandler(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = force_disband_fleet::ForceDisbandFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ForceDisbandFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = force_drop_fleet_cargo::ForceDropFleetCargo::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ForceDropFleetCargo(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = idle_to_loading_bay::IdleToLoadingBay::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::IdleToLoadingBay(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = idle_to_respawn::IdleToRespawn::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::IdleToRespawn(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = init_game::InitGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::InitGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = init_game_state::InitGameState::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::InitGameState(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = invalidate_rental::InvalidateRental::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::InvalidateRental(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = invalidate_ship::InvalidateShip::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::InvalidateShip(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = load_fleet_crew::LoadFleetCrew::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::LoadFleetCrew(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = loading_bay_to_idle::LoadingBayToIdle::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::LoadingBayToIdle(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = loading_bay_to_respawn::LoadingBayToRespawn::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::LoadingBayToRespawn(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = mine_asteroid_to_respawn::MineAsteroidToRespawn::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::MineAsteroidToRespawn(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = mint_certificate::MintCertificate::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::MintCertificate(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = mint_crew_to_game::MintCrewToGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::MintCrewToGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = redeem_certificate::RedeemCertificate::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RedeemCertificate(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_mine_item::RegisterMineItem::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterMineItem(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_planet::RegisterPlanet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterPlanet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                register_progression_config::RegisterProgressionConfig::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterProgressionConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_resource::RegisterResource::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterResource(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_sage_crew_config::RegisterSageCrewConfig::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterSageCrewConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                register_sage_player_profile::RegisterSagePlayerProfile::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterSagePlayerProfile(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                register_sage_point_modifier::RegisterSagePointModifier::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterSagePointModifier(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_sector::RegisterSector::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterSector(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_ship::RegisterShip::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterShip(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_star::RegisterStar::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterStar(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_starbase::RegisterStarbase::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterStarbase(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = register_starbase_player::RegisterStarbasePlayer::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterStarbasePlayer(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                register_survey_data_unit_tracker::RegisterSurveyDataUnitTracker::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RegisterSurveyDataUnitTracker(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_cargo_pod::RemoveCargoPod::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RemoveCargoPod(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_connection::RemoveConnection::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RemoveConnection(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_crew_from_game::RemoveCrewFromGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RemoveCrewFromGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_invalid_ship_escrow::RemoveInvalidShipEscrow::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RemoveInvalidShipEscrow(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_ship_escrow::RemoveShipEscrow::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RemoveShipEscrow(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = respawn_to_loading_bay::RespawnToLoadingBay::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::RespawnToLoadingBay(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = scan_for_survey_data_units::ScanForSurveyDataUnits::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::ScanForSurveyDataUnits(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = set_next_ship::SetNextShip::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::SetNextShip(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = start_crafting_process::StartCraftingProcess::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::StartCraftingProcess(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = start_mining_asteroid::StartMiningAsteroid::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::StartMiningAsteroid(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = start_starbase_upgrade::StartStarbaseUpgrade::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::StartStarbaseUpgrade(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = start_subwarp::StartSubwarp::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::StartSubwarp(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = stop_crafting_process::StopCraftingProcess::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::StopCraftingProcess(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = stop_mining_asteroid::StopMiningAsteroid::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::StopMiningAsteroid(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = stop_subwarp::StopSubwarp::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::StopSubwarp(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                submit_starbase_upgrade_resource::SubmitStarbaseUpgradeResource::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::SubmitStarbaseUpgradeResource(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = sync_starbase_player::SyncStarbasePlayer::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::SyncStarbasePlayer(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                sync_starbase_upgrade_ingredients::SyncStarbaseUpgradeIngredients::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::SyncStarbaseUpgradeIngredients(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = transfer_cargo_at_starbase::TransferCargoAtStarbase::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::TransferCargoAtStarbase(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                transfer_cargo_within_fleet::TransferCargoWithinFleet::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::TransferCargoWithinFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = unload_fleet_crew::UnloadFleetCrew::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UnloadFleetCrew(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_game::UpdateGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_game_state::UpdateGameState::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateGameState(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_mine_item::UpdateMineItem::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateMineItem(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_planet::UpdatePlanet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdatePlanet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_progression_config::UpdateProgressionConfig::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateProgressionConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_resource::UpdateResource::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateResource(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_ship::UpdateShip::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateShip(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_ship_escrow::UpdateShipEscrow::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateShipEscrow(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_ship_in_fleet::UpdateShipInFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateShipInFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_star::UpdateStar::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateStar(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_starbase::UpdateStarbase::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateStarbase(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                update_survey_data_unit_tracker::UpdateSurveyDataUnitTracker::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::UpdateSurveyDataUnitTracker(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = warp_lane::WarpLane::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::WarpLane(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = warp_to_coordinate::WarpToCoordinate::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::WarpToCoordinate(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = withdraw_cargo_from_fleet::WithdrawCargoFromFleet::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::WithdrawCargoFromFleet(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = withdraw_cargo_from_game::WithdrawCargoFromGame::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::WithdrawCargoFromGame(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                withdraw_crafting_ingredient::WithdrawCraftingIngredient::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: SageInstruction::WithdrawCraftingIngredient(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
