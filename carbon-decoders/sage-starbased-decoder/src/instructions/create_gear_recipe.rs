//! This code was AUTOGENERATED using the Codama library.
//!
//! Phase-E crew-individuation addition. Source: `program/src/instructions/admin.rs`
//! (`CreateGearRecipe`, admin.rs:186-194). Gear items ARE `Recipe` rows (master §6.1 A7), so
//! this is the EXISTING `CreateRecipe` arg set under a crew-named alias: arg field order
//! `key_index`, `recipe_id`, `input` — IDENTICAL to `CreateRecipeInstructionArgs` in the
//! sibling `sage-star-frame-decoder` (`generated/instructions/create_recipe.rs`).
//!
//! ── INTERIM INLINE TYPES (delete on IDL regen) ─────────────────────────────────────────────
//! The `sage-starbased-decoder` variant does not decode the recipe-creation path, so its
//! `types/` slice carries NONE of the recipe value-objects. The arg `input:
//! CreateRecipeInputData` and its transitive deps (`RecipeId`, `CargoId`, `ResearchTag`,
//! `U32F32`, `RecipeFeeRecipient`, `StarbaseLevel`, `MinMax`) are therefore defined INLINE at
//! the bottom of this file, byte-exact (field + variant order) to the canonical
//! `sage-star-frame-decoder` codama output. Notes: `U32F32` is `fixed::types::U32F32` (u32 int
//! + u32 frac, 64-bit backing) → its borsh wire is the backing `u64` (this variant carries no
//! `fixed` crate); `Name` is the on-chain `[u8; 64]` (the `Name` newtype is borsh-transparent).
//! When the SAGE IDL is regenerated WITH `create_gear_recipe`, codama emits these into
//! `crate::types::*` — at that point DELETE the inline block below and restore:
//!     use crate::types::CreateRecipeInputData;
//!     use crate::types::RecipeId;
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct CreateGearRecipe {
    /// The index of the key in the player profile.
    pub key_index: u16,
    /// The recipe id (PDA seed).
    pub recipe_id: RecipeId,
    /// The recipe input data (inputs map, ATLAS `fee_amount`, the gear effect-family + tier).
    pub input: CreateRecipeInputData,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateGearRecipeInstructionAccounts {
    // ProfileValidation<true> -> fixed 4-acct prefix (signer, profile, certificate [SAGE_ID-sentinel when none], program); cf. create_recipe.
    pub profile_validation_signer: solana_pubkey::Pubkey,
    pub profile_validation_profile: solana_pubkey::Pubkey,
    pub profile_validation_certificate: solana_pubkey::Pubkey,
    pub profile_validation_program: solana_pubkey::Pubkey,
    pub game: solana_pubkey::Pubkey,
    pub recipe: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CreateGearRecipe {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[19, 246, 183, 35, 27, 120, 73, 19] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for CreateGearRecipe {
    type ArrangedAccounts = CreateGearRecipeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let profile_validation_signer = next_account(&mut iter)?;
        let profile_validation_profile = next_account(&mut iter)?;
        let profile_validation_certificate = next_account(&mut iter)?;
        let profile_validation_program = next_account(&mut iter)?;
        let game = next_account(&mut iter)?;
        let recipe = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CreateGearRecipeInstructionAccounts {
            profile_validation_signer,
            profile_validation_profile,
            profile_validation_certificate,
            profile_validation_program,
            game,
            recipe,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

// ═══════════════════════════════════════════════════════════════════════════════════════════
// INTERIM INLINE recipe value-objects (see file header). Byte-exact to the canonical
// `sage-star-frame-decoder` codama output. DELETE this block on IDL regen — codama emits them
// into `crate::types::*`. Declaration order and enum-variant order are LOAD-BEARING (borsh is
// positional; an enum's variant index IS its 1-byte discriminant). These mirror the existing
// in-repo idioms (`u16` aliases, raw `[u8; 64]` + `serde_big_array`, plain borsh enums —
// see `accounts/mine_item.rs`); `Hash` is added (canonical lacks it) only so the crew-set's
// uniform `#[derive(.. Hash)]` on the instruction struct holds — harmless for a decoder.
// ═══════════════════════════════════════════════════════════════════════════════════════════

/// Recipe id (PDA seed). codama: `pub type RecipeId = u16;`.
pub type RecipeId = u16;
/// Cargo mint id. codama: `pub type CargoId = u16;`.
pub type CargoId = u16;
/// Research-requirement tag. codama: `pub type ResearchTag = u16;`.
pub type ResearchTag = u16;
/// `fixed::types::U32F32` (32 int + 32 frac bits) — 64-bit backing ⇒ `u64` borsh wire. This
/// variant carries no `fixed` crate; the raw backing integer is the decoded value.
pub type U32F32 = u64;

/// Recipe crafting-fee recipient (variant order = borsh discriminant).
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub enum RecipeFeeRecipient {
    Atlas,
    Daoatlas,
}

/// Minimum starbase level gating the recipe (variant order = borsh discriminant).
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub enum StarbaseLevel {
    Level0,
    Level1,
    Level2,
    Level3,
    Level4,
    Level5,
    Css,
}

/// A min/max modifier pair, each a `U32F32` percentage of the base value.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct MinMax {
    pub min: U32F32,
    pub max: U32F32,
}

/// The recipe input data — byte-exact to the canonical `CreateRecipeInputData`
/// (field order = `sage-star-frame-decoder/.../types/create_recipe_input_data.rs`).
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct CreateRecipeInputData {
    pub recipe_id: RecipeId,
    pub duration: u64,
    pub min_duration: u64,
    /// On-chain `Name` = `[u8; 64]` (the `Name` newtype is borsh-transparent).
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub name: [u8; 64],
    pub fee_amount: u64,
    pub fee_recipient: RecipeFeeRecipient,
    pub usage_limit: u64,
    pub min_starbase_level: StarbaseLevel,
    pub value: U32F32,
    pub crafting_efficiency_minmax: MinMax,
    pub crafting_speed_minmax: MinMax,
    pub crafting_crew_count_minmax: MinMax,
    pub crafting_fee_minmax: MinMax,
    pub hab_required: bool,
    pub research_requirements: Vec<ResearchTag>,
    pub inputs: Vec<(CargoId, u64)>,
    pub outputs: Vec<(CargoId, u64)>,
}
