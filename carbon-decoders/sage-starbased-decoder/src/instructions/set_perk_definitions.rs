//! This code was AUTOGENERATED using the Codama library.
//!
//! Phase-E crew-individuation addition. Source: `program/src/instructions/admin.rs`
//! (`SetPerkDefinitions`). Arg field order = admin.rs:120-126: `key_index`, `defs`
//! (`Vec<PerkDefInput>`). Account set = admin.rs:132-146: `profile_validation` (admin
//! `MANAGE_CREW_CONFIG`), `game` (Mut — the tail `crew_perk_definitions` map),
//! `system_program` (the tail growth may need rent for the resize).
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// One perk-catalogue row authored on-chain (the on-chain mirror of the reference `PerkDef`).
/// Codama would emit this under `types/perk_def_input.rs`; inlined here to keep the
/// crew-instruction slice self-contained (the program derives `BorshSerialize/Deserialize +
/// TypeToIdl` on it — admin.rs:97-115). `personality_op == 0xFF` => no personality gate.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq, Eq, Hash)]
pub struct PerkDefInput {
    /// Stable catalogue id (`PerkSlot.perk_id`; `0` reserved = empty).
    pub id: u16,
    /// The no-op-default slot family this perk's delta lands on (raw `SlotFamily` byte).
    pub slot_family: u8,
    /// The trigger gating the perk (raw `Trigger` byte).
    pub trigger: u8,
    /// Magnitude per tier, as the `i16` q-format the summary stores.
    pub mag_per_tier: i16,
    /// The aptitude whose major/anomalous tag gates the offer (raw `Aptitude` byte).
    pub aptitude_gate: u8,
    /// The rarity floor (raw `Rarity` byte).
    pub rarity_floor: u8,
    /// Optional personality gate trait index.
    pub personality_trait_idx: u8,
    /// Optional personality gate comparator (`0xFF` => no gate).
    pub personality_op: u8,
    /// Optional personality gate q0.8 threshold.
    pub personality_threshold_q8: u8,
}

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct SetPerkDefinitions {
    /// The index of the key in the player profile.
    pub key_index: u16,
    /// The perk rows to author (keyed by `id` into `Game.crew_perk_definitions`).
    pub defs: Vec<PerkDefInput>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SetPerkDefinitionsInstructionAccounts {
    // ProfileValidation<true> -> fixed 4-acct prefix (signer, profile, certificate [SAGE_ID-sentinel when none], program); cf. create_recipe.
    pub profile_validation_signer: solana_pubkey::Pubkey,
    pub profile_validation_profile: solana_pubkey::Pubkey,
    pub profile_validation_certificate: solana_pubkey::Pubkey,
    pub profile_validation_program: solana_pubkey::Pubkey,
    pub game: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl SetPerkDefinitions {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[12, 169, 4, 46, 86, 85, 173, 254] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for SetPerkDefinitions {
    type ArrangedAccounts = SetPerkDefinitionsInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let profile_validation_signer = next_account(&mut iter)?;
        let profile_validation_profile = next_account(&mut iter)?;
        let profile_validation_certificate = next_account(&mut iter)?;
        let profile_validation_program = next_account(&mut iter)?;
        let game = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(SetPerkDefinitionsInstructionAccounts {
            profile_validation_signer,
            profile_validation_profile,
            profile_validation_certificate,
            profile_validation_program,
            game,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
