//! This code was AUTOGENERATED using the Codama library.
use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U8;
use carbon_core::postgres::primitives::U16;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct AddConnectionRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub sub_coordinates1: Vec<i64>,
    pub flags1: U8,
    pub sub_coordinates2: Vec<i64>,
    pub flags2: U8,
    pub key_index: U16,
}

impl AddConnectionRow {
    pub fn from_parts(
        source: crate::instructions::add_connection::AddConnection,
        metadata: InstructionMetadata,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            sub_coordinates1: source
                .sub_coordinates1
                .into_iter()
                .map(|element| element.into())
                .collect(),
            flags1: source.flags1.into(),
            sub_coordinates2: source
                .sub_coordinates2
                .into_iter()
                .map(|element| element.into())
                .collect(),
            flags2: source.flags2.into(),
            key_index: source.key_index.into(),
        }
    }
}

impl TryFrom<AddConnectionRow> for crate::instructions::add_connection::AddConnection {
    type Error = carbon_core::error::Error;
    fn try_from(source: AddConnectionRow) -> Result<Self, Self::Error> {
        Ok(Self {
            sub_coordinates1: source
                .sub_coordinates1
                .into_iter()
                .map(|element| Ok(element.into()))
                .collect::<Result<Vec<_>, _>>()?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
            flags1: source.flags1.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            sub_coordinates2: source
                .sub_coordinates2
                .into_iter()
                .map(|element| Ok(element.into()))
                .collect::<Result<Vec<_>, _>>()?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
            flags2: source.flags2.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            key_index: source.key_index.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::add_connection::AddConnection
{
    fn table() -> &'static str {
        "add_connection_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "sub_coordinates1",
            "flags1",
            "sub_coordinates2",
            "flags2",
            "key_index",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AddConnectionRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO add_connection_instruction (
                "sub_coordinates1",
                "flags1",
                "sub_coordinates2",
                "flags2",
                "key_index",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.sub_coordinates1.clone())
        .bind(self.flags1.clone())
        .bind(self.sub_coordinates2.clone())
        .bind(self.flags2.clone())
        .bind(self.key_index.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AddConnectionRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO add_connection_instruction (
                "sub_coordinates1",
                "flags1",
                "sub_coordinates2",
                "flags2",
                "key_index",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "sub_coordinates1" = EXCLUDED."sub_coordinates1",
                "flags1" = EXCLUDED."flags1",
                "sub_coordinates2" = EXCLUDED."sub_coordinates2",
                "flags2" = EXCLUDED."flags2",
                "key_index" = EXCLUDED."key_index",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.sub_coordinates1.clone())
        .bind(self.flags1.clone())
        .bind(self.sub_coordinates2.clone())
        .bind(self.flags2.clone())
        .bind(self.key_index.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for AddConnectionRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM add_connection_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for AddConnectionRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM add_connection_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct AddConnectionMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for AddConnectionMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS add_connection_instruction (
                -- Instruction data
                "sub_coordinates1" INT8[] NOT NULL,
                "flags1" INT2 NOT NULL,
                "sub_coordinates2" INT8[] NOT NULL,
                "flags2" INT2 NOT NULL,
                "key_index" INT4 NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS add_connection_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}
