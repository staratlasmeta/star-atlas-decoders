//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct ProcessPartialDeposit {
    pub claim_stake_quantity: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ProcessPartialDepositInstructionAccounts {
    pub player_account: solana_pubkey::Pubkey,
    pub claim_staking_account: solana_pubkey::Pubkey,
    pub claim_stake_vars_account: solana_pubkey::Pubkey,
    pub escrow_authority: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub claim_stake_mint: solana_pubkey::Pubkey,
    pub from_token_account: solana_pubkey::Pubkey,
    pub claim_stake_escrow: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ProcessPartialDeposit {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[52, 195, 156, 20, 83, 73, 30, 21] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ProcessPartialDeposit {
    type ArrangedAccounts = ProcessPartialDepositInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let player_account = next_account(&mut iter)?;
        let claim_staking_account = next_account(&mut iter)?;
        let claim_stake_vars_account = next_account(&mut iter)?;
        let escrow_authority = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let claim_stake_mint = next_account(&mut iter)?;
        let from_token_account = next_account(&mut iter)?;
        let claim_stake_escrow = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ProcessPartialDepositInstructionAccounts {
            player_account,
            claim_staking_account,
            claim_stake_vars_account,
            escrow_authority,
            system_program,
            token_program,
            claim_stake_mint,
            from_token_account,
            claim_stake_escrow,
            remaining: remaining.to_vec(),
        })
    }
}
