//! This code was AUTOGENERATED using the Codama library.
use crate::types::MaxReserveValuesInputUnpacked;
use crate::types::RewardRateValuesInputUnpacked;
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq, Eq, Hash)]
pub struct ProcessRegisterClaimStake {
    pub reward_rate_per_second: RewardRateValuesInputUnpacked,
    pub claim_stake_max_reserves: MaxReserveValuesInputUnpacked,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ProcessRegisterClaimStakeInstructionAccounts {
    pub funder: solana_pubkey::Pubkey,
    pub update_authority_account: solana_pubkey::Pubkey,
    pub global_vars_account: solana_pubkey::Pubkey,
    pub claim_stake_vars_account: solana_pubkey::Pubkey,
    pub claim_stake_mint: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ProcessRegisterClaimStake {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[78, 163, 242, 247, 23, 44, 87, 171] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ProcessRegisterClaimStake {
    type ArrangedAccounts = ProcessRegisterClaimStakeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let funder = next_account(&mut iter)?;
        let update_authority_account = next_account(&mut iter)?;
        let global_vars_account = next_account(&mut iter)?;
        let claim_stake_vars_account = next_account(&mut iter)?;
        let claim_stake_mint = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ProcessRegisterClaimStakeInstructionAccounts {
            funder,
            update_authority_account,
            global_vars_account,
            claim_stake_vars_account,
            claim_stake_mint,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
