//! This code was AUTOGENERATED using the Codama library.
use crate::ClaimStakeDecoder;
use crate::PROGRAM_ID;

pub mod process_harvest;
pub mod process_initial_deposit;
pub mod process_initialize;
pub mod process_partial_deposit;
pub mod process_register_claim_stake;
pub mod process_settle;
pub mod process_update_claim_stake_vars;
pub mod process_withdraw_claim_stakes;

pub use self::process_harvest::*;
pub use self::process_initial_deposit::*;
pub use self::process_initialize::*;
pub use self::process_partial_deposit::*;
pub use self::process_register_claim_stake::*;
pub use self::process_settle::*;
pub use self::process_update_claim_stake_vars::*;
pub use self::process_withdraw_claim_stakes::*;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[cfg_attr(
    feature = "serde",
    derive(carbon_core::InstructionType, serde::Serialize, serde::Deserialize)
)]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum ClaimStakeInstruction {
    ProcessHarvest(ProcessHarvest),
    ProcessInitialDeposit(ProcessInitialDeposit),
    ProcessInitialize(ProcessInitialize),
    ProcessPartialDeposit(ProcessPartialDeposit),
    ProcessRegisterClaimStake(ProcessRegisterClaimStake),
    ProcessSettle(ProcessSettle),
    ProcessUpdateClaimStakeVars(ProcessUpdateClaimStakeVars),
    ProcessWithdrawClaimStakes(ProcessWithdrawClaimStakes),
}

impl carbon_core::instruction::InstructionDecoder<'_> for ClaimStakeDecoder {
    type InstructionType = ClaimStakeInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = process_harvest::ProcessHarvest::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessHarvest(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_initial_deposit::ProcessInitialDeposit::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessInitialDeposit(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_initialize::ProcessInitialize::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessInitialize(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_partial_deposit::ProcessPartialDeposit::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessPartialDeposit(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                process_register_claim_stake::ProcessRegisterClaimStake::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessRegisterClaimStake(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = process_settle::ProcessSettle::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessSettle(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                process_update_claim_stake_vars::ProcessUpdateClaimStakeVars::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessUpdateClaimStakeVars(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) =
                process_withdraw_claim_stakes::ProcessWithdrawClaimStakes::decode(data)
            {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: ClaimStakeInstruction::ProcessWithdrawClaimStakes(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
