//! This code was AUTOGENERATED using the Codama library.
use crate::ClaimStakeDecoder;
use crate::PROGRAM_ID;

pub mod claim_stake_var;
pub mod claim_staking;
pub mod global_vars;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum ClaimStakeAccount {
    ClaimStakeVar(Box<claim_stake_var::ClaimStakeVar>),
    ClaimStaking(Box<claim_staking::ClaimStaking>),
    GlobalVars(Box<global_vars::GlobalVars>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for ClaimStakeDecoder {
    type AccountType = ClaimStakeAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = claim_stake_var::ClaimStakeVar::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ClaimStakeAccount::ClaimStakeVar(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = claim_staking::ClaimStaking::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ClaimStakeAccount::ClaimStaking(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = global_vars::GlobalVars::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ClaimStakeAccount::GlobalVars(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
