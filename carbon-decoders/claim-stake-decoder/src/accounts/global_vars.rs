//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct GlobalVars {
    /// Account version
    pub version: u8,
    /// Update authority key
    pub update_authority_master: Pubkey,
    /// Mint for Fuel R4
    pub fuel_mint: Pubkey,
    /// Mint for Food R4
    pub food_mint: Pubkey,
    /// Mint for Arms R4
    pub arms_mint: Pubkey,
    /// Mint for Toolkit R4
    pub toolkit_mint: Pubkey,
    /// Bump for Global Vars Account
    pub bump: u8,
}

impl GlobalVars {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[104, 180, 91, 194, 178, 47, 136, 26] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
