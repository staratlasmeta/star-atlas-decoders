//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "AddExistingMemberToRole")]
pub struct AddExistingMemberToRoleGraphQL {
    pub instruction_metadata: crate::instructions::graphql::InstructionMetadataGraphQL,
    pub key_index: i32,
    pub key_index_in_membership_account: i32,
}

impl TryFrom<crate::instructions::postgres::AddExistingMemberToRoleRow>
    for AddExistingMemberToRoleGraphQL
{
    type Error = carbon_core::error::Error;
    fn try_from(
        row: crate::instructions::postgres::AddExistingMemberToRoleRow,
    ) -> Result<Self, Self::Error> {
        Ok(Self {
            instruction_metadata: row.instruction_metadata.into(),
            key_index: (*row.key_index) as i32,
            key_index_in_membership_account: (*row.key_index_in_membership_account) as i32,
        })
    }
}
