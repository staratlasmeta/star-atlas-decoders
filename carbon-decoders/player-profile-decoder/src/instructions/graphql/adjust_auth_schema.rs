//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::AddKeyInputGraphQL;
use carbon_core::graphql::primitives::U8;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "AdjustAuth")]
pub struct AdjustAuthGraphQL {
    pub instruction_metadata: crate::instructions::graphql::InstructionMetadataGraphQL,
    pub auth_indexes: Vec<i32>,
    pub new_key_permissions: Vec<AddKeyInputGraphQL>,
    pub remove_range: Vec<i32>,
    pub new_key_threshold: U8,
}

impl TryFrom<crate::instructions::postgres::AdjustAuthRow> for AdjustAuthGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::instructions::postgres::AdjustAuthRow) -> Result<Self, Self::Error> {
        Ok(Self {
            instruction_metadata: row.instruction_metadata.into(),
            auth_indexes: row
                .auth_indexes
                .into_iter()
                .map(|item| (*item) as i32)
                .collect(),
            new_key_permissions: row
                .new_key_permissions
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
            remove_range: row
                .remove_range
                .into_iter()
                .map(|item| (*item) as i32)
                .collect(),
            new_key_threshold: carbon_core::graphql::primitives::U8((*row.new_key_threshold) as u8),
        })
    }
}
