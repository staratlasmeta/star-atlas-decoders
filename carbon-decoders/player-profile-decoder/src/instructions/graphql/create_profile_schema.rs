//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::AddKeyInputGraphQL;
use carbon_core::graphql::primitives::U8;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "CreateProfile")]
pub struct CreateProfileGraphQL {
    pub instruction_metadata: crate::instructions::graphql::InstructionMetadataGraphQL,
    pub key_permissions: Vec<AddKeyInputGraphQL>,
    pub key_threshold: U8,
}

impl TryFrom<crate::instructions::postgres::CreateProfileRow> for CreateProfileGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::instructions::postgres::CreateProfileRow) -> Result<Self, Self::Error> {
        Ok(Self {
            instruction_metadata: row.instruction_metadata.into(),
            key_permissions: row
                .key_permissions
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
            key_threshold: carbon_core::graphql::primitives::U8((*row.key_threshold) as u8),
        })
    }
}
