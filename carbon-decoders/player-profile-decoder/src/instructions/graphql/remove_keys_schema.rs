//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "RemoveKeys")]
pub struct RemoveKeysGraphQL {
    pub instruction_metadata: crate::instructions::graphql::InstructionMetadataGraphQL,
    pub key_index: i32,
    pub keys_to_remove: Vec<i32>,
}

impl TryFrom<crate::instructions::postgres::RemoveKeysRow> for RemoveKeysGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::instructions::postgres::RemoveKeysRow) -> Result<Self, Self::Error> {
        Ok(Self {
            instruction_metadata: row.instruction_metadata.into(),
            key_index: (*row.key_index) as i32,
            keys_to_remove: row
                .keys_to_remove
                .into_iter()
                .map(|item| (*item) as i32)
                .collect(),
        })
    }
}
