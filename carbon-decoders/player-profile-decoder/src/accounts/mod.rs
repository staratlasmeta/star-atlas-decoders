//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::PlayerProfileDecoder;

pub mod player_name;
pub mod profile;
pub mod profile_role_membership;
pub mod role;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum PlayerProfileAccount {
    PlayerName(Box<player_name::PlayerName>),
    Profile(Box<profile::Profile>),
    ProfileRoleMembership(Box<profile_role_membership::ProfileRoleMembership>),
    Role(Box<role::Role>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for PlayerProfileDecoder {
    type AccountType = PlayerProfileAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = player_name::PlayerName::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PlayerProfileAccount::PlayerName(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = profile::Profile::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PlayerProfileAccount::Profile(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = profile_role_membership::ProfileRoleMembership::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PlayerProfileAccount::ProfileRoleMembership(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = role::Role::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PlayerProfileAccount::Role(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
